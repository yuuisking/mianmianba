- 2026-05-07 17:20 学习中心第二轮体验收口：
  - 顶部体验：学习文档页的模式栏已改为滚动时保持可见，避免下滑后看不到顶部菜单。
  - 训练入口：文档页顶部原“去训练”不再直接跳旧刷题设置页，改为先进入文档内的“面试模式”；同时在文档结尾补充“去面试”入口，直接跳专项训练页，并预填当前文档主题、摘要和追问重点。
  - 题库生成：已关闭 `starterBankBlueprints.ts` 中“一个核心题自动扩成 3-4 道并列导航题”的扩题逻辑；`agentBankBuilder.ts` 已新增硬规则，要求只生成核心题，并对“底层原因 / 项目收益 / 边界条件”这类扩写式标题做归一与去重。
  - 当前判断：新的题库生产方向正式切到“少量核心题 + 文内追问/项目取舍/边界条件 + 训练入口”，后续脏数据需要按新规则重建后再清理旧生成结果。
# 说明文档

## 项目规划
- 项目名称：面面吧（AI 模拟面试平台）
- 当前目标：先打稳线上基础设施与数据存储，再继续功能迭代。
- 当前质量判断：
  - 学习文档内容已达到“小规模试生产”标准，但还不允许全量批量生产。
  - 当前放量前置门槛已收口为三项 P0：流程图稳定性、自测提交后的真实反馈结果卡、面试提交后的真实评分报告。
- 当前范围：
  - 业务主数据已迁移到 PostgreSQL。
  - 学习中心已切换到 `data/learning-center/` 文件优先内容仓，`data/learning-center.json` 仅保留为迁移过渡输入。
  - 线上部署运行于阿里云 ECS `47.95.233.109`。
- 继续开发原则：
  - 不扩展未规划范围。
  - 每完成一项任务，立即补充进度记录。
  - 上线问题优先于新功能开发。

## 实施方案
- 代码与文档：统一通过 GitHub 仓库 `https://github.com/yuuisking/mianmianba.git` 管理。
- 应用部署：
  - 应用目录：`/srv/resumer`
  - 应用服务：`systemctl status resumer`
  - 反向代理：`systemctl status nginx`
- 运维安全基线：
  - 运维文档：`doc/ops-security-baseline.md`
  - 备份脚本：`/srv/resumer/scripts/ops/backup-postgres.sh`
  - 恢复校验：`/srv/resumer/scripts/ops/verify-postgres-backup.sh --restore-check`
  - 定时任务：`/etc/cron.d/resumer-postgres-backup`（每天 `03:17`）
  - 轮换脚本：`/srv/resumer/scripts/ops/rotate-db-password.sh`、`/srv/resumer/scripts/ops/rotate-auth-secrets.sh`
- 数据存储：
  - 主库：PostgreSQL `resumer_prod`
  - 运行态文件：`.env.local`
  - 学习中心数据：`data/learning-center/`（`index.json` 兼容索引 + `kbs/*` / `drafts/*` 内容文件）
- 本阶段重点：
  - 保证登录、注册、面试记录、报告等核心链路稳定可用。
  - 完成线上配置问题修复与交接信息固化。
  - 持续收口学习中心与学习管理首页，优先保持极简入口和可编译状态。

## 进度记录
- 2026-05-09 当前：开始执行工作区收口，已完成首轮临时文件清理、`.gitignore` 补充与提交前验证。
  - 清理结果：已删除明确不该入库的临时内容，包括 `tmp/`、`test-results/`、根目录本地导出文件 `test.pdf / test.md / test.txt / test2.md / test3.txt / test.jsonl / 杨宇-四年工作经验-高级Java研发工程师.pdf / 验收问题.docx`。其中 `test.*` 为历史已跟踪文件，本轮将以正式删除的方式收口，不再继续留在仓库中。
  - 忽略规则结果：`.gitignore` 已补充 `tmp/`、`test-results/` 以及上述本地导出文件的忽略规则，后续同类临时产物不会再反复进入未跟踪列表。
  - 验证结果：`node ./node_modules/next/dist/bin/next build --webpack` 通过；`npm run lint` 未通过，失败原因为仓库当前 `node_modules/.bin/eslint` 入口损坏并报 `Cannot find module '../package.json'`；`node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 未通过，失败原因为 `tsconfig.json` 继续包含 `.next/types/**/*.ts`，而当前 `.next/types` 产物不完整，报大量 `TS6053` 文件不存在。这两项属于当前仓库环境级问题，已记录，不在本轮“工作区收口”范围内额外扩修。
  - 下一步：将剩余正式资产按“正式代码提交”和“文档/spec 提交”拆分为独立逻辑提交，继续降低工作区噪音并为双验收窗口提供清晰边界。
- 2026-05-08 23:48 当前：完成 P0/P1 第一轮代码落地，并完成服务端验证。
  - 图表稳定性结果：新增标准图组件 `src/components/learning/LearningDiagram.tsx`，当前阅读页图解改为“稳定流程主线 + 图下注释 + 原图查看”双层展示；页面已支持 `放大查看 / 复制图 / 下载图`，并在打印样式中避免图解块被拆页、避免原始 Mermaid 在打印时继续撑坏版式。
  - 训练结果页结果：`src/app/learning/_components/LearningAssessmentPanels.tsx` 已把自测、面试、追问的反馈卡统一升级为结果页结构，补齐了得分摘要、答得好的地方、遗漏点、事实错误、为什么这样答、推荐答案 / 改进版回答、下一步建议等区块。
  - 评估接口稳态结果：新增 `src/lib/auth/getOptionalServerSession.ts`，学习评估与学习进度相关 API 在浏览器携带失效 `next-auth` cookie 时，改为按匿名用户继续而不是直接抛错；已用带坏 cookie 的 `curl` 实测 `POST /api/learning/assessment` 返回 `200` 且能拿到完整结构化评分 JSON。
  - 构建与部署结果：本轮改动已重新通过 `node ./node_modules/next/dist/bin/next build --webpack`，并已精简包直传 ECS、执行 `npx prisma generate`、重启 `resumer` 完成上线。
  - 当前边界结果：服务端 P0 能力已经落地；浏览器页若仍保留旧 JS chunk，可能继续显示旧错误提示，复验时需先强刷。下一轮继续优先做面试模式浏览器端的最终交互复测，并把标准图数据协议逐步回灌到种子内容。
- 2026-05-08 23:20 当前：吸收最新一轮“文档质量 / 训练闭环 / 图表稳定性”验收反馈，并收口下一轮优先级。
  - 用户判断结果：当前版本已经不是“AQS 内容不对”，而是“流程图展示不稳定 + 训练闭环还没真正落地”；结论已统一为“允许小规模试生产，不允许全量批量生产”。
  - 内容判断结果：AQS 15 分钟深读结构、主线和专题性已达到可继续扩样的水平，`volatile state`、CAS、CLH 风格等待队列、Node 入队、park/unpark、独占/共享模式、Condition、state 映射与工程落地均已进入可用母版。
  - P0 任务结果：下一轮必须优先解决三项能力后再考虑放量：
    - 流程图展示稳定性：节点缩短、图下注释、避免跨页拆裂、支持放大、图表渲染失败不得发布。
    - 自测反馈闭环：提交后必须返回得分、命中点、遗漏点、事实错误、推荐答案、为什么这样答、建议回看章节。
    - 面试训练闭环：提交后必须返回总分、必答点命中、加分点命中、事实错误、表达建议、改进版答案与下一轮追问。
  - P1 任务结果：在 P0 完成后推进标准图组件化和 20-30 篇核心题的小规模试生产，继续禁止直接全量铺开。
- 2026-05-08 23:12 当前：完成学习页滚动遮挡的结构性修复，并已重新部署 ECS 后复测通过。
  - 根因结果：上一版虽然给模式工具条加了 sticky 和 spacer，但标题区仍然是单独流式块；滚动后正文会很快顶到工具条下沿，体感上仍像“正文滑进了菜单区”，所以用户会持续感觉没有修好。
  - 结构修复结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/QuestionDetailClient.tsx` 已把 `learning-reader__toolbar` 和 `learning-reader__header` 合并进同一个 `learning-reader__sticky-shell`；`src/app/globals.css` 已改为由这个整体 sticky 顶栏统一承接模式条和标题区，正文只能滑到这整个顶栏下面，不再只固定模式条而放任标题区与正文分离。
  - 验证与部署结果：已重新执行 `node ./node_modules/next/dist/bin/next build --webpack`，随后通过精简包直传 ECS、远端 `npx prisma generate`、`systemctl restart resumer` 完成上线；当前 `systemctl is-active resumer` 为 `active`，公网目标题目页 `GET` 返回 `200`，并已在浏览器中重新打开 AQS 页面实际下滚复测，当前可见状态为“模式条 + 标题区”整体固定，正文从其下方开始连续滚动。
- 2026-05-07 16:35 前后：完成学习中心 V2 第一轮收口，正式把公开文档链路、后台链路和学习进度主链路继续切向 Prisma 文档系统。
  - 后台结果：`/admin/learning` 与 `/admin/learning/[kbId]` 已从旧“题库工坊 run 记录”视角切换为题库 / 文档 / 审核 / AI 任务视角；`/api/admin/learning/studio*`、`/api/admin/learning/kbs/[kbId]`、`/api/admin/learning/generate-kb` 已不再依赖旧 `knowledgeBase` 作为正式管理模型，并新增 `documents`、`documents/[id]/versions`、`review-tasks`、`ai-tasks` 四组后台接口。
  - 文档链路结果：`/api/learning/kbs`、`/api/learning/kbs/[kbId]/categories`、`/api/learning/questions/[questionId]/assistant`、`/api/learning/progress` 已改为围绕 `TopicBank / Chapter / Document / DocumentVersion / LearningProgress / Favorite` 工作，不再把旧 `Question` 体系当成学习中心正式主链路。
  - 契约结果：`content-contract.ts` 已为 `SelfTest` 增补统一展示标签兼容；`interviewTrainingService.ts` 已移除不存在的 `qaHistory` 写入；题目详情页自测区已不再依赖 `test.level` 旧字段。
  - 种子结果：`v2Seeder.ts` 已修正来源材料写入顺序，避免来源材料依附在 tag 循环内部，保证首批标杆文档导入时来源信息稳定落库。
  - 前台结果：题库卡片继续向参考项目靠拢，补充副标题层级；文档页原有 Prisma 文档详情链路保持不变，继续沿用语雀式目录 + 白底正文方向。
  - 验证结果：由于当前仓库 `tsc/eslint` 命令在本机存在长时间无输出的环境级不稳定，本轮改用 TypeScript 编译器 API 对新增和改动文件做语法级快速校验，结果通过；整仓类型检查仍建议后续结合目标环境继续做一次完整验证。
- 2026-05-07 01:18 当前：开始按 `reference/yuque` 参考样稿收首批黄金题，已完成 `Java 集合 / Java 并发 / 操作系统` 三套题库中的重点题富内容升级并重建落库。
  - 内容升级结果：已在 `src/lib/learning/starterBankBlueprints.ts` 中新增“黄金样稿覆盖”能力，不再只靠通用模板扩写；首批命中题包括 `ArrayList 和 LinkedList 有什么区别？`、`ArrayList 和 Vector 有什么区别？`、`Iterator 的 fail-fast 机制是什么？`、`CAS 和 AQS 分别解决了什么问题？`、`线程上下文切换为什么成本高？`。这些题目的 `知识点总结` 已升级为带 Markdown 表格、代码示例、工程选型说明和误区提示的富内容文档。
  - 数据回灌结果：已使用本地 `jiti` 脚本重建 `Java 集合 / Java 并发 / 操作系统` 三套 starter 题库，当前重建结果为 `warnings = 0`；抽查落库文件确认新样稿中的表格、代码块和 30 秒 / 1 分钟 / 深入追问答案都已成功写入文件仓数据。
- 2026-05-07 00:55 当前：重新阅读 `doc/learning-content-quality-system.md` 最新版后，确认后续内容提质应从“文本润色”升级为“事实卡片 + 题型规划 + 富内容块样稿”三线并行。
  - 方案判断结果：现有 `contentQuality.ts` 与 `agentBankBuilder.ts` 的硬规则、风险等级和自动发布决策方向是正确的，但仍只解决了“别把明显坏内容放出去”，还没有解决“怎样稳定产出 99 分内容”；下一步重点应转到事实卡片、题型驱动规划、V2 富内容块和黄金样稿，而不是继续靠三段纯文本微调。
  - UI 小优化结果：已将学习题目阅读页 `src/app/globals.css` 中 `learning-reader` 相关容器背景收口为纯白色，只影响学习文档阅读页面，不影响全站其他页面底色。
- 2026-05-07 00:46 当前：已恢复 ECS 线上服务，学习中心主链路重新可验收。
  - 构建收口结果：重新以本地 `node ./node_modules/next/dist/bin/next build --webpack` 验证当前版本时，先暴露出 `/api/speech/asr` 在构建期加载 `undici` 触发的 `webidl.util.markAsUncloneable is not a function`；已通过在 `src/lib/speech/volc.ts` 中将 `undici` 改为运行时惰性加载，并在 `next.config.ts` 中补充 `serverExternalPackages: ["undici"]`，避免 Next 16 在构建期提前初始化该语音 WebSocket 兼容层。
  - 部署结果：本地 `webpack` 构建已成功生成 `.next/BUILD_ID` 与完整服务端产物，虽然最终在 `Collecting build traces` 阶段仍偶发本机文件系统 `ETIMEDOUT: connection timed out, read`，但产物已可用于运行；随后已通过 `./scripts/ops/deploy-local-build-to-ecs.sh --skip-backup` 直传 ECS，绕过远端 1.8G 机器的 Next 16 构建 OOM。
  - 线上验证结果：ECS `systemctl status resumer` 当前为 `active (running)`，启动日志显示 `Next.js 16.2.4` 已 `Ready in 302ms`；公网复核通过，`GET /learning` 返回 `200`，`GET /api/learning/banks` 返回 `200` 且包含两套 24 题样板库，`GET /learning/bank-java20-59-7-180/...` 返回 `200`，题目详情接口抽查已确认 `Java 值传递和引用传递有什么区别？` 仍为“知识点总结 / 面试常问 / 参考答案和解析”的有效内容结构。
- 2026-05-06 23:55 当前：吸收 `doc/learning-content-quality-system.md` 最新方案后，完成题目页文档壳收口、内容质量本地硬门禁升级，以及 `Java 基础` / `Java 集合` 两套样板题库第二轮重建，准备部署 ECS。
  - 方案收口结果：已将最新质量方案并入现有执行面，当前 `src/lib/learning/contentQuality.ts` 不再只是做维度打分，还新增了标题模板化检测、难度与正文深度一致性检查、工程迁移信息提醒、风险等级 `R0~R4` 和自动决策 `auto_publish / auto_rewrite / manual_queue / discard`；`src/lib/learning/agentBankBuilder.ts` 已同步切到“只有无阻断且高分内容才能自动发布”的发布口径，避免审核链路再次把模板味内容直接放行。
  - 前台壳子结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/QuestionDetailClient.tsx` 已进一步收成“左侧整库目录 + 中间连续正文”双栏结构，移除右侧目录、底部分页和多余工作台感；`src/app/globals.css` 已清掉残留旧样式并补齐更接近语雀/文档阅读页的排版、难度色签和正文样式，继续降低题目页 AI 味和卡片感。
  - 样板题库结果：`src/lib/learning/starterBankBlueprints.ts` 已补齐 `Collection vs Collections`、`Queue / Deque / BlockingQueue`、`LinkedHashMap`、`TreeMap`、`BlockingQueue 在生产者消费者模型中的价值`、`遍历集合时直接增删元素`、`集合选型`、`集合性能排查` 等知识模板，并修正 `LinkedHashMap` 被 `HashMap` 通用模板误命中的问题；随后重新执行样板题库重建，当前 `Java 基础题库`、`Java 集合题库` 均为 `24` 题，`warnings` 为 `0`，抽查 `Collection 和 Collections 有什么区别？`、`TreeMap 为什么能排序？`、`如何排查集合使用导致的内存和性能问题？` 的 `参考答案和解析` 已改为可直接复述的标准回答稿。
  - 部署阻塞结果：本地 `next build` 与 `next build --webpack` 仍会长时间卡在 `Creating an optimized production build ...`；改为手工打包源码并同步到 ECS 后，远端 `npm run build` 与 `node ./node_modules/next/dist/bin/next build --webpack` 依旧都会出现 `Next.js build worker exited with code: null and signal: SIGKILL`。尝试以 `next dev --webpack` 临时托底时，进程能 `Ready`，但一触发 `/api/learning/banks` 编译就再次退出，当前 ECS 仍未恢复到稳定可验收状态。后续需要继续处理 Next 16 构建 worker 在当前 ECS 规格下的内存/编译问题。
- 2026-05-06 21:40 当前：完成内容质量专项第一轮收口，已重构知识点模板并重建 `Java 基础`、`Java 集合` 两套样板题库后部署 ECS。
  - 模板治理结果：`src/lib/learning/starterBankBlueprints.ts` 已重写对比类扩题规则，去掉“的底层实现差异会带来哪些性能影响”这类题目换皮尾巴；同时把默认知识点兜底从“先说明 / 再讲 / 然后补充”的答题提纲，改成真正讲概念、原理、场景、边界与取舍的知识表达。`src/lib/learning/contentQuality.ts`、`src/lib/learning/agentBankBuilder.ts`、`src/lib/learning/autoFactory.ts` 也同步收口，避免后台其他生成链路继续产出“回答时 / 如果面试官继续追问”式脚手架文案。
  - 样板重建结果：已基于最新源码重新执行 `seedStarterBanks({ topics: ['Java 基础', 'Java 集合'], resetExisting: true })`，两套题库当前 `warnings` 均为 `0`；`Java 值传递和引用传递有什么区别？` 的知识点总结已恢复为原理讲解，相关衍生题标题也已替换为更自然的知识问题，不再出现用户指出的那种答题模板式知识点。
  - 验证与部署结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`npm run build` 并通过 `./scripts/ops/deploy-local-build-to-ecs.sh` 发布到 ECS；本轮后续将继续按专项 spec 扩展到其他重点题库。
- 2026-05-06 当前：按用户要求将题目页智能助手恢复为原来的打开模式，并为题库内容质量建立独立专项 spec。
  - 助手恢复结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/QuestionDetailClient.tsx` 已移除 `triggerMode="hover"`，题目页智能助手恢复到原来的常规打开模式；已重新执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`npm run build` 并通过 `./scripts/ops/deploy-local-build-to-ecs.sh` 部署到 ECS。
  - 专项规划结果：新增 `.trae/specs/learning-content-quality-hardening/spec.md`、`tasks.md`、`checklist.md`，将题库内容质量从零散修补升级为单独专项，明确了质量目标、重点题库、治理策略、任务顺序与验收口径，后续内容提质将严格按该专项推进。
- 2026-05-06 当前：开始执行“学习中心秒开”性能专项，已完成题目页服务端首屏直出和学习数据仓缓存。
  - 首屏链路结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已从整页客户端取数改为服务端首屏直出，并新增 `QuestionDetailClient.tsx` 只承接阅读模式、答案展开和进度上报等轻交互；当前用户打开题目时不再先经历一轮前端空白等待。
  - 数据层结果：新增 `src/lib/learning/questionDetail.ts` 统一处理题目详情读取，并让题目详情优先命中文件仓题库数据；`src/lib/db/learningDb.ts` 已补充进程内聚合缓存，避免每次请求都重新遍历题库文件并 `JSON.parse` 整套内容。
  - 导航结果：首页 `src/app/learning/_components/LearningCenterClient.tsx` 已改为 `Link` 直达首题并启用预取；题目页目录树、上一题 / 下一题、关联题和去训练入口都改成预取型导航，减少连续学习时的切题等待。
  - 验证与部署结果：已重新执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；本地热态实测同一路由首题页面响应已压到约 `0.055s`，相比调整前的 `2s+` 有明显改善。随后执行 `./scripts/ops/deploy-local-build-to-ecs.sh` 完成 ECS 部署；公网复核通过：`GET /learning` 首次约 `0.089s`，题目页首题路由首次约 `0.476s`，热态已压到约 `0.068s ~ 0.145s`。
- 2026-05-06 当前：开始处理最新一轮学习中心验收问题，已完成首页头部删除、卡片按钮对齐和进入题库链路性能收口。
  - 首页结果：`src/app/learning/page.tsx` 与 `src/app/learning/_components/LearningCenterClient.tsx` 已改为服务端直接注入题库数据，去掉首页顶部“学习中心 / 题库”整块头部；当前首页首屏直接展示搜索、分类和题库列表，减少了一次客户端拉取等待，并避免顶部区域继续和站点菜单形成视觉遮挡。
  - 卡片结果：`src/lib/learning/bankStudio.ts` 已为每个题库补充 `defaultQuestionPath`，首页卡片统一直接跳首题真实地址；`src/app/globals.css` 已把卡片改成统一高度与底部操作区结构，`进入` 按钮固定在卡片底部同一基线，不再随标题和摘要长度上下漂移。
  - 性能结果：`src/app/api/learning/questions/[questionId]/route.ts` 已支持在题目详情响应里合并返回整库目录树，并附加短时缓存头；`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已从“题目详情 + 题库目录”双请求收口为单请求，同时将题目页助手改为右侧 hover 展开并通过 `dynamic` 延迟加载，优先保障正文与目录先出。
  - 验证与部署结果：已重新执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；随后执行 `./scripts/ops/deploy-local-build-to-ecs.sh` 完成 ECS 部署。公网复核通过：`GET http://47.95.233.109/learning` 返回 `200`，`GET /api/learning/banks` 返回 `200` 且已包含 `defaultQuestionPath` 字段，说明首页直达首题的新链路已经在线生效。
- 2026-05-06 当前：完成学习中心第二轮收口，恢复首页助手、重做知识点模式，并将 20 个题库统一扩到 52 题后重新部署 ECS。
  - 首页助手结果：新增 `src/app/learning/_components/LearningHomeAssistant.tsx`，并在 `src/app/learning/_components/LearningCenterClient.tsx` 重新挂回首页右下悬浮学习智能助手入口；该入口只恢复首页自由提问场景，不修改原有“面面吧智能助手”核心链路。线上快照已确认学习中心首页右下再次出现悬浮助手。
  - 阅读模式结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已继续收口双模式。当前 `知识点模式` 不再显示题目题干和 quickFacts 气泡卡，改为直接输出和当前题目相关的连续知识讲解文档；`面试模式` 保留 `题目题干 / 面试常问 / 查看答案 / 参考答案和解析` 的刷题链路。线上快照已确认知识点模式仅保留知识讲解正文，面试模式仍保留题目与答案结构。
  - 扩题结果：`src/lib/learning/starterBankBlueprints.ts` 已将每道 starter 基础题扩展为 4 道同主题衍生题，并继续保留 `高频追问与实战` 分组；随后通过 `seedStarterBanks()` 重建 20 个题库。当前 20 个题库全部扩到 `52` 题，满足“50～66”范围要求，且 `warnings` 为 `0`。
  - 质量结果：`src/lib/learning/contentQuality.ts` 继续承担正式文档结构归一化和质量评分，保证扩题后仍保持 `知识点总结 / 面试常问 / 参考答案和解析` 三段结构。接口抽查 `Java 集合题库` 已确认章节结构稳定，未因扩题而回退为低质量空壳内容。
  - 验证与部署结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`npm run build`、`seedStarterBanks()` 重建数据，并再次执行 `./scripts/ops/deploy-local-build-to-ecs.sh` 部署 ECS。公网复核通过：`GET http://47.95.233.109/learning` 返回 `200`，`GET /api/learning/banks` 返回 `20` 个题库且每库 `52` 题；线上页面快照已确认首页助手恢复、题库直达首题仍正常、知识点模式为纯知识文档。
- 2026-05-06 当前：完成学习中心“最后一公里”收口并重新部署 ECS。
  - 首页与入口结果：`src/app/learning/_components/LearningCenterClient.tsx` 与 `src/app/globals.css` 已把学习中心改成更宽的内容区，题库卡片去掉首字母图标，改为轻量配色卡片；筛选区压缩为更克制的一屏数量，降低首页拥挤感。`src/app/learning/[kbId]/page.tsx` 已改成服务端直达首题，用户进入题库后不再停留在中间选择页。
  - 阅读页结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已重做为整库目录树 + 超宽正文 + 右侧阅读辅助的学习界面，顶部动作只保留 `查看答案` 和 `去训练`，并新增 `知识点模式 / 面试模式` 双模式切换；线上快照已确认 `Java 集合题库` 进入后左侧直接展示 4 组目录、当前题目正文优先展示知识点总结和面试常问。
  - 文档质量结果：新增 `src/lib/learning/contentQuality.ts` 统一执行三段式文档归一化和质量评分；`src/lib/learning/agentBankBuilder.ts` 已将 `知识点总结 / 面试常问 / 参考答案和解析` 作为正式文档硬门槛，并在审核阶段结合结构、信息密度、主题相关性、面试可用性进行拦截；后台页面已补充质量 warnings 展示，便于后续继续提质。
  - 扩题结果：`src/lib/learning/starterBankBlueprints.ts` 已为所有 starter 题库追加 `高频追问与实战` 分组，随后通过 `seedStarterBanks()` 本地重建 20 个题库；当前 `Java 集合题库` 已从 9 题扩到 13 题，目录为 `集合体系总览 / Map 与 Set / 并发集合 / 高频追问与实战`，其他细分题库同步扩题。
  - 验证与部署结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`npm run build`，结果通过；随后执行 `./scripts/ops/deploy-local-build-to-ecs.sh` 重新部署。公网校验通过：`GET http://47.95.233.109/learning` 返回 `200`，`GET /api/learning/banks` 返回 `20` 个题库，`Java 集合题库` 返回 `13` 道题且题目接口章节为 `知识点总结 / 面试常问 / 参考答案和解析`。
- 2026-05-05 当前：完成学习中心本轮验收修复并重新部署 ECS。
  - 题目页样式结果：`src/app/globals.css` 已删除一整段重复的 `learning-workspace / learning-doc-header / learning-outline-card` 旧样式定义，只保留当前题目阅读页主样式，修复了向下滚动和响应式切换时的布局覆盖问题；`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 保持去掉顶部冗余元信息块后的结构，线上快照确认题目页顶部已不再出现“题库 / 分类 / 来源”等红框信息块。
  - 首页卡片结果：`src/app/learning/_components/LearningCenterClient.tsx` 与 `src/app/globals.css` 已继续压缩题库卡片尺寸和信息密度，当前卡片改为更紧凑的五列优先布局、缩小封面块、减少标签数量、弱化底部文案，只保留更接近 `mianshiya` 的题库名、题量、摘要、标签与更新时间。
  - 内容纠偏结果：线上 `Java 集合题库` 重新核对通过，当前目录已恢复为 `集合体系总览 / Map 与 Set / 并发集合`；题目接口已返回 `知识点总结 / 面试常考 / 参考答案 / 项目延伸` 四类内容，首题 `List、Set、Map 有什么区别？` 的知识点正文已为具体集合知识，而不是跑偏的通用 Java 内容。
  - 验证与部署结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；随后执行 `./scripts/ops/deploy-local-build-to-ecs.sh` 完成重新部署。公网复核 `GET http://47.95.233.109/learning` 返回 `200`，`GET /api/learning/banks` 返回 `20` 个题库且包含 `Java 集合题库`，线上题目页快照已确认只保留 `题目题干 / 知识点总结 / 面试常考 / 关联题目` 等有效内容区。
- 2026-05-05 当前：继续按最新验收意见收口题目详情页样式，并修复首批题库“只有壳子、没有学习内容”的问题。
  - 题目详情页结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已删去顶部“阅读方式 / 当前状态”大说明块，改为更接近 `mianshiya` 的紧凑题目头部；当前主内容区固定展示 `题目题干`、`知识点总结`、`面试常考`，并在用户展开后展示 `参考答案`，弱化原来的说明页感。
  - 内容结构结果：`src/lib/learning/autoFactory.ts` 与 `src/lib/learning/agentBankBuilder.ts` 已把首批题库内容改为题目导向结构，统一生成 `知识点总结`、`面试常考`、`参考答案`、`项目延伸` 四类内容；`src/lib/learning/questionBankFallback.ts` 与 `src/app/api/learning/questions/[questionId]/route.ts` 已补充 `quickFacts / sections` 返回，使前台可以直接按结构渲染，而不是只拼一整段纯文本。
  - 本地抽查结果：已重新通过后台真实接口以 `quickStart=true` 重建 20 个题库；抽查 `Java 基础题库 -> Java 基础语法与面向对象` 时，题目接口当前已返回 `6` 条 `quickFacts`，章节已包含 `知识点总结 / 面试常考 / 参考答案 / 项目延伸`，说明首批题库内容已从“结构壳子”切到“可学习内容”。
  - 验证与部署结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、定向 `eslint` 与 `npm run build`，结果通过；随后重新部署 ECS 并准备做线上抽查。
- 2026-05-09 当前：已按“不要集中到一类题库上”的要求完成首批试生产导入，当前线上正式试生产规模为 `20` 篇，四个核心题库均衡分布。
  - 试生产导入结果：本轮先在 `src/lib/learning/v2SeedData.ts` 新增 `8` 篇种子题，覆盖 `MySQL / Redis / Java 并发 / JVM` 各 `2` 篇；随后通过本地直连生产库执行 `seedLearningCenterV2(resetExisting: false)`，成功把试生产题量从 `12` 篇扩到 `20` 篇。
  - 线上分布结果：当前四个核心题库均为 `5` 篇，没有出现单类题库堆积。
    - `Java 并发核心专题`：`5` 篇
    - `JVM 核心专题`：`5` 篇
    - `MySQL 核心专题`：`5` 篇
    - `Redis 核心专题`：`5` 篇
  - 本轮新增题目清单：
    - MySQL：`覆盖索引和回表到底该怎么讲`、`为什么在 RR 下还要用间隙锁防幻读`
    - Redis：`缓存击穿和雪崩为什么不能混着答`、`Redis 分布式锁为什么经常翻车`
    - Java 并发：`synchronized 和 ReentrantLock 该怎么讲取舍`、`ThreadLocal 为什么容易引发内存泄漏`
    - JVM：`同一个类为什么被不同 ClassLoader 加载后就不相等`、`线上 Full GC 频繁到底该怎么排查`
  - 技术落地说明：由于 ECS 侧 `jiti` 直跑 `ts` 模块会触发解析器异常，本轮改为通过 SSH 隧道把生产库映射到本地，再由本地 TypeScript 转译执行器直接调用 `seedLearningCenterV2`，已确认真实写入生产库。该导入路径后续可继续复用，避免再次被远端运行时工具链卡住。
  - 当前结论：首批试生产已满足“跨题库均衡扩容”的要求，可以进入人工抽检与逐篇验收阶段；在完成抽检前，仍保持“已发布但按试生产看待”的边界，不建议直接继续全量扩容。
- 2026-05-09 当前：继续执行评分稳定性收口，已完成评估服务端 hardening、ECS 部署和连续回归验证，并补齐小规模试生产建议清单。
  - 根因排查结果：本轮未稳定复现应用层 `POST /api/learning/assessment -> 502`。远端直连 `POST /api/learning/assessment` 当前返回 `200`，DeepSeek 探针也返回正常；结合日志看，之前已确认的一次大面积异常主要出现在服务重启后的失效 cookie/JWT 解密噪音附近。现阶段更接近“上游或运行态短暂抖动 + 服务端缺少额外 hardening”，而不是评估提示词本身必然报错。
  - 模型调用 hardening 结果：`src/lib/ai/deepseek.ts` 已新增超时、可重试错误识别、退避重试与统一超时错误码，避免 DeepSeek 偶发超时或 5xx 时立即把训练接口拖成失败。当前评估链路默认 `18s timeout + 2 次重试`。
  - 评估引擎结果：`src/lib/learning/assessmentEngine.ts` 已将学习评估调用切到带超时/重试的 DeepSeek 调用；若模型超时、429、5xx 或连接抖动，当前会直接记录 warning 并回退到规则化评分，不再因为上游模型异常直接中断自测/面试闭环。
  - 持久化隔离结果：`src/lib/learning/assessmentService.ts` 已把 `learning_progress` 同步和面试评分落库改为 best-effort。当前即使进度写入或会话写入异常，也只会记录 warning，不会影响前台拿到评分结果。
  - 部署与回归结果：本地 `next build --webpack` 通过后，已重新部署 ECS。远端使用相同 AQS 主问题连续执行 `5` 次评分回归，结果全部 `200`，单次耗时约 `12.44s - 15.33s`，均返回结构化评分 JSON，当前未再出现非 `200`。
  - 当前判断：评分链路已从“偶发抖动可能直接让前台失败”收口到“模型抖动优先服务端降级，持久化抖动不阻断前台结果”。后续仍建议继续压缩平均响应时间，并持续观察是否还有真实 `502` 样本。
  - 小规模试生产建议清单：建议先按 `20-30` 篇推进，并统一标记为“草稿 / 待审核 / 人工抽检”。推荐范围如下。
    - Java 并发：`AQS 为什么能成为 Java 并发包的同步器基础`、`volatile 为什么能保证可见性却不能直接保证复合操作原子性`、`synchronized 和 ReentrantLock 该怎么讲取舍`、`线程池参数为什么不能只背 corePoolSize 和 maxPoolSize`、`ThreadLocal 为什么容易引发内存泄漏`、`CAS 为什么会有 ABA 问题`
    - JVM：`JVM 内存结构和运行时数据区怎么区分`、`对象创建到回收经历了什么`、`G1 和 CMS 该怎么讲差异`、`双亲委派为什么重要`、`类加载全过程怎么讲`、`线上 Full GC 频繁怎么排查`
    - MySQL：`MySQL 为什么选择 B+Tree 作为索引底层结构`、`事务四大特性是怎么落地的`、`MVCC 是怎么工作的`、`最左前缀原则怎么解释`、`覆盖索引和回表该怎么讲`、`深分页为什么慢`
    - Redis：`缓存穿透为什么会压垮数据库，应该怎么治理`、`缓存击穿和雪崩怎么区分`、`RDB 和 AOF 为什么要一起理解`、`Redis 为什么快`、`Redis 分布式锁有哪些坑`、`Redis 主从复制和哨兵怎么讲`
    - 试生产门禁：继续沿用现有三类门禁，不满足则不发布。文档必须满足固定骨架；图表必须满足短词、可放大、不断页；训练必须保证提交后一定有结果或降级结果。
- 2026-05-09 当前：独立 AI 抽检 agent 已修通真实写库链路，当前已完成单篇文档真跑、写库核对与构建验证，准备继续部署 ECS。
  - 阻塞修复结果：`src/lib/learning/qualityAgent.ts` 已按 `prisma/schema.prisma` 实际模型修正字段与关系，不再误用不存在的 `Document.currentVersion`、`Document.qualityReportId`、`AiTask.inputPayload/outputSummary`、`Document.sortOrder`。当前改为按 `currentVersionId -> document_versions` 读取当前版本，`QualityReport` 按真实字段写入 `totalScore / factScore / learningScore / interviewScore / originalityScore / readabilityScore / codeDiagramScore / issues / suggestions / pass`，`AiTask` 改为写 `input / output`，整库抽检也增加了“单篇失败不拖垮整批”的容错。
  - 单篇真跑结果：已通过本地 TypeScript 转译执行器 + 生产库 SSH 隧道，对文档 `cmowzn2j5003tpaluvjj8n2be` 真实执行 `runDocumentQualityAgent(...)`，返回 `score=78 / riskLevel=high / recommendation=block`，说明 agent 不只是编译通过，而是已经能真实读取线上 `DocumentVersion.learningContent` 并给出结构化质检结果。
  - 写库核对结果：已确认同一轮执行后，生产库里真实新增了 `QualityReport`、`ReviewTask(reviewType=AI_QUALITY_CHECK)`、`AiTask(taskType=QUALITY_AUDIT)` 三条记录；文档 `qualityScore` 已更新为 `78`，并因当前建议为 `block` 被回退为 `DRAFT`。这说明“AI 初检 -> 质量报告 -> 审核任务 -> AI 任务日志”的最小闭环已经打通。
  - 构建验收结果：本地再次执行 `node ./node_modules/next/dist/bin/next build --webpack` 已通过，`/api/admin/learning/quality-audit` 继续出现在构建路由列表中，说明本轮 schema 收口未引入新的构建错误。
  - ECS 部署结果：已按当前整个工作区状态通过 `./scripts/ops/deploy-local-build-to-ecs.sh --skip-backup` 直接发版到 ECS；远端 `resumer.service` 已在 `2026-05-09 22:58:43 CST` 成功重启，`Next.js 16.2.4` 启动日志显示 `Ready in 404ms`。
  - 公网验证结果：`GET http://47.95.233.109/learning` 返回 `200`，`GET http://47.95.233.109/api/learning/banks` 返回 `200` 且仍能拿到学习中心题库数据；`POST http://47.95.233.109/api/admin/learning/quality-audit` 在未登录态下返回 `401 Unauthorized`，说明线上新路由已生效且权限校验正常。
  - 下一步：在后台登录态真实回归“运行 AI 抽检”按钮；随后根据首轮 AI 审核结果，补做“高风险文档集中展示 / 批量摘要展示”。
- 2026-05-09 当前：按最新验收结论继续执行学习中心 P0/P1，已把流程图组件、评分失败降级态、自测/面试结果卡与来源可信度升级上线 ECS，并完成浏览器真测。
  - P0 流程图结果：`src/components/learning/LearningDiagram.tsx` 与 `src/app/globals.css` 已进一步收口为“短词流程主线 + 图中节点说明 + 补充说明 + 放大/复制/下载/原图查看”的稳定图解组件；同时新增横向滚动、节点换行、打印态 `break-inside: avoid` 保护。`src/lib/learning/content-contract.ts` 与 `src/lib/learning/v2SeedData.ts` 已补齐 `diagramSpec` 标准协议，AQS 主流程图已回灌固定节点与连线，不再依赖长文本 Mermaid 自由发挥。
  - P0 评分失败结果：`src/app/learning/_components/LearningAssessmentPanels.tsx` 已将前端评估请求改为结构化错误处理，失败时不再只显示“学习评估失败”；当前会保留本地草稿、展示“评分暂时失败，但你的回答已保留”、给出错误编号、支持“重新评分 / 先看参考答案 / 复制我的回答”，并自动回退到基础命中检查结果卡。自测、面试主问题与追问都已接入同一套降级逻辑。
  - P0 结果卡结果：自测结果卡已固定输出“答得好的地方 / 还缺什么 / 表达建议 / 为什么这样答 / 推荐答案”；面试结果卡已固定输出“答得好的地方 / 表达建议 / 为什么这样答 / 改进版回答”，并在训练页新增简化评分标准卡，让用户边答边看必答点与加分点。
  - P1 来源可信度结果：`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/QuestionDetailClient.tsx` 已把“来源与可信度”升级成卡片化展示，支持 `适用版本 / 本文引用事实 / 最近复核` 三个字段；`src/lib/learning/content-contract.ts` 已新增统一来源契约。AQS 正式内容已补齐 `Java 8+ / state、独占共享模式、ConditionObject、队列等待机制、park/unpark / 2026-05-08`。
  - 线上部署结果：本地已执行 `node ./node_modules/next/dist/bin/next build --webpack` 通过；随后重新打包并部署 ECS，远端 `systemctl restart resumer` 后服务恢复正常。由于远端种子执行器不可用，本轮改为直接更新线上 AQS `DocumentVersion.learningContent` 快照，确保标准图协议与来源可信度字段已真实在线生效。
  - 浏览器真测结果：公网 `http://47.95.233.109/learning/cmowzn2j0003ppalug17o5saz/category/cmowzn2j3003rpalu534tjawp/question/cmowzn2j5003tpaluvjj8n2be` 已确认展示新的 AQS 图解与“来源与可信度”字段；面试模式输入真实回答后，即使评估服务返回 `HTTP_502`，页面也会显示降级评分结果卡和“看改进版答案 / 继续追问 / 重新回答主问题”；深读自测提交后同样会显示“评分暂时失败，但你的回答已保留”、错误编号、重试/参考答案/复制回答入口与基础命中检查。说明本轮两个 P0 已从“直接报错中断”收口到“失败可继续训练”。
  - 当前边界结论：当前版本已满足“小规模试生产”条件，但仍不建议“全量批量生产”。原因是流程图主线与评分失败体验虽已收口，但 AI 评分服务本身仍存在 `502` 抖动；现阶段更合理的节奏仍是先小规模试生产 20-30 篇并人工抽检，再继续压低评估服务失败率。
- 2026-05-05 当前：继续按验收意见收口学习中心视觉与题库分布，并准备部署 ECS。
  - 前台首页结果：`src/app/learning/_components/LearningCenterClient.tsx` 已将题库列表从长条切换为多列卡片栅格，卡片现包含封面字母、题库标题、简短摘要、分类标签、分类/题目数量和进入入口，整体更接近 `mianshiya` 的题库卡片感，不再是一条条大列表。
  - 前台详情结果：`src/app/learning/[kbId]/page.tsx` 已继续按 `mianshiya` 的题库详情结构收口，改为“紧凑头部 + 左侧分类导航 + 右侧题目列表”的布局，新增“开始刷题”入口，弱化原来的工作台感。
  - 文案去味结果：`src/lib/learning/agentBankBuilder.ts` 与 `src/lib/learning/bankStudio.ts` 已去掉用户可见的“AI 生成题库 / 自动生成题库 / 自动生成的首版题库”等表达；当前题库统一展示为“高频面试题库”“整理核心考点与结构化答案”等正常产品文案。
  - 题库覆盖结果：已将首批题库扩充为 20 个主题，并通过后台真实接口重新初始化，当前包括 `Java 基础`、`Java 集合`、`Java 并发`、`JVM`、`Spring`、`SpringBoot`、`SpringCloud`、`MySQL`、`Redis`、`Kafka`、`计算机网络`、`操作系统`、`消息队列`、`后端系统设计`、`设计模式`、`JavaScript`、`TypeScript`、`前端工程化`、`Vue 3`、`React`；本地复核 `GET /api/learning/banks` 已返回 20 个题库。
  - 验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、定向 `eslint`、`npm run build`，结果通过；并通过本地管理员真实调用 `POST /api/admin/learning/studio` 以 `quickStart=true` 重建 20 个题库。
- 2026-05-05 当前：继续按 `mianshiya` 的极简结构收口学习中心与后台详情页，并通过后台正式初始化首批题库供验收。
  - 前台页面结果：`src/app/learning/[kbId]/page.tsx` 已继续做减法，去掉说明块、阅读建议和分类说明卡，当前只保留返回入口、统计信息、搜索、分类筛选与题目列表；详情页整体风格已与首页统一到同一套 `minimal-learning-*` 体系。
  - 后台页面结果：`src/app/admin/learning/[kbId]/page.tsx` 已从多层 tab 和大段说明收口为单页极简工作台，当前只保留题库操作、分类、题目列表和最近运行；`src/app/admin/learning/page.tsx` 新增“初始化首批正式题库”入口，便于直接批量补库。
  - 初始化能力结果：`src/lib/learning/agentBankBuilder.ts` 已新增 `DEFAULT_BANK_TOPICS`、批量初始化能力与快速正式初始化能力；`src/app/api/admin/learning/studio/route.ts` 已支持通过后台一次性初始化预设题库主题；已用后台真实接口完成 10 个首批题库初始化，当前 `GET /api/learning/banks` 返回题库包括 `Java 基础`、`Java 集合`、`Java 并发`、`JVM`、`Spring`、`SpringBoot`、`MySQL`、`Redis`、`Vue 3`、`React`。
  - 数据修正结果：已修复题库落库时按分类标题重复建组的问题，当前题库分类与题目统计恢复正常；本地复核 `/api/learning/banks` 已返回 10 个题库，其中 `Java 基础 / Java 集合 / Java 并发 / JVM / Spring / SpringBoot` 各 7 个分类、28 道题，`MySQL / Redis` 各 3 个分类、12 道题，`Vue 3 / React` 各 4 个分类、16 道题。
  - 验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、定向 `eslint` 与 `npm run build`，结果通过；并通过本地管理员登录后真实调用 `POST /api/admin/learning/studio` 完成批量初始化验收。
- 2026-05-05 当前：极简重做 `learning` 首页与 `admin/learning` 首页，只保留核心入口并完成编译验收。
  - 前台首页结果：`src/app/learning/_components/LearningCenterClient.tsx` 已删去首页说明块、推荐区、热度区与首页助手挂载，只保留搜索、分类筛选与题库列表三部分；交互仍直接连接 `/api/learning/banks`，进入题库的主链路未变。
  - 后台首页结果：`src/app/admin/learning/page.tsx` 已删去工坊 Hero、多 Agent 卡片、说明区、焦点区等装饰层，只保留生成入口、最近运行、题库列表三块；继续复用 `/api/admin/learning/studio` 的读取、创建与删除能力。
  - 样式结果：`src/app/globals.css` 已新增 `minimal-learning-*` 与 `minimal-admin-learning-*` 极简样式域，独立承接本轮首页重排，避免影响既有详情页与其他模块样式。
  - 验证结果：已执行 `node ./node_modules/eslint/bin/eslint.js src/app/learning/_components/LearningCenterClient.tsx src/app/admin/learning/page.tsx --max-warnings=0`、`node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过。
- 2026-05-05 当前：按全新 spec 重做学习中心与学习管理，前台改为题库化结构，后台改为多 Agent 题库工坊，并清空旧学习数据。
  - 前台重做结果：`src/app/learning/_components/LearningCenterClient.tsx` 已改为题库列表首页，接入新的 `/api/learning/banks`；`src/app/learning/[kbId]/page.tsx` 已改为题库详情工作台，支持分类导航、题目列表和关键词筛选；题目详情页仍保留原语雀式阅读工作区，仅将用户可见“知识库”标签收口为“题库”。
  - 后台重做结果：`src/app/admin/learning/page.tsx` 与 `src/app/admin/learning/[kbId]/page.tsx` 已切到新的题库工坊视图；新增 `/api/admin/learning/studio` 与 `/api/admin/learning/studio/[kbId]` 作为多 Agent 工坊总览、生成、删除与详情接口。
  - 数据主链路结果：新增 `src/lib/db/learningStudio.ts` 和 `src/lib/learning/bankStudio.ts`；学习中心和学习管理首版统一以 `data/learning-center` 文件仓为唯一正式数据源，不再让 Prisma 与文件仓并行承担正式链路。
  - 多 Agent 结果：新增 `src/lib/learning/agentBankBuilder.ts`，将后台生成流程收敛为“架构师 -> 出题员 -> 答案师 -> 审核员 -> 关联员 -> 发布器”六阶段，并明确由代码负责状态、持久化和发布，AI 负责结构规划与内容生成。
  - 数据清空结果：`data/learning-center/index.json`、`admin-state.json`、`studio-state.json` 已重置为空底座，旧学习中心历史数据已不再作为新版前台正式来源。
  - 智能助手保护结果：本轮没有修改首页/文档“面面吧智能助手”核心链路，也没有改题目深挖助手的核心 API 边界，确保学习中心重做期间智能助手保持独立。
  - 验证结果：已执行定向 `eslint`、`tsc --noEmit`、`npm run build`；并本地启动 `next start` 验证空仓状态下 `/api/learning/banks` 返回 `200` 且结果为空列表，前台可正确接住空状态。
- 2026-05-05 当前：重构 `admin/learning` 首页与 `[kbId]` 题库工坊详情页，统一切到新的 `studio` 接口并补齐新版样式。
  - 首页接线结果：`src/app/admin/learning/page.tsx` 已从旧 `GET/DELETE /api/admin/learning/kb` 与 `POST /api/admin/learning/auto-build` 切换为 `GET/POST /api/admin/learning/studio` 与 `DELETE /api/admin/learning/studio/[kbId]`；当前首页围绕 `totals / banks / runs` 展示题库总览、最近运行、重点题库和单入口启动表单，不再依赖旧知识工厂快照结构。
  - 详情页重做结果：`src/app/admin/learning/[kbId]/page.tsx` 已改为新版题库工坊详情页，只使用 `GET /api/admin/learning/studio/[kbId]` 返回的 `bank / categories / questions / runs` 数据；当前详情页集中展示题库概览、分类地图、题目筛选列表与运行时间线，并保留删除题库与跳转前台学习页能力。
  - 样式补齐结果：`src/app/globals.css` 已新增 `factory-workshop-toggle`、`studio-status-chip`、`studio-run-*` 和 `studio-detail-*` 样式域，用于承接首页运行状态、详情页卡片、阶段时间线与移动端收口，保持后台现有样式体系内聚。
  - 本地验证结果：已执行 `node ./node_modules/eslint/bin/eslint.js src/app/admin/learning/page.tsx 'src/app/admin/learning/[kbId]/page.tsx' --max-warnings=0`、`node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过。
- 2026-05-05 当前：重做 `learning` 与 `admin/learning` 首页，前台切到题库化入口，后台切到多 Agent 工坊，同时保持题目页语雀式体验与“面面吧智能助手”核心边界不变。
  - 前台题库化结果：`src/app/learning/_components/LearningCenterClient.tsx` 已重构为“题库 Hero + 方向筛选侧栏 + 题库地图 + 高频方向信号”结构，首页交互改为先选方向、再进题库、再进题目流；继续挂载原 `DocumentAssistantPanel` 通用模式，不改动 `/api/learning/assistant` 核心链路与智能助手实现。
  - 后台工坊化结果：`src/app/admin/learning/page.tsx` 已重构为“多 Agent 工坊驾驶舱”，把课题策略、来源侦察、目录架构、题目生成、审核发布拆成显式工位卡片；首页仍复用现有 `POST /api/admin/learning/auto-build` 与 `GET /api/admin/learning/kb` 数据能力，只重做展示层，不改自动建库主流程。
  - 样式收口结果：`src/app/globals.css` 已新增独立的 `learning-bank-*` 与 `factory-workshop-*` 样式域，专门服务这轮前后台重做；题目详情页继续沿用既有 `learning-workspace` / `yuque-layout` 体系，未破坏语雀式阅读工作区与题目深挖助手外壳。
  - 编译修复与验收结果：为满足“完成可编译代码”，已顺手修复 `src/lib/learning/agentBankBuilder.ts` 中答案阶段 fallback 返回结构与泛型声明不一致的既有类型问题；随后已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`node ./node_modules/eslint/bin/eslint.js src/app/learning/_components/LearningCenterClient.tsx src/app/admin/learning/page.tsx src/lib/learning/agentBankBuilder.ts --max-warnings=0` 与 `npm run build`，结果全部通过。
- 2026-05-05 当前：修复题目深挖接口在 Prisma 题库表缺失时直接 500，恢复“面面吧智能助手”可用性。
  - 根因定位结果：已真实复现线上 `POST /api/learning/questions/[questionId]/assistant`，报错为 `The table public.Question does not exist in the current database.`。问题不在前端文案或壳子，而在 `src/app/api/learning/questions/[questionId]/assistant/route.ts` 的 `loadQuestionAssistantContext()` 先强查 Prisma 的 `Question` 表，但线上当前题库链路实际依赖文件仓兜底；Prisma 查询抛错后，原逻辑没有真正兜回 `getFallbackQuestionDetail()`，因此题目深挖直接 500。
  - 修复结果：`src/app/api/learning/questions/[questionId]/assistant/route.ts` 已将 Prisma 读取改为 `try/catch` 包裹；当 `Question` 表不存在、查询失败或运行态暂不可用时，当前会自动回退到文件仓题目上下文，继续围绕当前题目、参考答案、关联题和分类上下文生成回答，不再因为数据库表缺失直接报错。
  - 真实验收结果：已重新构建并部署 ECS；随后用真实文件仓题目 `kbId=javamobkc2vt`、`categoryId=java-basics`、`questionId=syntax` 复测，`GET /api/learning/questions/[questionId]` 已恢复 `200`，`POST /api/learning/questions/[questionId]/assistant` 也已恢复 `200` 并返回正常题目深挖回答。说明当前“面面吧智能助手”在题目深挖模式下已重新可用。
- 2026-05-05 当前：修正题目阅读页助手的对外品牌文案，统一回到“面面吧智能助手”。
  - 品牌文案结果：`src/app/learning/_components/QuestionAssistantPanel.tsx`、`src/app/api/learning/questions/[questionId]/assistant/route.ts`、`src/app/learning/[kbId]/page.tsx`、`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已将“题目助手”全部改回“面面吧智能助手”；当前题目页仍保持“只围绕当前题目深挖”的行为边界，但对外统一使用你要求的产品名称，不再出现割裂命名。
  - 本地验证结果：已执行定向 `eslint` 与 `npm run build`，结果通过，准备重新部署 ECS。
- 2026-05-05 当前：按最新验收意见撤回题目页自定义小面板，恢复为原“面面吧智能助手”壳子与交互。
  - 交互恢复结果：`src/app/learning/_components/QuestionAssistantPanel.tsx` 已不再使用单独的 `question-assistant-*` 小面板结构，而是恢复为与原 `DocumentAssistantPanel` 一致的入口按钮、品牌头部、左侧历史会话、新对话、聊天工作区和底部输入区，避免题目页助手和你已经满意的“面面吧智能助手”出现两套完全不同的观感。
  - 边界保持结果：题目页仍继续调用 `src/app/api/learning/questions/[questionId]/assistant/route.ts`，因此回答边界依旧只围绕当前题目、参考答案、关联题和分类上下文，不会影响学习中心首页与文档页原有“面面吧智能助手”核心能力。
  - 历史会话结果：当前题目页也已补齐与原助手一致的本地会话能力，包括新对话、历史切换、重命名、置顶、删除，以及首问自动标题，确保不只是名字一样，而是交互壳子也回到原来的体系。
  - 本地验证结果：已执行定向 `eslint`，通过后继续进行 `npm run build` 与重新部署验证。
- 2026-05-05 当前：基于 `rebuild-learning-center-question-bank` spec 高质量重构 `learning` 与 `admin/learning` 前端，恢复学习计划、语雀式阅读与后台成果可见性，并保持“面面吧智能助手”核心能力不变。
  - 学习中心结果：`src/app/learning/_components/LearningCenterClient.tsx` 已重构为“品牌化入口 Hero + 学习计划工作台 + 知识库目录”三段式布局，首页重新接入 `/api/learning/plan` 与 `/api/learning/progress`，支持生成并缓存学习计划、展示知识库统计、掌握率与标签筛选，同时继续独立挂载首页 `DocumentAssistantPanel`，不改动原 `/api/learning/assistant` 核心链路。
  - 前台阅读结果：`src/app/learning/[kbId]/page.tsx` 与 `src/app/learning/[kbId]/category/[categoryId]/page.tsx` 已升级为统一的编排式分类浏览；`src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已切到语雀式阅读工作区，补齐左侧题目导航、右侧目录、结构化答案、代码示例、Mermaid 图示、关联题、收藏初始化回显，以及基于当前分类顺序的“上一题 / 下一题”跳转。
  - 后台工作台结果：`src/app/admin/learning/[kbId]/page.tsx` 已重构为知识库工作台视图，当前可直接看到自动生成文章目录、题目筛选列表、草稿审核队列、来源池、发布成果和 AI 生成入口；管理员可在同页切换草稿 / 发布 / 归档状态，并继续进入草稿审核页，不再需要靠接口层或零散页面确认成果是否真的生成出来。
  - 样式与边界结果：`src/app/globals.css` 已补齐缺失的 `.btn-outline` 与 `.input` 基础样式，收口学习中心和后台工作台的粗糙交互视觉；本轮未改动 `src/app/api/learning/assistant/route.ts`、首页“面面吧智能助手”核心交互与题目助手的独立边界，只调整其挂载位置与外层页面布局。
  - 文档与验证结果：`.trae/specs/rebuild-learning-center-question-bank/tasks.md`、`checklist.md` 已回填 Task 19 / 21 / 22 的完成进度与新增 Task 27；本轮已执行 `node ./node_modules/eslint/bin/eslint.js .`、`node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`npm run build`。其中 `tsc` 与 `build` 通过；ESLint 全仓输出 6 条既有 warning（位于 `src/app/api/learning/progress/route.ts`、`src/app/learning/[kbId]/category/[categoryId]/practice/page.tsx`、`src/app/learning/_components/DocumentAssistantPanel.tsx`、`src/app/practice/page.tsx`、`src/components/Mermaid.tsx`），未阻断构建。本地 `npm run lint` 因 `node_modules/.bin/eslint` 入口损坏报 `Cannot find module '../package.json'`，因此改用官方可执行入口完成真实校验。
- 2026-05-05 当前：按 `rebuild-learning-center-question-bank` spec 修复学习中心分类页报错与题目列表读取链路，补齐文件仓兜底并完成构建验收。
  - 分类页链路结果：新增 `src/lib/learning/questionBankFallback.ts` 作为题库文件仓适配层，将 `data/learning-center/` 中的 `kb / group / topic` 统一映射为分类题目列表与题目详情结构；`src/app/api/learning/categories/[categoryId]/questions/route.ts` 已改为 `Prisma` 优先、空结果/异常时回退文件仓，并稳定返回 `questions + pagination`，避免分类页因题库表未就绪或分类 ID 来自文件仓而直接报错。
  - 题目读取结果：`src/app/api/learning/questions/[questionId]/route.ts`、`src/app/api/learning/questions/[questionId]/assistant/route.ts`、`src/app/api/learning/practice/evaluate/route.ts` 已统一接入文件仓题目兜底；当前从分类页进入题目详情、打开独立“题目助手”、进入刷题模式做 AI 评估时，都可在数据库题目缺失时继续读取文件仓内容，不再卡死在 `Question` 表。
  - 定位与兼容结果：`src/app/learning/[kbId]/category/[categoryId]/page.tsx`、`practice/page.tsx`、`question/[questionId]/page.tsx` 以及 `src/app/learning/_components/QuestionAssistantPanel.tsx` 已补充 `kbId` / `categoryId` 定位参数；当前即使多个知识库存在同名分类或同名 topic id，列表、详情和题目助手也不会串库。`src/app/api/learning/progress/route.ts` 同步为文件仓题目增加无副作用成功返回，避免外键不存在时产生 500 噪音。
  - 助手边界结果：本轮未改动 `src/app/api/learning/assistant/route.ts` 与首页“面面吧智能助手”交互，只为题目详情页独立题目助手补充文件仓兼容，继续保持“题库助手独立、原智能助手不受影响”的边界。
  - 文档与验证结果：`.trae/specs/rebuild-learning-center-question-bank/tasks.md`、`checklist.md` 已回填本轮 Task 26 与验收项；已执行 `node node_modules/typescript/bin/tsc --noEmit` 与 `npm run build`，结果通过。注意：环境内 `npx tsc` 的 `.bin/tsc` 入口损坏，首次执行报 `Cannot find module '../lib/tsc.js'`，因此改用项目内真实 TypeScript 入口完成类型校验。
- 2026-05-04 当前：已完成 `rebuild-learning-center-question-bank` spec checklist 与当前实现的逐项核对，并同步更新任务文档。
  - checklist 核对结果：`.trae/specs/rebuild-learning-center-question-bank/checklist.md` 已按当前代码实现重新勾选。当前已确认通过的能力包括：`prisma/schema.prisma` 中的 `KnowledgeBase / Category / Question / UserQuestionProgress` 模型；`GET /api/learning/kbs`、`GET /api/learning/kbs/[kbId]/categories`、`GET /api/learning/categories/[categoryId]/questions`、`GET /api/learning/questions/[questionId]`、`GET/POST /api/learning/progress`；前台知识库卡片、分类页题目列表、题目详情页“查看答案”、刷题设置与 AI 评估；题目详情页独立 `QuestionAssistantPanel`；以及后台统计面板、建库、删库、建分类、文本/主题生成题目并保存草稿等基础能力。
  - 未通过项结论：当前仍未拿到题库四张表真正落库的 migration 证据，`prisma/migrations/` 中也还没有 `KnowledgeBase / Category / Question / UserQuestionProgress` 的 SQL 迁移；学习中心首页目前只有关键字搜索，没有真正“按分类筛选知识库”；题目详情页还缺“上一题/下一题”与稳定收藏回显；刷题模式没有“收藏”闭环；进度面板 UI 仍未落地；后台虽然已有部分 Prisma API，但题目编辑页文件尚不存在，筛选、审核队列、URL 抓取生成、来源管理、学习计划/报告联动、旧链路清理与最终验收也都还未完成。
  - 任务回填结果：`.trae/specs/rebuild-learning-center-question-bank/tasks.md` 已新增 `Task 17` 到 `Task 25`，将迁移落库、首页分类筛选、详情页导航与收藏、刷题闭环、进度面板、后台筛选与编辑、来源管理、计划/报告联动、最终验收等未通过项拆成后续可执行任务，便于下一轮按顺序继续推进。
- 2026-05-05 当前：按题库版学习中心 spec 修复知识库列表报错，并确保“面面吧智能助手”独立可见。
  - 列表修复结果：`src/app/api/learning/kbs/route.ts` 已改为 `Prisma` 题库表优先、学习中心文件内容仓兜底；当前当 `KnowledgeBase / Category / Question` 表尚未完成迁移或运行态暂不可读时，`/learning` 入口页不会再因为 `/api/learning/kbs` 直接 `500` 而整页报错，仍会基于 `data/learning-center/` 正常返回知识库卡片列表。`src/app/api/learning/kbs/[kbId]/categories/route.ts` 同步增加文件目录树兜底，`src/app/learning/[kbId]/page.tsx` 与 `src/app/learning/[kbId]/category/[categoryId]/page.tsx` 已补充接口数据结构容错，避免异常 JSON 直接触发前端崩溃。
  - 助手可见性结果：`src/app/learning/_components/LearningCenterClient.tsx` 已在 `/learning` 首页重新挂载通用模式的 `DocumentAssistantPanel`；当前用户不进入题目详情，也能在学习中心首页独立看到并打开“面面吧智能助手”。
  - 助手隔离结果：新增 `src/app/learning/_components/QuestionAssistantPanel.tsx` 与 `src/app/api/learning/questions/[questionId]/assistant/route.ts`，题目详情页 `src/app/learning/[kbId]/category/[categoryId]/question/[questionId]/page.tsx` 已切换为独立“题目助手”；当前它只围绕当前题目、参考答案、关联题和同分类题目回答，不再错误复用“面面吧智能助手”组件，符合题库 spec 中“原智能助手不改、题库助手独立”的边界要求。
  - 文档回填结果：`.trae/specs/rebuild-learning-center-question-bank/tasks.md` 与 `checklist.md` 已补记本轮“入口报错修复、助手独立可见、题目助手拆分”的完成状态。
  - 本地验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；构建产物已包含 `ƒ /api/learning/kbs`、`ƒ /api/learning/kbs/[kbId]/categories`、`ƒ /api/learning/questions/[questionId]/assistant`、`○ /learning` 与 `ƒ /learning/[kbId]/category/[categoryId]/question/[questionId]`。
- 2026-05-05 当前：按最新 spec 修复学习后台创建知识库样式，并收口旧智能生成链路的 JSON 解析报错。
  - 后台样式结果：`src/app/admin/learning/page.tsx` 已从旧“题库工厂 + 弹窗建库”页面切回当前知识工厂单入口驾驶舱结构，直接复用 `src/app/globals.css` 里已经沉淀的 `admin-learning-*` 样式；当前后台首页会展示总览 Hero、单入口自动建库表单、指标侧栏、自动链路说明与重点知识库卡片，不再与 spec 脱节。
  - 创建链路结果：后台首页创建动作已改为直接请求 `POST /api/admin/learning/auto-build`，与当前文件优先知识工厂链路保持一致；这次不再继续走旧的 Prisma 题库建库接口，也避免首页展示和实际生成逻辑分裂。
  - JSON 报错修复结果：`src/app/api/admin/learning/generate-kb/route.ts` 已为旧智能生成接口补充 Markdown 围栏剥离、首个完整 JSON 对象提取与解析失败提示；当前即使模型返回 ```json 围栏或前后附带说明文字，也不会再因为原先的贪婪正则截取而直接 `JSON.parse` 报错。
  - 文档结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md`、`checklist.md` 已补记本轮“后台样式 + JSON 解析”收口任务，便于后续继续回溯知识工厂后台链路。
- 2026-05-04 当前：补充发起面试页一个上传体验优化，避免旧简历摘要残留。
  - 上传体验结果：`src/app/setup/page.tsx` 已在 `handleFileUpload()` 中补充 `setResumeText("")`；当前用户重新上传新简历文件时，会先清空下方简历内容框，避免旧文件解析后的摘要文本继续残留，造成“新文件 + 旧摘要”混在一起的误导。
  - 本地验证结果：已执行 `node ./node_modules/eslint/bin/eslint.js src/app/setup/page.tsx --max-warnings=0 --rule '@next/next/no-img-element: off'` 与 `npm run build`，结果通过。
- 2026-05-04 当前：继续修复“发起面试”中的简历解析间歇性失败，并根据线上 23:20 - 23:22 的真实日志回溯主流程。
  - 根因定位结果：已检查线上 nginx `access.log`，`23:20:25` 的 `/api/parse` 请求真实状态码为 `499`，表示客户端在服务端返回前主动断开连接；同一时间窗内 `23:20:48` 与 `23:21:06` 的后续 `/api/parse` 请求都返回 `200`。这说明问题不是 PDF 已无法解析，也不是服务端把这条链路整体改坏，而是解析过程中存在客户端侧中断后被前端直接视为失败的问题。
  - 解析稳定性结果：`src/app/setup/page.tsx` 已为“开始解析并生成画像”补充并发保护、旧请求忽略与临时网络失败自动重试。当前如果用户连续点击、旧请求晚返回，或发生一次瞬时网络中断，不会再让过期请求覆盖最新状态；按钮在解析中也会进入禁用态并显示“正在解析，请稍候...”。
  - 复核结果：已再次核对 `/Users/yangyu/Desktop/resumer/杨宇-四年工作经验-高级Java研发工程师.pdf`，当前 PDF 文本提取仍正常；本地 `eslint` 与 `npm run build` 已通过，准备重新部署 ECS 验证主流程。
- 2026-05-04 当前：继续收口文字面试消息样式，并复核“发起面试”主流程中的简历解析稳定性。
  - 文字面试结果：`src/app/globals.css` 已将文字面试 / 专项训练中的用户侧消息正文也切到与“面面吧智能助手”一致的排版节奏；当前用户消息与 AI 消息都会使用更舒展的段落间距和更稳定的阅读字体，不再出现“用户提问文字样式还没改过来”的割裂感。
  - 简历解析复核结果：已用真实文件 `/Users/yangyu/Desktop/resumer/杨宇-四年工作经验-高级Java研发工程师.pdf` 复核当前链路。`src/app/api/parse/route.ts` 中的 PDF 提取仍可正常读出正文，本地按同一 `pdf-parse` 流程提取到 `2632` 个字符；同时浏览器真测线上 `/setup` 上传这份 PDF 后，`POST /api/parse` 成功，页面已正常跳转到 `/profile?mode=text`。当前未发现“这份简历已无法解析”的稳定性回归。
  - 结论说明：本次并不是我把 PDF 解析链路改坏了；你截图里的“解析失败”更像是当时那次请求的临时失败、旧前端状态或未完成刷新，而不是当前线上稳定复现的问题。
  - 本地验证结果：已执行 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx src/app/setup/page.tsx src/app/api/parse/route.ts --max-warnings=0 --rule '@next/next/no-img-element: off'` 与 `npm run build`，结果通过。
- 2026-05-04 当前：继续收口学习中心历史会话与面试页文字样式，但按当前选择不处理本次需求之外的仓库阻断文件。
  - 历史会话结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已将历史列表进一步拆分为“置顶会话 / 普通会话”两个分组，并保留原有重命名、置顶、删除能力；同时菜单仍挂在每条历史右侧 `...`，交互语义不变。
  - 菜单展示结果：`src/app/globals.css` 已将历史项从 `overflow: hidden` 调整为 `overflow: visible`，并为分组标签、菜单层级补齐样式，修复“点击右侧 `...` 什么都没有展示”的问题。
  - 面试页样式结果：`src/app/interview/page.tsx` 与 `src/app/globals.css` 已将专项训练 / 发起面试中的 AI 回答区切换为更接近学习中心“面面吧智能助手”的轻正文展示；当前 AI 回答会沿用更舒展的段落节奏、代码块、内联代码和引用样式，用户回答仍保留原有气泡，不改动面试流程逻辑。
  - 当前验证状态：已执行定向 `eslint` 校验通过；但全量 `tsc` / `build` 仍被仓库中与本次需求无关的文件 `src/app/learning/[kbId]/category/[categoryId]/page.tsx` 语法错误阻断。按当前选择，本轮不处理该无关阻断，因此暂不继续部署 ECS。
- 2026-05-04 当前：仅收口学习中心智能助手“历史标题相关”能力，不改动其他已完成功能。
  - 标题策略结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已把会话标题改为“首次生成后锁定”的策略；当前新会话仍以“新对话”进入列表，但只有在首轮服务端返回标题且本地 `titleState` 仍为 `pending` 时才会写入，后续追问不会再持续覆盖标题；如果用户手动重命名，标题状态会切到 `custom`，后续服务端返回也不会再改名。
  - 会话管理结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已为历史会话补齐 DeepSeek 风格的更多操作菜单，当前支持“重命名 / 置顶 / 删除”；同时在本地持久化结构中新增 `titleState` 与 `isPinned` 字段，并将历史排序调整为“置顶优先，其余按更新时间倒序”。
  - 样式结果：`src/app/globals.css` 已仅围绕历史标题区域补充菜单按钮、置顶标签和重命名编辑态样式；正常列表仍保持此前已验收通过的单行历史会话视觉，不改动聊天区、Mermaid、输入框和回答样式。
  - 服务端结果：`src/app/api/learning/assistant/route.ts` 已继续收口 `sessionTitle` 逻辑；文档模式规划阶段现要求标题只根据第一次用户提问概括，通用模式则新增一次独立的标题生成调用，用模型根据首问生成更稳定的短标题，再回传给前端作为首次命名。
  - 本地验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false`、`node ./node_modules/eslint/bin/eslint.js src/app/learning/_components/DocumentAssistantPanel.tsx src/app/api/learning/assistant/route.ts --max-warnings=0 --rule '@next/next/no-img-element: off'` 与 `npm run build`，结果通过。
- 2026-05-04 当前：继续收口学习中心智能助手的输入体验、回答样式和知识兜底能力，并准备重新部署 ECS。
  - 输入体验结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已为输入框增加组合输入态保护，`onCompositionStart / onCompositionEnd` 与 `nativeEvent.isComposing / keyCode === 229` 会共同拦截输入法选词时的回车，不再把“选中候选词”误判成“发送消息”；`Shift + Enter` 换行保持不变。
  - 回答样式结果：`src/app/globals.css` 已将学习助手的助手侧回答从厚重气泡收口为更接近 DeepSeek 的轻量展示，同时重做 `pre`、内联 `code` 和 `blockquote` 的视觉层级；当前代码块改为浅色卡片，内联代码改为低对比标签风格，不再继承全局橙色强调样式。
  - 知识兜底结果：`src/app/api/learning/assistant/route.ts` 已把火山方舟 `searchKnowledgeBase()` 真正接入学习助手；文档模式会在知识库证据之外补充 `external_knowledge` 外部知识片段，通用模式也会在生成前主动检索外部知识；同时补充“歧义术语优先结合用户语境与检索结果消歧”的提示词约束，避免将 `AI SKILL / 学习 SKILL 语法` 误答成 EDA 方向。
  - 本地验证结果：已重新执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过。
  - 线上验收结果：已执行 `./scripts/ops/deploy-local-build-to-ecs.sh` 重新发布 ECS；公网 `GET http://47.95.233.109/learning` 返回 `200`；公网 `POST /api/learning/assistant` 在通用模式下提问“我想学习 AI SKILL，请先解释它是什么，再给我一个入门路径，不要回答成 EDA 或 Cadence SKILL。”时，当前已返回 AI 技能学习路径，不再误答成 EDA；当只输入含糊的 `skill` 时，当前会先反问澄清具体方向，而不是直接答偏。
- 2026-05-04 当前：完成学习中心智能助手的两项体验升级，并重新部署 ECS。
  - 流程图体验结果：`src/components/Mermaid.tsx` 已改为浅色主题渲染，避免黑底导致节点内容看不清；同时增加右上角“放大查看”入口，点击后以弹层形式展示大图，便于查看复杂流程图。`src/app/globals.css` 已同步补充图表卡片、放大按钮和弹层样式。
  - 学习中心首页助手结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已改为支持 `document` 与 `general` 双模式复用；`src/app/api/learning/assistant/route.ts` 已新增学习中心首页通用助手链路，不再强依赖 `kbId + topicId` 绑定具体文档；`src/app/learning/_components/LearningCenterClient.tsx` 已在学习中心首页挂载“自由提问”助手入口，用户在未进入具体文档前也可以直接提问、闲聊和生成 Mermaid 图示。
  - 线上验收结果：已重新执行本地构建并部署 ECS；浏览器真测确认学习中心首页已出现不绑定文档的“面面吧智能助手”，文档页流程图区域已出现“放大查看流程图”按钮，点击后可打开大图查看弹层。
- 2026-05-04 当前：继续修复“流程图源码直接显示到聊天气泡中”的问题，并重新部署 ECS。
  - 根因3：部分回答虽然已经返回 Mermaid，但模型偶尔会输出不规范围栏或裸 `mermaid flowchart` 片段，导致前端把它当普通 Markdown 文本/代码显示，而不是图表。
  - 根因4：即使 Mermaid 解析失败，旧逻辑也只会回退为源码展示，用户看到的是代码而不是图示。
  - 修复结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已新增 Mermaid 文本标准化逻辑，会自动把裸 Mermaid 片段与不规范围栏修正为标准 ` ```mermaid ` 代码块；`src/components/Mermaid.tsx` 已将失败兜底从“显示源码”改为“显示结构化流程图兼容视图”，避免再把代码直接暴露给用户。
- 2026-05-04 当前：修复学习助手 Mermaid 流程图报错与流式阶段页面连续报错问题，并重新部署 ECS。
  - 根因1：流程图回答中包含 `>、>=、≥、&` 等 Mermaid 11 容易误判的字符，导致 `src/components/Mermaid.tsx` 在渲染时抛出 `Parse error`。
  - 根因2：学习助手采用流式回答，Mermaid 代码块在尚未输出完整时就被前端反复尝试渲染，导致控制台连续刷出大量 `Mermaid parsing failed` 日志，页面体验异常。
  - 修复结果：`src/components/Mermaid.tsx` 已增加 Mermaid 围栏剥离、节点标签字符清洗、失败降级源码展示，并启用 `suppressErrorRendering` 防止 Mermaid 将错误节点喷到整页；`src/app/learning/_components/DocumentAssistantPanel.tsx` 已改为在助手消息仍处于流式生成时先按普通代码块展示，待消息结束后再进行 Mermaid 渲染；`src/app/api/learning/assistant/route.ts` 已继续约束模型输出更保守的 Mermaid 语法。
  - 验证结果：已重新执行本地 `tsc --noEmit` 与 `npm run build`，并重新部署 ECS；浏览器真测同一文档页再次提问生成流程图时，流式阶段控制台已无 Mermaid 报错，回答结束后正常展示流程图节点内容。
- 2026-05-04 当前：完成学习助手 Mermaid 流程图能力的 ECS 部署收口，已恢复线上服务并通过真实接口验收。
  - 部署恢复结果：定位到此前 ECS `resumer` 重启失败的直接原因是运行态 `.next` 缺失 `BUILD_ID`，导致 `next start` 持续报错 “Could not find a production build in the '.next' directory.”；本地已删除旧 `.next` 后重新执行 `npm run build`，确认生成新的 `BUILD_ID`，随后通过 `./scripts/ops/deploy-local-build-to-ecs.sh` 重新下发到 ECS，当前 `systemctl is-active resumer` 已恢复为 `active`。
  - 流程图结果：`src/app/api/learning/assistant/route.ts` 已继续收口规划阶段提示词；当用户明确要求 Mermaid 流程图时，只要知识库证据能支撑出步骤或分支，就不允许因为“不能画图”而拒答，而是继续进入 Mermaid 输出链路。
  - 线上验收结果：已在 ECS 上真实调用 `POST /api/learning/assistant`，问题为“请给我画一个 HashMap put 方法的流程图，用 mermaid 表示。”；当前返回 `refusal=false`，正文中已包含完整 ```mermaid 代码块，不再返回“无法直接生成 mermaid 流程图”的拒答文案。同时复验 `GET /home` 与 `GET /learning`，均返回 `200`。
- 2026-05-04 当前：按最新要求为学习中心智能助手补上“流程图”能力，但严格限制在 Mermaid 图表范围内，不接任意图片生成，并准备部署 ECS 验收。
  - 渲染结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已复用项目现有 `src/components/Mermaid.tsx`，把聊天消息中的 ```mermaid 代码块直接渲染成流程图；`src/app/globals.css` 同步补充了助手消息区的 Mermaid 容器与 SVG 自适应样式，保证流程图在对话气泡内可读。
  - 提示词结果：`src/app/api/learning/assistant/route.ts` 已放开“流程图类问题”的 Mermaid 输出能力；当用户明确要求流程图、时序图、状态图或结构图时，模型会优先输出 Mermaid 代码块并附带简短说明，同时明确禁止承诺 PNG、JPG 这类任意图片生成。
  - 本地验证结果：已重新执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过。
- 2026-05-04 当前：根据最新验收截图继续收口学习中心助手与学习后台，重点修复“把整张品牌规范图硬塞进入口按钮”“单条历史会话占满整列”“学习后台仍像旧表单页”三个问题，并准备重新部署 ECS。
  - 助手 Logo 结果：新增 `public/branding/mianmianba-assistant-mark.svg`，从 `mianmianba-logo-direction-c-tech-light.svg` 中提取可独立使用的 App Icon，`src/app/learning/_components/DocumentAssistantPanel.tsx` 已切换为只渲染这个精简图标；`src/app/globals.css` 同步把右下角入口、面板头部和空态图标全部收成“小图标 + 品牌文案”的结构，不再把整张规范板缩略塞进按钮。
  - 历史会话结果：`src/app/globals.css` 已将 `learning-assistant-history__list` 改为 `align-content: start` 并锁定会话项固定高度，单条历史记录只按一行列表展示，不会再把整个侧栏空间撑成一条超高卡片。
  - 学习后台结果：`src/app/admin/learning/page.tsx` 与 `src/app/globals.css` 已重做为“顶部总览 Hero + 自动建库入口 + 指标侧栏 + 自动链路 + 重点知识库 + 工作台卡片”的驾驶舱结构，视觉与信息组织更贴近 `mianshiya.com` 这类中后台首页，不再是单纯表单堆叠页。
  - 本地验证结果：已重新执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过。
- 2026-05-04 当前：按最新验收意见继续收口首页首屏与学习中心助手入口，重点解决“问候语和主标题距离仍不够自然”以及“智能助手看起来还是旧 Logo”的感知问题，并重新部署 ECS。
  - 首页结果：`src/app/globals.css` 已继续上调 `home-hero` 内边距、`home-hero__content` 间距与 `home-hero__headline` 的垂直间距，当前“你好，追梦人”和“这是把学习、训练和模拟面试放在一起的准备平台”之间的呼吸感更明显，但整体仍控制在收敛的首屏尺寸内，没有把 hero 放得发散。
  - 助手入口结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已不再使用手绘近似图形，而是直接切到 `public/branding/mianmianba-logo-direction-c-tech-light.svg` 正式品牌素材；入口按钮同步改成更明确的“面面吧智能助手 / 文档追问与连续对话”双层文案，`src/app/globals.css` 也已放大入口宽高、品牌图占比和浅色层次，确保和上一版视觉上明显区分。
  - 本地验证结果：已重新执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；源码检索中未再发现用户可见的旧 `DeepSeek` 品牌文案。
- 2026-05-04 当前：按最新反馈继续微调首页首屏和学习中心智能助手入口，重点放大问候语与主标题之间的呼吸感，并把助手入口做成更明确的“图标 + 品牌文字”组合，随后重新部署 ECS。
  - 首页结果：`src/app/globals.css` 已进一步拉开 `home-hero__intro` 与主标题之间的间距，并适度增大首页 hero 容器上下左右留白，保证“你好，追梦人”和“这是把学习、训练和模拟面试放在一起的准备平台”之间更自然、不拥挤，同时整体尺寸仍保持收敛。
  - 助手入口结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已将右下角入口从“纯图标球”进一步强化成“浅色科技感 Logo + 面面吧 / 智能助手”组合按钮；`src/app/globals.css` 同步放大按钮尺寸、强化浅色边框与阴影层次，让用户在有限空间下仍能一眼识别这是产品内的智能助手入口。
  - 验证结果：已重新执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，并在本地 `.next` 产物中确认存在“面面吧智能助手”字符串，避免再次把旧助手 UI 发布到 ECS。
- 2026-05-04 当前：按最新验收反馈继续收口首页与学习中心智能助手，重点修复首页标题仍然拥挤换行、助手品牌露出不足、历史会话列表过高以及页面残留 `DeepSeek` 品牌文案的问题，并重新部署 ECS。
  - 首页结果：`src/app/home/page.tsx` 与 `src/app/globals.css` 已把首页主标题改成显式双 `span` 结构，在桌面端强制走真正的单行优先排版；同时进一步下调字号、收紧字距并保留中小屏自动回落，确保“这是把学习、训练和模拟面试放在一起的准备平台”不再被挤成多行团块。首屏说明句也同步改成更平铺的单行优先策略。
  - 助手品牌结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已把文档助手品牌从 `DeepSeek` 全部替换为“面面吧智能助手”；入口按钮新增文字标识“面面吧智能助手”，面板头部标题、输入框占位符、消息角色名等用户可见文案也已统一为自有品牌表述。
  - 助手视觉结果：入口 Logo 已参考 `public/branding/mianmianba-logo-direction-c-tech-light.svg` 重画为浅色科技感图形，采用浅底圆角方形 + `M` 轮廓 + 蓝橙绿点缀的轻量版本，更适合当前浅色学习中心界面。
  - 历史会话结果：助手左侧历史区已从“标题 + 摘要 + 时间”的高卡片样式收成 DeepSeek 式单行列表，标题单行省略、时间缩为小字，删除按钮独立悬停显示，不再出现一条历史会话占满整列高度的问题。
  - 交互结果：修复了面板打开后右下角入口与发送区视觉冲突的问题；当前浮动入口在面板展开时自动隐藏，不再压在发送按钮附近造成“logo 和发送混在一起”的误判。
  - 验证结果：已执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；随后按当前 ECS 发布链路重新部署线上。
- 2026-05-04 当前：完成首页首屏主标题与说明区的横向舒展优化，重点解决“准备平台”主文案和说明句过度换行、视觉拥挤的问题，并重新部署 ECS。
  - 首页结果：`src/app/globals.css` 已继续放宽首页首屏内容区宽度，把 `home-hero` 文案区从偏保守的窄列策略改成更宽的横向铺开布局；主标题已取消原先 `15ch` 左右的窄宽限制，并在桌面宽度下改为优先单行展示，避免“这是把学习、训练和模拟面试放在一起的准备平台”被硬压成多行。
  - 说明区结果：首屏说明文案已改成更大的横向占位，桌面端优先单行平铺展示，并把右侧能力标签区保留在同一层横向信息结构里；到中小屏时再自动回退为居中与多行，避免窄屏溢出。
  - 验证结果：已执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果通过；随后按当前 ECS 发布链路重新部署线上。
- 2026-05-04 当前：根据反馈补充 B/C 浅底版 Logo，去掉原先偏重的黑底图形背景。
  - 输出结果：新增 `public/branding/mianmianba-logo-direction-b-seal-light.svg`、`public/branding/mianmianba-logo-direction-c-tech-light.svg` 与对比图 `public/branding/mianmianba-logo-bc-light-overview.svg`。
  - 调整目标：在保留 B 的中文品牌感与 C 的科技产品感前提下，把图形底座改成更适合浅色 UI 的轻量方案，避免 Logo 在首页和导航里显得过重。
- 2026-05-04 当前：补充“面面吧”Logo 三个不同视觉方向，供品牌路线选择。
  - 输出结果：新增 `public/branding/mianmianba-logo-direction-a-dialogue.svg`、`public/branding/mianmianba-logo-direction-b-seal.svg`、`public/branding/mianmianba-logo-direction-c-tech.svg` 与总览图 `public/branding/mianmianba-logo-directions-overview.svg`。
  - 方向说明：A 为“对话人物版”，强调面试官与候选人的面对面交流；B 为“中文印章版”，从“面”字结构提炼中文原创品牌感；C 为“科技字母版”，用 `M + 对话气泡` 表达 AI 产品气质。
  - 当前用途：本轮先用于看方向，不直接替换线上正式 Logo；待确认方向后，再进一步细化成正式导航版、favicon 版和登录页品牌版。
- 2026-05-04 当前：完成学习中心验收前最后一轮收口，修复机器人入口不稳定、重做 Logo 与面板细节，继续扩深知识工厂目录，并清零全仓库 ESLint 错误。
  - 文档助手结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已将旧的机器人圆球 Logo 替换为更接近 Trae 风格的黑底几何符号；入口按钮补齐 `aria-expanded`、`title` 与更稳定的点击状态。面板内部继续往 `chat.deepseek.com` 靠拢，新增更清晰的顶部上下文栏、左侧“发起新对话”入口，并统一空态文案、输入区占位文案和消息区结构。
  - 样式结果：`src/app/globals.css` 已清理此前与新助手样式重叠的一整段旧 CSS，避免同类选择器重复覆盖导致点击区、层级和移动端行为漂移；当前只保留一套最新助手样式定义，并进一步加固入口按钮 `z-index / pointer-events`、面板层级、顶部工具栏、侧栏新对话按钮、消息卡片阴影和移动端布局。
  - 知识工厂结果：`src/lib/learning/autoFactory.ts` 已把 `Java / 前端 / Vue / React / AI / 工程化` 模板继续往“面试鸭式大而全目录”扩深，补齐并发、JVM、Spring、数据库缓存、分布式、业务场景、源码理解、系统设计、项目表达等更细章节；`src/lib/learning/sourceDiscovery.ts` 同步扩大各领域来源池，新增如 `Spring Guides`、`Dubbo`、`Redis`、`W3C`、`WHATWG`、`Chrome Developers`、`TanStack Query`、`react-hook-form`、`Anthropic Docs`、`DeepSeek API 文档`、`LlamaIndex` 等来源，并提高各领域可纳入来源上限。
  - 仓库治理结果：已修复全仓库既有 ESLint 错误，覆盖历史脚本 `clean.js`、`clean_duplicates.js`、`list_sessions.js`、`test-summarizer.js/.ts`、`scripts/import-github.ts`、多组旧 API 路由、`src/lib/ai/router.ts` 与 `src/lib/knowledge/volc.ts`；当前全仓库 `eslint` 已无 error、无 warning。
  - 验证结果：已顺序执行 `node node_modules/eslint/bin/eslint.js . --ext .js,.jsx,.ts,.tsx`、`node node_modules/typescript/bin/tsc --noEmit --pretty false` 与 `npm run build`，结果全部通过；同时确认 `src/app/globals.css` 中旧助手样式残留已被移除，不再与新样式冲突。
- 2026-05-04 当前：完成首页 UI 一轮收口，按最新反馈调整首屏文案、卡片交互与顶部用户区展示。
  - 首页结果：`src/app/home/page.tsx` 已移除首屏顶部“面面吧 · AI 模拟面试平台”，将问候语改为“你好，追梦人”，主标题同步去掉句号；说明文案也已去掉句号，并将四张功能卡改为整卡点击跳转，保留“进入模块”作为视觉提示但不再要求精确点中文本。
  - 样式结果：`src/app/globals.css` 已同步放大首屏问候语字号、放宽主标题与说明区排版，尽量把说明文字收敛到两行展示；同时去掉四张卡片上的 `01/02/03/04` 数字徽标，并移除卡片右下角的圆形装饰，整体观感更干净。
  - 头像区结果：`src/components/layout/Header.tsx` 已将登录后的顶部用户区收口为“头衔 + 头像”结构，不再显示头像右侧名称文字；非 VIP 用户现在会显示“免费版”，VIP 用户继续显示会员头衔。
  - 验证结果：已执行 `node ./node_modules/eslint/bin/eslint.js src/app/home/page.tsx src/components/layout/Header.tsx --max-warnings=0`，结果通过；全量 `node ./node_modules/typescript/bin/tsc --noEmit --pretty false` 当前被仓库既有问题 `src/app/learning/_components/DocumentAssistantPanel.tsx(841,17): error TS18047: 'activeSession' is possibly 'null'.` 阻断，不属于本轮首页改动引入的问题。
- 2026-05-04 当前：完成学习中心新一轮产品纠偏，重做文档机器人交互与知识工厂自动生成策略，并完成本地真测闭环。
  - 文档助手结果：`src/app/learning/_components/DocumentAssistantPanel.tsx` 已改为科技感机器人 Logo 浮动入口，保留用户认可的外层交互框尺寸与位置；内部内容重排为更接近 DeepSeek 网页版的“左侧历史会话 + 右侧消息流 + 底部输入区”结构，支持新对话、删除历史、会话持久化、会话标题自动生成与跨文档引用跳转。
  - 流式结果：`src/app/api/learning/assistant/route.ts` 已从“整段生成完再切段伪流式”重构为“两阶段真流式”链路：先基于知识库证据做规划，再调用 DeepSeek 原生 `stream: true` 持续向前端输出 NDJSON 增量；本地浏览器实测中，发送问题后会先进入 `回答中...`，随后正文逐步出现，不再整段一次性落地。
  - 交互修复结果：本轮顺手修复了机器人面板中多个真实点击阻断问题，包含“新对话”“删除会话”“发送按钮”被样式层 `pointer-events` 挡住的问题；同时在 `next.config.ts` 显式关闭 `devIndicators`，并在 `src/app/globals.css` 保留对 Next 左下角开发态角标的兜底隐藏规则。
  - 知识工厂结果：`src/lib/learning/sourceDiscovery.ts`、`src/lib/learning/autoFactory.ts` 与 `src/app/api/admin/learning/auto-build/route.ts` 已重构为“同领域归并到单一总知识库 + 扩充高质量来源 + 自动发布首批文档”的链路。当前 `Java`、`Vue`、`React`、`前端`、`AI` 等课题会统一归并到各自总知识库，不再按输入词拆成多个零散 KB；Java 来源池已扩充到 Oracle / OpenJDK / Spring / Maven / Spring Boot / MyBatis / JUnit / 高 star GitHub 等更完整来源；自动建库后默认直接发布首批大章节文档，减少人工审核负担。
  - 后台入口结果：`src/app/admin/learning/page.tsx` 的单入口文案和结果区已同步改为“自动扩充知识库并发布首批文档”，展示通过来源数、拦截来源数与自动发布文档数，不再强调“草稿审核工作台”。
  - 验证结果：已执行 `node node_modules/typescript/bin/tsc --noEmit --pretty false`、`npm run build` 与聚焦本轮改动文件的 `node node_modules/eslint/bin/eslint.js ...`，结果通过；另在本地浏览器对 `/learning/[kbId]` 实测通过新对话、删除历史、发送消息与流式回答链路。仓库全量 `eslint` 仍存在既有历史错误，主要集中在本轮未改动的旧脚本与旧 API 文件，不属于本轮新增问题。
- 2026-05-04 当前：完成学习中心 Task22.3 / Task22.4 并收尾 Task21，本地与 ECS 四类回归全部通过，同时补齐部署脚本的 Prisma / 构建收口。
  - 本地环境结果：已从现有 ECS 运行态安全补齐本地 `.env.local` 所需的有效 `DEEPSEEK_API_KEY`，并确认本地 `DATABASE_URL`、`NEXTAUTH_SECRET`、`NEXTAUTH_URL` 均为可用值；随后结束旧的 `next dev` 并重新启动本地开发服务，避免继续命中旧环境变量。
  - 本地真测结果：真实登录 `admin@163.com / yy1741..` 后，`/api/auth/session` 返回 `ADMIN / ACTIVE`，`GET /admin/learning` 页面已包含“单入口知识工厂 / 输入课题即可自动生成 / 自动生成知识工厂”；随后对本地 `POST /api/admin/learning/auto-build`、`POST /api/learning/plan`、`POST /api/learning/summarize`、`POST /api/learning/assistant` 逐项真测，结果均返回 `200` 且结构符合预期。自动建库本地测试课题已在验证完成后通过后台删除接口清理。
  - ECS 阻断与修复结果：本轮复验中发现 ECS `GET /api/auth/providers` 与 `GET /api/auth/csrf` 临时返回 `500`；远端 `journalctl -u resumer` 明确记录 `.next` 运行态缺少 `@prisma/client-2c3a283f134fdcb6` 外部模块，根因是直接下发本地 `.next` 后，远端 Prisma 产物与 Turbopack 生产构建不再一致。已在 ECS `/srv/resumer` 重新执行 `npx prisma generate`、`npm run build` 并重启 `resumer`，随后认证接口恢复正常。
  - ECS 真测结果：修复后，公网 `GET /api/auth/providers`、`GET /api/auth/csrf` 均返回 `200`；再次真实登录 `admin@163.com / yy1741..` 后，`/api/auth/session` 返回 `ADMIN / ACTIVE`，`GET /admin/learning` 页面已命中单入口文案；随后对 ECS `POST /api/admin/learning/auto-build`、`POST /api/learning/plan`、`POST /api/learning/summarize`、`POST /api/learning/assistant` 逐项真测，结果均返回 `200` 且内容正常。ECS 测试课题同样已在验证完成后删除清理。
  - 部署流程结果：已更新 `scripts/ops/deploy-local-build-to-ecs.sh`，远端解包并修正属主后，会额外以 `deploy` 用户执行 `npx prisma generate` 与 `npm run build`，再重启 `resumer`，避免再次出现“本地 `.next` 可用但 ECS 认证因 Prisma 外部模块别名失配而 500”的问题。
  - 任务结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md` 已将 `Task21` 与 `Task22` 全部勾选完成；`checklist.md` 剩余未勾项已全部补齐。
- 2026-05-04 当前：推进学习中心 `Task22.3` 本地验证环境补齐，已消除 PostgreSQL `DATABASE_URL` 非法这一主阻断，并补齐本地引导脚本与回归说明。
  - 环境脚本结果：新增 `scripts/setup-local-validation.mjs` 与 `npm run setup:local-validation`，当前会自动补齐项目内 PostgreSQL 数据目录 `tmp/local-postgres/`、启动本地实例 `127.0.0.1:54329`、写入合法 `.env.local -> DATABASE_URL`，并在业务表不存在时重建本地验证库、按正确顺序手动应用 `prisma/migrations/20260503_init_postgresql` 与 `20260503_expand_user_auth_model`，最后幂等补齐管理员账号 `admin@163.com / yy1741..`。
  - Homebrew 兼容结果：本机 `postgresql@17` 安装缺少 versioned `share/lib` 软链时，脚本会自动补齐 `postgresql@17` 所需的 `share` 与 `lib` 链接，避免 `initdb` 因 `timezone` 或 `dict_snowball` 资源缺失失败。
  - 本地数据库验证结果：已执行 `npm run setup:local-validation`，结果通过；随后使用 `psql 'postgresql://yangyu@127.0.0.1:54329/resumer_local' -c '\dt'` 确认 `User / InterviewSession / Message / Report / Weakness / KnowledgeDocument` 六张业务表已创建；使用 `psql ... -c "select email, role, status from \"User\" where email = 'admin@163.com';"` 确认管理员账号状态为 `ADMIN / ACTIVE`。
  - 运行态验证结果：`GET /api/auth/providers` 与 `GET /api/auth/csrf` 已在本地返回 `200`，说明 NextAuth 运行态可用；但当前已有一个旧的 `next dev` 进程在运行，仍持有修复前环境变量，因此直接请求 `POST /api/auth/callback/credentials` 仍会命中旧 `DATABASE_URL` 并报 Prisma 协议错误。已在脚本输出与 `README.md` 明确要求：执行本地环境引导后，如 `next dev` 已在运行，需先重启开发服务，再继续管理员登录与后台自动建库回归。
  - 文档助手现状：当前本地环境中仍无 `DEEPSEEK_API_KEY`，因此 `POST /api/learning/assistant` 的真实回归尚不能在当前运行态下完成；`README.md` 已补充必填说明，待把有效密钥写入项目根 `.env.local` 并重启开发服务后，即可完成文档助手本地回归。
  - 文档结果：`README.md` 已改写为本地验证指南，新增 PostgreSQL / NextAuth / DeepSeek 前置说明、`npm run setup:local-validation` 用法、管理员测试账号、开发服务重启要求与停止本地 PostgreSQL 的命令。
- 2026-05-04 当前：完成学习中心 Task22.1 / Task22.2，修复 ECS 学习计划旧结构与文档助手 404，并补齐可复用部署脚本。
  - 根因结果：ECS `/srv/resumer` 运行态并非当前工作区版本。远端 `src/app/api/learning/plan/route.ts` 仍是旧版 `groups` 契约，且 `src/app/api/learning/assistant/route.ts` 文件完全不存在；进一步核对本地 `git ls-files src/app/api/learning/plan/route.ts src/app/api/learning/assistant/route.ts` 为空，说明此前发布流程实际只同步了 Git 已跟踪内容，导致新增但未纳入发布包的学习中心路由没有被下发到 ECS。
  - 流程修复结果：新增 `scripts/ops/deploy-local-build-to-ecs.sh`，改为从当前本地工作区直接打包，并连同本地已验证的 `.next` 构建产物一起同步到 `/srv/resumer`；脚本会默认执行远端 PostgreSQL 备份、替换运行态 `.next`、清理 `._*` 苹果元数据、修正 `deploy:deploy` 属主并重启 `resumer`，避免再次因“只发 Git 已跟踪文件”漏发新路由。
  - 本地验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/api/learning/plan/route.ts src/app/api/learning/assistant/route.ts`、`npm run build` 与 `bash -n scripts/ops/deploy-local-build-to-ecs.sh`，结果通过；本地构建产物已明确包含 `ƒ /api/learning/assistant` 与 `ƒ /api/learning/plan`。
  - ECS 部署结果：已执行 `chmod +x scripts/ops/deploy-local-build-to-ecs.sh` 与 `./scripts/ops/deploy-local-build-to-ecs.sh`；脚本在 ECS 上调用 `/srv/resumer/scripts/ops/backup-postgres.sh` 生成备份后完成解包与服务重启，`systemctl is-active resumer` 返回 `active`。
  - ECS 真测结果：`POST http://47.95.233.109/api/learning/plan` 已返回 `trackKey / trackLabel / coverageOverview / steps / uncovered` 新结构，不再返回旧版 `groups`；`POST http://47.95.233.109/api/learning/assistant` 已不再返回 `404`，当前返回 `HTTP/1.1 200 OK` 与正常 JSON 问答结果，示例问题“HashMap 为什么线程不安全？”已返回基于知识库证据的正式回答与引用。
  - 任务结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md` 已将 `SubTask 22.1` 与 `SubTask 22.2` 回填为完成；`Task 22` 其余本地环境补齐与 Task21 全量复验子任务继续保留未完成状态。
- 2026-05-04 当前：执行学习中心 Task21 最终验证，结论为未通过，已新增后续修复任务 `Task 22`。
  - 本地通过项：`POST /api/learning/plan` 已真测返回 `trackKey / trackLabel / coverageOverview / steps / uncovered` 新结构；Java 目标命中 `java-backend` 路线且结果文档未混入 Android，Vue 目标命中 `vue` 路线并输出“为什么学 / 当前缺口 / 覆盖范围 / 文档依据”。
  - 自动建库通过项：已执行 `node --experimental-strip-types --loader ./scripts/alias-loader.mjs -e "import ..."` 直接验证 `src/lib/learning/sourceDiscovery.ts` 与 `src/lib/learning/autoFactory.ts`；结果确认单课题画像、自动来源筛选、主流目录骨架与固定草稿模板均可生成。Java 自动来源优先命中 `Oracle Java 文档`、`OpenJDK`、`openjdk/jdk`、`Spring Framework 官方文档`，Vue 自动来源优先命中 `Vue 官方文档`、`Pinia`、`Nuxt`、`Vite` 与 `vuejs/core`，且低分社区源会被拒绝。
  - 线上通过项：`POST http://47.95.233.109/api/learning/summarize` 对 HashMap 文档返回 `200`，结果包含 `headline / summary / keyPoints / recommendedFocus`，说明文档 AI 总结线上可用。
  - 失败点 1：本地管理员验证被环境阻断。`POST /api/auth/callback/credentials` 登录 `admin@163.com` 时，NextAuth/Prisma 直接报错 `DATABASE_URL` 不符合 PostgreSQL 协议要求，导致无法在本地真实进入 `/admin/learning` 验证单入口页面与后台接口登录态。
  - 失败点 2：本地文档助手回归失败。`POST /api/learning/assistant` 返回 `500`，`.next/dev/logs/next-development.log` 明确记录 `Learning assistant route failed: Error: Missing DEEPSEEK_API_KEY`。
  - 失败点 3：ECS 学习计划仍是旧运行态。`POST http://47.95.233.109/api/learning/plan` 返回旧版 `groups` 结构，而不是当前 Task18/Task21 要求的 `trackKey / coverageOverview / steps / uncovered` 新契约，说明线上未完成最新学习计划链路部署或仍命中旧构建。
  - 失败点 4：ECS 文档机器人未上线。`POST http://47.95.233.109/api/learning/assistant` 返回 `404 Not Found`，说明 Task17 的公开机器人问答路由未在当前线上运行态生效。
  - 文档结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md` 已保留 `Task 21` 未完成状态，并新增 `Task 22` 用于修复本地环境阻断与 ECS 回归后再重跑最终验证。
- 2026-05-04 当前：完成学习中心 Task20，重构知识文档生成模板与质量标准，并补齐发布前审核检查项。
  - 模板结果：新增 `src/lib/learning/documentTemplate.ts`，把正式文档固定收口为“总结归纳、正文解析、流程图/架构图/代码实例、面试常考”四段骨架；`src/lib/learning/autoFactory.ts` 已改为按主题自动判断哪些块必须补齐，并在自动草稿里统一生成固定结构。
  - 降级结果：当前对流程图、架构图、代码实例等块统一改为“缺失即标记”；自动链路拿不到可核对依据时，会明确写入缺失标记或按主题可选说明，不再用空泛段落凑数。
  - 审核结果：`src/app/api/admin/learning/draft/route.ts`、`update/route.ts`、`publish/route.ts` 与 `src/app/admin/learning/[kbId]/draft/[draftId]/page.tsx` 已接入发布前质量检查；当前会校验固定骨架、主题必需块、面试常考数量与主题纯度，未通过时禁止将草稿标记为“可发布”或直接发布。
  - 校验脚本结果：新增 `scripts/learning-document-template-check.mjs` 与 `scripts/alias-loader.mjs`，覆盖“旧草稿对齐固定骨架、必需块缺失时阻断发布、完整草稿允许发布”三类 Task20 核心断言。
  - 文档结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md` 已将 Task20 与四个子任务全部回填为完成，`checklist.md` 中正式文档固定骨架要求也已勾选完成。
  - 验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/lib/learning/documentTemplate.ts src/lib/learning/sourceDiscovery.ts src/lib/learning/autoFactory.ts src/app/api/admin/learning/draft/route.ts src/app/api/admin/learning/draft/update/route.ts src/app/api/admin/learning/draft/publish/route.ts 'src/app/admin/learning/[kbId]/draft/[draftId]/page.tsx' scripts/learning-document-template-check.mjs scripts/alias-loader.mjs` 与 `node --experimental-strip-types --loader ./scripts/alias-loader.mjs scripts/learning-document-template-check.mjs`；结果通过。最后一个命令仅有 Node 对实验性 loader 和无 `type: module` 的提示警告，不影响断言结果。
- 2026-05-04 当前：完成学习中心 Task19，将知识工厂重做为“输入课题即可自动生成”的单入口自动化系统。
  - 单入口结果：`src/app/admin/learning/page.tsx` 已改为默认展示“输入课题 -> 自动生成知识工厂”的主入口，管理员输入 `Java`、`Vue`、`前端工程化` 等课题后，会直接触发自动建库，不再要求先手动新建 KB 再逐步点采集、归类与草稿按钮。
  - 自动链路结果：新增 `src/app/api/admin/learning/auto-build/route.ts` 与 `src/lib/learning/autoFactory.ts`，将自动来源筛选、目录初始化、草稿生成串成同一条后台链路；当前会自动创建知识库、登记通过门槛的来源、生成边界受控的目录骨架，并把首批章节草稿送入待审核队列。
  - 来源与边界结果：`src/lib/learning/sourceDiscovery.ts` 已重构为课题画像 + 来源评分模型，支持 Java / 前端 / Vue / React / AI 等领域模板，优先官方文档、高权威社区资料和高 star GitHub 仓库；未通过质量门槛或越过领域边界的来源会被直接拒绝，例如 Java 不会混入 Android，Vue 不会混入 React。
  - 工作台结果：`src/app/admin/learning/[kbId]/page.tsx` 的来源列表已补充质量分、来源类别、边界结果与选择理由，进入 KB 工作台后可直接复核自动筛选依据；旧的自动发现接口 `src/app/api/admin/learning/discover/route.ts` 也已复用新规则。
  - 文档结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md` 已将 Task19 与四个子任务全部回填为完成。
  - 验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/lib/learning/sourceDiscovery.ts src/lib/learning/autoFactory.ts src/lib/db/learningFactory.ts src/app/api/admin/learning/discover/route.ts src/app/api/admin/learning/auto-build/route.ts src/app/admin/learning/page.tsx 'src/app/admin/learning/[kbId]/page.tsx'` 与 `npm run build`，结果全部通过。
- 2026-05-04 当前：完成学习中心 Task17，为文档阅读页增加 DeepSeek 风格机器人对话助手，并顺手清理本轮编译阻断。
  - 入口与面板结果：`src/app/learning/_components/LearningCenterClient.tsx` 已新增“机器人助手”入口；新增 `src/app/learning/_components/DocumentAssistantPanel.tsx` 作为固定悬浮入口 + 右侧抽屉面板，当前支持消息面板、历史会话列表、新建对话、跨文档切换与继续追问。
  - 会话持久化结果：文档助手历史已按知识库维度持久化到浏览器 `localStorage`，每条历史会话保留标题、所属文档、最后更新时间、最近一条消息摘要与完整消息流；刷新页面后仍可恢复并继续对话。
  - 上下文工程结果：新增 `src/app/api/learning/assistant/route.ts`，服务端会基于当前文档、同分组相邻文档和知识库内相关文档构造证据包，多轮问答时持续携带最近消息与当前证据上下文。
  - 真实性结果：当前文档助手已改为“有依据才回答”的结构化协议；若模型未给出有效引用、问题超出当前知识库依据，或证据不足，接口会统一拒答“当前知识库未提供足够信息”；涉及版本、配置、兼容性、生产建议等问题时会补充必要核对提示。
  - 顺手修复结果：同步修正 `src/app/api/admin/learning/auto-build/route.ts` 对 `learningFactory.updateDraft()` 的旧调用签名，消除本轮 `tsc / build` 暴露的既有编译阻断。
  - 验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/api/learning/assistant/route.ts src/app/learning/_components/DocumentAssistantPanel.tsx src/app/learning/_components/LearningCenterClient.tsx src/app/api/admin/learning/auto-build/route.ts` 与 `npm run build`，结果全部通过；构建产物已包含 `ƒ /api/learning/assistant`。
- 2026-05-04 稍早：完成学习中心 Task18，重构学习计划生成逻辑与入口页展示依据。
  - 接口结果：`src/app/api/learning/plan/route.ts` 已从旧的“体系 -> 文档链接”结构重构为“主流路线基线 + 真实覆盖分析”结构，统一输出 `trackKey`、`trackLabel`、`coverageOverview`、`steps`、`uncovered`；每一步都会明确学习顺序、推荐原因、能力缺口、覆盖范围和对应文档依据，不再只给链接。
  - 路线结果：当前已内置 Java 后端、前端、Vue、通用四套路线基线；Java 后端会按“语言基础 -> 集合并发 -> JVM -> 排障调优 -> 框架分布式”排序，前端与 Vue 会按主流路线生成顺序，但如果公开知识库没有对应文档，会明确标成“待补内容”，不会伪造无关链接。
  - 前端结果：`src/app/learning/_components/LearningCenterClient.tsx` 已改造学习计划面板类型与详情区展示；当前会展示路线标签、覆盖概览、每一步的推荐原因/能力缺口/覆盖范围，以及对应文档依据卡片；`src/app/globals.css` 也已同步补齐步骤状态徽标、信息块和文档依据样式，不再只是文档标题列表。
  - 文档结果：`.trae/specs/rebuild-learning-center-knowledge-factory/tasks.md` 已将 Task18 与四个子任务全部回填为完成。
  - 验证结果：已执行 `node ./node_modules/eslint/bin/eslint.js src/app/api/learning/plan/route.ts src/app/learning/_components/LearningCenterClient.tsx`、`node ./node_modules/typescript/bin/tsc --noEmit` 与 `npm run build`，结果全部通过。
- 2026-05-04 14:00 前后：完成学习中心 Task16，移除文档 AI 总结编造内容的兜底逻辑，并补齐前后端真实性校验。
  - 总结真实性结果：新增 `src/lib/learning/documentSummary.ts` 统一校验公开文档与后台原始资料的“正文是否足够总结”；公开接口 `src/app/api/learning/summarize/route.ts` 已移除标题/速览兜底与异常回退总结，正文不足时统一返回“当前文档正文信息不足，暂时无法生成 AI 总结，请补充正文后再试。”。
  - 后台链路结果：`src/lib/ai/summarizer.ts`、`src/app/api/admin/learning/summarize/route.ts`、`src/app/api/admin/learning/ingest/route.ts`、`src/app/api/admin/learning/batch/route.ts` 已同步接入正文充足性与结果结构校验；资料过短时不再继续生成草稿总结，并会把来源状态回写为失败原因。
  - 前端结果：`src/app/learning/_components/LearningCenterClient.tsx` 已在阅读页复用同一套正文判断，正文不足时会禁用“AI 总结”按钮并展示信息不足提示，避免用户继续触发编造型总结。
  - 额外校验结果：新增 `scripts/learning-summary-guard-check.mjs`，覆盖“短正文拒绝总结、充足正文允许总结、缺字段结果不再兜底”三类 Task16 核心断言。
  - 验证结果：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/api/learning/summarize/route.ts src/lib/learning/documentSummary.ts src/lib/ai/summarizer.ts src/app/api/admin/learning/summarize/route.ts src/app/api/admin/learning/ingest/route.ts src/app/api/admin/learning/batch/route.ts src/app/learning/_components/LearningCenterClient.tsx`、`node --experimental-strip-types scripts/learning-summary-guard-check.mjs` 与 `npm run build`，结果通过；校验脚本运行时仅有 Node 对 `.ts` ESM 解析方式的提示警告，不影响断言结果。
- 2026-05-04 21:35-21:55：修复学习中心第二轮上线后的线上权限与体验问题，并重新部署 ECS。
  - 权限修复：定位到 `resumer.service` 运行用户为 `deploy`，但 `/srv/resumer` 与 `data/learning-center` 目录属主被打包解压成 `501:games`，导致管理员新建知识库时报 `EACCES: permission denied, open '/srv/resumer/data/learning-center/kbs/.../meta.json'`；已执行 `chown -R deploy:deploy /srv/resumer` 并删除历史 `._*` 脏文件，随后用 `sudo -u deploy` 实测目录创建与 `meta.json` 写入通过。
  - 知识工厂收口：将 `src/app/admin/learning/[kbId]/page.tsx` 从多块“Task / 多 AI / 流水线”面板改为线性四步工作台：基础设置、步骤 1 采集、步骤 2 归类、步骤 3 总结与发布、步骤 4 调度配置；保留原有能力，但整体交互和文案明显减负。
  - 学习计划收口：`/learning` 的学习计划入口文案与结果卡片继续减 AI 味，按钮从“AI 学习计划”收口为“学习计划”；详情区只保留目标、生成时间、体系和文档链接，去掉大段解释性文案与多余卡片层级，并把滚动容器改成明确高度的独立滚动区域。
  - 本地验证：执行 `node ./node_modules/typescript/bin/tsc --noEmit`、聚焦改动文件的 `eslint`、`npm run build` 全部通过。
  - ECS 部署：远端先生成备份 `/var/backups/resumer/postgres/resumer_prod_20260504_132326.dump`，随后重新下发当前代码并执行 `systemctl restart resumer`；部署后远端 `systemctl is-active resumer` 为 `active`。
  - ECS 复验：`http://127.0.0.1:3000/learning` 返回 `200`；`http://127.0.0.1:3000/admin/learning` 返回登录态重定向 `307`；公网 `POST http://47.95.233.109/api/learning/plan` 返回 `groups=4` 且 `has_phases=False`，确认线上仍为新计划结构。
- 2026-05-04 21:10-21:35：继续按最新反馈收口学习中心入口与阅读工作区，并完成新版本 ECS 部署。
  - 学习计划结果：`/learning` 的 AI 学习计划面板在没有历史记录时只保留输入区，不再渲染空的历史/详情容器；一旦已有历史计划，历史目标改为独立滚动容器，避免把知识库列表整体向下顶得过长。
  - 阅读工作区结果：`/learning/[kbId]` 已支持左侧目录栏和右侧“本页目录”拖拽调宽，并加了最小/最大宽度限制；右侧大纲默认宽度也已放大，减少标题被挤压的感觉。
  - 字体排版结果：继续参考语雀公开阅读页体验，阅读工作区统一切到更接近语雀的系统 UI 中文字体栈，并同步收口标题字重、正文宽度、字号、行高和段落节奏，不再只是简单替换 `font-family`。
  - 本地验证结果：执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `npm run build` 均通过；聚焦 `LearningCenterClient.tsx` 的 `eslint` 通过，`globals.css` 因当前仓库 ESLint 未配置 CSS 检查而被忽略，未出现新的脚本级报错。
  - ECS 部署结果：先在远端执行 `/srv/resumer/scripts/ops/backup-postgres.sh`，生成 `resumer_prod_20260504_130545.dump`；随后通过本地构建 + `tar + scp + 远端解包` 下发到 `/srv/resumer`，再执行 `systemctl restart resumer`，服务状态保持 `active`。
  - ECS 验证结果：远端内网 `http://127.0.0.1:3000/learning` 与 `http://127.0.0.1:3000/learning/javae79fa5e8af86e5ba93?doc=hashmap20e6ba90e7a081e58886e69e90` 均返回 `200`；公网 `POST http://47.95.233.109/api/learning/plan` 已返回 `groups=4` 且 `has_phases=False`，确认线上已切到新的学习计划结构。
- 2026-05-04 20:30-21:10：按最新产品反馈完成学习中心第二轮产品收口，重点重做入口页、学习计划展示、阅读工作区与知识工厂自动来源发现。
  - 入口页结果：`/learning` 已删除上一版偏重说明性的入口模块，改为更极简的知识库列表；知识库项只保留“名称、简介、更新时间”，并将学习计划入口收口为知识库页角标按钮。
  - 学习计划结果：`/api/learning/plan` 已从“阶段化 + 去训练”结构改为“历史记录友好”的 `groups` 结构；前端会把每次生成结果写入当前浏览器本地历史列表，并按“体系 -> 文档链接列表”展示，不再混入专项训练入口。
  - 阅读工作区结果：结合语雀开发者文档的真实页面结构，重新收口 `src/app/learning/_components/LearningCenterClient.tsx` 与学习中心样式；当前阅读页改为左侧目录树、中间正文、右侧轻量大纲，AI 总结移回正文流内，工作区字体、留白和边框层次都已明显减重。
  - 知识工厂结果：新增 `src/lib/learning/sourceDiscovery.ts` 与 `/api/admin/learning/discover`，后台工作台增加“自动来源发现 AI”入口，可根据知识库名称、标签和描述自动补齐官方文档与社区资料来源，降低手工贴链接依赖。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、聚焦改动文件的 `eslint` 全部通过；本地浏览器已实看 `/learning` 与 `/learning/[kbId]` 新结构；接口真测 `POST /api/learning/plan` 返回 `groups` 字段且不再返回旧 `phases` 结构。
- 2026-05-04 20:10-20:22：完成学习中心重构版的 ECS 部署与线上复验，当前可直接上服务器查看公开学习中心与后台知识工厂。
  - 部署结果：先在 ECS `47.95.233.109` 上执行 `/srv/resumer/scripts/ops/backup-postgres.sh`，生成 `resumer_prod_20260504_121002.dump`；随后通过 `tar + scp + 远端解包` 将当前工作区代码同步到 `/srv/resumer`。
  - 构建结果：远端默认 `next build` 与 `npx next build --webpack` 均因 ECS 资源瓶颈被系统 `SIGKILL`；进一步排查确认 `src/app/layout.tsx` 使用 `next/font/google` 会触发构建期外网字体拉取，因此已收口为系统字体变量，消除远端构建对 Google 字体下载的依赖。
  - 上线方式：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `npm run build` 均通过后，将本地已验证的 `.next` 构建产物同步到 ECS，替换 `/srv/resumer/.next`，再执行 `systemctl restart resumer`；`systemctl is-active resumer` 返回 `active`。
  - 线上验证结果：内网 `http://127.0.0.1:3000/learning` 与 `http://127.0.0.1:3000/api/learning` 均返回 `200`；公网 `http://47.95.233.109/api/learning` 返回 `200` 且当前知识库数为 `3`；`/api/learning/plan` 返回 `200` 并生成 `3` 个阶段计划；`/api/learning/summarize` 返回 `200`，示例标题为 `HashMap 核心原理`。
  - 后台验证结果：使用管理员账号 `admin@163.com / yy1741..` 真实登录后，请求 `http://47.95.233.109/admin/learning` 返回 `200`，页面已包含“知识工厂”相关内容，说明学习后台也已上线。
- 2026-05-04 当前：完成学习中心 Task5-8 的最小增量落地，将管理员后台重构为知识工厂，并补齐来源采集、目录规划、多 AI 流水线状态、草稿审核、发布记录与基础调度。
  - 后台结果：`/admin/learning` 已升级为知识工厂总览页，支持查看知识库级来源数、草稿数、调度数、发布数，并可创建/更新知识库配置后进入 KB 工作台。
  - 工作台结果：`/admin/learning/[kbId]` 已改为单 KB 一体工作台，覆盖知识库配置、手动来源采集、GitHub 批量导入到草稿队列、AI 目录规划、手动目录补点、草稿审核队列、发布记录与基础调度规则。
  - 审核结果：`/admin/learning/[kbId]/draft/[draftId]` 已补齐来源追溯、流水线状态、审核备注、改动摘要和发布动作；发布后会写入知识库内容并记录发布历史。
  - 存储结果：新增学习中心专用工厂态侧边索引 `data/learning-center/admin-state.json`，用于保存来源、调度和发布记录；现有 `learningDb` 草稿记录同步扩展了审核状态、来源关联与流水线元数据，但未改动学习中心以外模块。
  - 验证说明：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、聚焦学习中心相关文件的 `eslint` 与 `npm run build`，结果均通过；本轮仍只改学习中心相关文件，未回滚工作区已有无关脏改动。
- 2026-05-04 当前：完成学习中心 Task2-4 的公开部分最小增量落地，仅调整学习中心相关页面、样式、公开接口与专项训练预填联动。
  - 入口页结果：`/learning` 已改为公开知识库大类入口页，支持按知识库/标签/文档标题筛选，并新增学习计划助手入口，基于当前公开知识库生成阶段化学习建议。
  - 阅读工作区结果：新增 `/learning/[kbId]?doc=<topicId>` 路由，页面已升级为语雀式三栏工作区，包含左侧目录树、中间长文阅读区、右侧大纲与文档辅助面板；现有 Markdown、Mermaid、提示块等结构继续兼容。
  - 学练联动结果：新增公开学习中心文档 AI 总结接口与学习计划接口；文档页支持“AI 总结当前内容”和“去专项训练”，并将文档标题、摘要、推荐追问方向透传到 `/practice` 预填输入中。
  - 验证说明：已执行 `node ./node_modules/typescript/bin/tsc --noEmit`、聚焦学习中心相关文件的 `eslint` 与 `npm run build`，结果均通过；当前仍只在学习中心相关文件内完成最小改动，未回滚工作区已有无关脏改动。
- 2026-05-04 现在：完成学习中心 Task1 最小增量落地，`learningDb` 已升级为“文件优先内容仓 + 索引兼容层”，并保留旧 `data/learning-center.json` 的一次性迁移兼容。
  - 存储结果：新增 `data/learning-center/index.json` 作为兼容索引，知识库内容拆分到 `data/learning-center/kbs/<kbId>/meta.json`、`tree.json`、`topics/<topicId>.json`，草稿拆分到 `data/learning-center/drafts/<kbId>/<draftId>.json`。
  - 兼容结果：现有学习中心 API 与页面继续复用 `learningDb.getLearningData()` 返回的 `kbs / trees / contents / drafts` 聚合结构，无需同步改动前后台调用层。
  - 迁移结果：首次读取新内容仓时，会自动将旧 `data/learning-center.json` 作为迁移输入写入新目录；迁移完成后，旧 JSON 不再作为主写入目标。
- 2026-05-03 09:00 前后：完成项目交接梳理，确认 `.trae/documents/`、`.trae/specs/`、PRD 与关键路由结构。
- 2026-05-03 10:00 前后：完成数据存储盘点，明确“业务主数据迁移 PostgreSQL，学习中心暂不迁移”的基线。
- 2026-05-03 10:00-11:00：完成阿里云 ECS 基础部署、Nginx 与 systemd 配置、GitHub 仓库接入。
- 2026-05-03 11:00 前后：完成 SQLite 到 PostgreSQL 的正式迁移，业务主数据已切换到 PostgreSQL。
- 2026-05-03 11:35：修复线上登录故障。
  - 问题结果：`NEXTAUTH_SECRET` 在 `.env` 与 `.env.local` 中被写成空字符串，且 `resumer.service` 未加载环境文件，导致 NextAuth 持续报 `NO_SECRET`。
  - 处理结果：为 `resumer.service` 增加 `EnvironmentFile=-/srv/resumer/.env` 与 `EnvironmentFile=-/srv/resumer/.env.local`，重新生成有效 `NEXTAUTH_SECRET` 并重启服务。
  - 验证结果：使用账号 `yangyudei163@163.com` 登录成功，页面可正常进入 `/dashboard`。
- 2026-05-03 12:20-12:28：完成任务 7 运维安全基线落地，新增最小侵入备份/轮换脚本并接入 ECS。
  - 脚本结果：新增 `scripts/ops/backup-postgres.sh`、`verify-postgres-backup.sh`、`rotate-db-password.sh`、`rotate-auth-secrets.sh`、`install-backup-cron.sh` 与 `scripts/ops/lib.sh`，另补充 `doc/ops-security-baseline.md` 记录执行步骤、回滚方案与秘密清单。
  - 服务器接入结果：已通过 SSH 将脚本同步到 `/srv/resumer/scripts/ops`，并安装 `/etc/cron.d/resumer-postgres-backup`，当前计划为每天 `03:17` 自动备份 PostgreSQL 到 `/var/backups/resumer/postgres`，默认保留 7 天。
  - 备份验证结果：首次手工备份发现 ECS 缺少 `shasum` 且 `DATABASE_URL` 含 Prisma `schema` 参数，已兼容修复；之后手工备份成功生成 `resumer_prod_20260503_122742.dump`、`.manifest`、`.sha256`。
  - 恢复验证结果：执行 `verify-postgres-backup.sh --restore-check` 成功，临时恢复库校验通过，恢复得到 7 张非系统表。
  - 轮换验证结果：`rotate-db-password.sh --dry-run` 与 `rotate-auth-secrets.sh --dry-run` 均通过；本轮未实际轮换线上密码/密钥，避免无必要影响生产会话。
  - 健康检查结果：`systemctl is-active resumer` 返回 `active`，`curl http://127.0.0.1:3000/api/auth/providers` 返回 `200`。
- 2026-05-03 12:30 前后：完成任务 1-2 后端基础改造，补齐用户模型、角色会话、注册校验、权限工具与用户管理 API。
  - 模型结果：扩展 `Prisma User` 字段，新增 `role`、`status`、`vipType`、`vipExpiresAt`、`nickname`、`avatarUrl`、`lastLoginAt`，并补充迁移脚本将 `admin@resumer.com` 回填为管理员。
  - 认证结果：`NextAuth` 会话与 JWT 已透传角色、账号状态、会员信息、头像与最近登录时间；登录时会校验账号状态并回写 `lastLoginAt`。
  - 接口结果：新增 `admin/users` 与 `user/profile` 基础接口，注册接口增加邮箱/密码/昵称校验；现有后台管理路由统一切换到角色型权限工具，不再依赖邮箱字符串猜测管理员。
  - 验证结果：`node ./node_modules/typescript/bin/tsc --noEmit` 通过，`node ./node_modules/eslint/bin/eslint.js ...` 通过，`npx prisma generate` 通过。
  - 环境说明：`npx prisma validate` 因本地 `DATABASE_URL` 非 `postgresql://`/`postgres://` 格式失败；`npx tsc`、`npx eslint` 的二进制入口异常，已改用底层 `node ./node_modules/...` 命令完成检查。
- 2026-05-03 13:30 前后：完成任务 1/3/4/5/6 前台实现，保留现有面试与 `/profile` 解析页主流程不变。
  - 首页与路由结果：新增公开首页 `/home`，根路径 `/` 已切换跳转到 `/home`；顶部导航改为品牌化公开导航，未登录也可浏览核心页面入口。
  - 认证体验结果：新增统一登录注册弹层与复用表单，`/login` 独立页与弹层共用同一套表单逻辑；`dashboard`、`setup`、`practice`、`profile` 确认页、`interview`、`review`、`report` 已接入“动作触发登录、成功后续接原动作”的前台流程。
  - 用户侧结果：新增头像详情页 `/me`，支持查看昵称、邮箱、会员、账号状态、最近登录时间并编辑姓名/昵称/头像地址；未登录访问时展示受保护框架与登录引导。
  - 管理侧结果：新增 `admin` 用户管理页 `/admin/users`，支持按关键词/角色/状态筛选，查看用户列表并编辑角色、账号状态、会员类型、到期时间、姓名、昵称与头像地址。
  - 兼容性结果：保留现有 `/profile` 作为面试画像确认页，不复用为个人资料页，避免破坏“简历解析 -> 画像确认 -> 进入面试”的既有主链路。
  - 验证结果：`node ./node_modules/typescript/bin/tsc --noEmit` 通过；聚焦本次改动文件的 `eslint` 通过；全量 `node ./node_modules/eslint/bin/eslint.js src --ext .ts,.tsx` 仍失败，当前剩余为仓库既有问题，共 35 个 error、11 个 warning，主要集中在既有 `api/*`、`interview/page.tsx`、`learning/page.tsx` 与若干管理页中的 `any` 类型和旧警告。
- 2026-05-03 12:40：完成验收阻断项修复，解决 `/login` 页 `useSearchParams` 构建报错并完成本地构建验收。
  - 修复结果：`src/app/login/page.tsx` 按 Next 16 异步请求参数约定改为在服务端 `await searchParams` 后透传给 `AuthForm`，移除页面级 `useSearchParams()` 预渲染依赖。
  - 顺手修复：`src/app/practice/page.tsx` 同样补充 `Suspense` 边界，消除构建阶段暴露出的同类 `useSearchParams()` 阻断，未改动既有训练流程逻辑。
  - 验证结果：本地执行 `npm run build` 通过，`/login`、`/practice` 均完成静态页面生成，未再出现 `missing-suspense-with-csr-bailout` 报错。
- 2026-05-03 12:43-12:49：完成认证与用户管理改动的 ECS 安全部署，并执行任务 8 验收。
  - 部署结果：先执行 `/srv/resumer/scripts/ops/backup-postgres.sh` 生成 `resumer_prod_20260503_124328.dump`，随后以排除 `.env*`、`data/learning-center.json`、`node_modules`、`.next` 的方式将本地代码同步到 `/srv/resumer`；在 ECS 上执行 `npx prisma generate`、`npx prisma migrate deploy`，成功应用迁移 `20260503_expand_user_auth_model`，再执行 `npm run build` 与 `systemctl restart resumer`。
  - 健康检查结果：`systemctl is-active resumer` 返回 `active`；`curl http://127.0.0.1:3000/home` 返回 `200`；`curl http://127.0.0.1:3000/api/auth/providers` 返回 `200`；使用 Prisma 直连生产库读取 `User` 总数为 `8`。
  - 任务 8 关键场景结果：匿名访问 `/home`、`/practice`、`/me` 页面可达；未登录访问 `/api/user/profile` 与 `/api/admin/users` 均返回 `401`；临时普通用户完成注册、登录、个人资料读取与更新；普通用户访问后台 API 返回 `403`、访问后台页面被 `307` 跳转出后台；临时 admin 登录后台页面返回 `200`，可按关键词检索用户并更新会员类型与到期时间。
  - 任务 8 异常场景结果：临时普通用户会员时间被管理员分别写入未来与过期时间后，后台接口均返回最新值；将临时普通用户状态切到 `DISABLED` 后，凭证登录被重定向到 `/api/auth/error?error=Account%20is%20disabled`；恢复为 `ACTIVE` 后可再次成功登录到 `/dashboard`；服务重启后认证接口持续可用，说明本轮迁移/重启未造成明显中断。
  - 清理与遗留：验收过程中创建的临时普通用户与临时 admin 已从生产库删除，用户总数恢复为 `8`；现有普通用户 `yangyudei163@163.com` 与管理员 `admin@resumer.com` 已核对仍在库且状态正常。
- 2026-05-03 12:55：完成现有正式用户线上实登复核，补齐任务 8 最后一项验收。
  - 复核结果：先退出当前浏览器会话，再使用用户提供凭证 `yangyudei163@163.com / yy1741..` 在首页登录弹层完成正式登录，页面成功从匿名态恢复为“杨宇”登录态，说明本轮认证与用户管理重构未破坏现有用户登录能力。
  - 文档结果：`.trae/specs/refactor-auth-and-user-management/tasks.md` 与 `checklist.md` 已全部勾选完成。
- 2026-05-03 13:50 前后：完成会员体系展示收敛、admin 后台简化与首页极简化改造，并已同步上线 ECS。
  - 首页结果：`/home` 已删除 `Visitor Preview`、匿名浏览说明、公开入口说明、动作级鉴权说明和步骤说明，只保留产品介绍与四个模块入口：模拟面试、专项训练、学习中心、复盘中心。
  - 导航结果：顶部导航已移除“工作台”；登录/注册按钮重新收敛为简洁圆角按钮；admin 入口文案调整为“学习后台”以避免冗余。
  - 头像区结果：当前用户信息只保留一个名称；“查看个人详情”和“退出登录”改为头像 hover/focus 后展示；有效 VIP 用户会在头像旁显示“尊贵的VIP用户”。
  - 复盘中心结果：未登录态已改为少量 demo 复盘卡片，并统一提示“登录体验完整功能”，不再堆叠访问机制解释文案。
  - 后台结果：`/admin/users` 增加用户/会员摘要、前台会员展示预览、统一的会员类型选择项，页面整体改为更简洁的运营视图。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、聚焦文件 `eslint` 与 `npm run build` 全部通过；线上 `/srv/resumer` 已完成重新构建与 `systemctl restart resumer`，`/home` 线上页面已确认更新成功。
- 2026-05-03 13:38 前后：继续完成会员权益与 admin 批量运营任务 1/2/3，本轮仅修改会员权益、`/me`、`/admin/users`、`/api/admin/users`、`userPresentation` 及相关任务文档。
  - 会员模型结果：在 `src/lib/userPresentation.ts` 统一沉淀免费用户、月度 VIP、季度 VIP、年度 VIP、终身 VIP 五档权益模型，新增统一档位选项、权益摘要、权益列表和到期降级说明，前后台改为复用同一套会员展示规则。
  - 用户侧结果：`/me` 新增“会员权益摘要”区块，展示当前会员身份、权益说明、到期信息和降级规则，仍保留原有资料编辑流程不变。
  - 管理侧结果：`/admin/users` 新增批量勾选、全选当前结果、清空选择、批量角色/状态/会员类型/到期时间操作，以及成功数、失败数和失败原因反馈；单用户详情区同步补齐当前权益快照与编辑态会员权益预览。
  - 接口结果：`/api/admin/users` 已支持统一校验单用户与批量更新请求，批量模式会返回 `totalRequested`、`successCount`、`failureCount`、`failures` 与更新后的用户数据，便于前台即时反馈。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/me/page.tsx src/app/admin/users/page.tsx src/app/api/admin/users/route.ts src/lib/userPresentation.ts` 与 `npm run build` 全部通过。
- 2026-05-03 13:43 前后：完成前台任务 4/5/6 视觉收敛，调整 `/home` 标题与四卡片、统一头部和认证弹层登录注册按钮样式。
  - 首页结果：`/home` 主标题文案调整为更聚焦“学习、训练、模拟面试同一路径”的表达，补充能力概览标签行，整体标题层级与段落宽度同步收紧，首屏阅读节奏更清晰。
  - 四卡片结果：模拟面试、专项训练、学习中心、复盘中心四张入口卡片新增克制的品牌色点缀，分别复用橙、蓝、绿三组品牌辅助色做顶部细线、标签与链接强调，保留整体极简基底不变。
  - 认证按钮结果：顶部匿名态登录/注册按钮与认证弹层内登录/注册切换按钮统一为同一套圆角胶囊视觉语言，补齐悬停与阴影层次；提交按钮同步收口为一致的深色主按钮样式。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/home/page.tsx src/components/layout/Header.tsx src/components/auth/AuthForm.tsx` 与 `npm run build` 全部通过；本地开发服务访问 `curl -I http://127.0.0.1:3000/home` 返回 `200`。
- 2026-05-03 14:10 前后：完成过期会员前后台展示口径收敛，修复 `/me` 与 `/admin/users` 对 `requested/effective label` 的混用问题。
  - 代码结果：在 `src/lib/userPresentation.ts` 新增“当前生效会员标签”公共方法；`/me` 与 `/admin/users` 所有面向当前展示的会员标签、权益标题与预览文案统一切换为 `effectiveLabel` 口径，过期会员统一按当前生效的免费用户展示，具体到期说明继续保留在权益说明区。
  - 改动范围：仅调整 `src/lib/userPresentation.ts`、`src/app/me/page.tsx`、`src/app/admin/users/page.tsx`，未改动会员接口、数据库结构和后台更新逻辑。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/me/page.tsx src/app/admin/users/page.tsx src/lib/userPresentation.ts` 全部通过。
- 2026-05-03 13:51-13:55：完成认证与报告页说明文案收敛，按最小改动处理 `AuthForm`、`/login`、`/report`。
  - 文案结果：删除或弱化“动作级鉴权”“回流”“统一表单逻辑”等机制说明，改为更直接的产品导向表达；`AuthForm` 品牌标识收敛为“面面吧”，默认副标题与底部说明卡同步精简。
  - 页面结果：`/login` 首屏由流程解释调整为产品能力概览；`/report` 未登录态改为极简登录引导，仅保留查看报告所需的必要提示，并将次按钮返回路径改为 `/home`。
  - 验证结果：已执行聚焦本次改动文件的 `tsc` 与 `eslint` 检查，结果通过。
- 2026-05-03 14:20 前后：完成首页首屏收口与残余机制说明文案清理，补齐当前 spec 的最后验收项。
  - 首页结果：`/home` 主标题改为“你好，开发者，这是把学习、训练和模拟面试放在一起的准备平台。”，段落同步收敛成更自然的一句话说明，保持首屏信息量克制。
  - 样式结果：首页四张入口卡片继续保留基于品牌色的克制点缀；头部与认证弹层的登录/注册按钮维持同一套胶囊式视觉语言，交互状态未回退。
  - 文案结果：继续清理 `setup`、`profile`、`dashboard` 中残余的“匿名浏览”“继续当前动作”“Visitor Workspace”等机制说明，统一改为产品导向表达，不改既有登录链路。
  - 验证结果：执行 `node ./node_modules/typescript/bin/tsc --noEmit`、聚焦文件 `eslint` 与 `npm run build` 全部通过；全文检索未再发现本轮已识别的旧机制说明关键词。
- 2026-05-03 14:06-14:07：完成本轮改动的 ECS 同步与线上健康检查。
  - 部署结果：使用 `tar + scp + 远端解包` 方式将当前代码同步到 `/srv/resumer`，同步前已执行 PostgreSQL 备份脚本 `./scripts/ops/backup-postgres.sh`。
  - 服务结果：远端执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 后重启 `resumer` 服务；`systemctl status resumer` 显示服务处于 `active (running)`。
  - 验证结果：内网 `http://127.0.0.1:3000/home` 与公网 `http://47.95.233.109/home` 均返回 `200`，且首页已命中新主标题文案，确认本轮首页与会员/admin 收口结果已上线。
- 2026-05-03 14:30 前后：继续微调首页首屏布局，处理“标题和说明过于聚在一起”的视觉问题。
  - 布局结果：`/home` 首屏由单窄列改为横向铺开的双区结构，标题放在左侧更宽区域，说明与能力标签放到右侧，减少大标题密集换行。
  - 样式结果：基于既有 `brand-guidelines` 配色保持克制，仅通过宽度、网格和间距调整来提升舒展感，不新增重装饰元素。
  - 验证结果：`node ./node_modules/typescript/bin/tsc --noEmit` 通过；针对首页页面文件的 `eslint` 检查通过，样式文件因当前 ESLint 配置未覆盖 CSS 而被忽略。
- 2026-05-03 14:35 前后：继续优化首页首屏层次，并完成最新版本的 ECS 上线。
  - 首页结果：`/home` 首屏进一步调整为更舒展的横向主视觉，新增独立的“你好，开发者。”引导行，主标题宽度继续放开；右侧说明区增加更稳定的辅助信息层级，能力标签改成更规整的双列分布。
  - 样式结果：继续遵循 `brand-guidelines`，仅使用克制的橙色轻提示、浅边线和留白增强层次，不增加多余装饰元素。
  - 本地验证结果：执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/home/page.tsx` 与 `npm run build` 全部通过。
  - 上线结果：已通过 `tar + scp + 远端解包` 同步到 ECS；上线前再次执行 PostgreSQL 备份，随后完成远端 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 线上验证结果：内网 `http://127.0.0.1:3000/home` 与公网 `http://47.95.233.109/home` 均返回 `200`，并已确认线上页面包含新的首屏文案片段“你好，开发者。”。
- 2026-05-03 14:45 前后：继续收敛首页主标题，重点解决“字体太大、标题又聚在一起”的问题。
  - 布局结果：将主标题区改为更平铺的单行主视觉思路，取消大标题被左列宽度压缩的状态；说明与能力标签整体下移到标题下方一层承接，避免所有信息挤在同一块。
  - 样式结果：主标题字号进一步下调，行高放松，取消标题区的强制窄宽度；说明区由左侧竖分栏改为下方横向承接，更符合“平铺展开”的目标。
  - 验证结果：执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/home/page.tsx` 均通过。
- 2026-05-03 14:38-14:39：完成首页首屏居中版的 ECS 部署与复验。
  - 样式结果：`/home` 首屏已将平台标识、引导语、主标题、说明文字和能力标签统一改为居中布局，整体重心更稳定。
  - 部署结果：已通过 `tar + scp + 远端解包` 同步到 `/srv/resumer`；同步前执行 PostgreSQL 备份，随后在 ECS 完成 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 线上验证结果：`systemctl is-active resumer` 返回 `active`；内网 `http://127.0.0.1:3000/home` 与公网 `http://47.95.233.109/home` 均返回 `200`，公网页面已确认包含当前主标题文案。
- 2026-05-03 14:25-14:27：完成首页主标题最新收口版的 ECS 上线与复验。
  - 部署结果：使用 `tar + scp + 远端解包` 同步当前代码到 `/srv/resumer`；上线前执行 PostgreSQL 备份脚本 `./scripts/ops/backup-postgres.sh`，随后在 ECS 上完成 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 线上结果：`systemctl is-active resumer` 返回 `active`；内网 `http://127.0.0.1:3000/home` 与公网 `http://47.95.233.109/home` 均返回 `200`。
  - 页面结果：公网首页已确认包含当前主标题文案“这是把学习、训练和模拟面试放在一起的准备平台。”，说明本轮标题压低与平铺调整已上线。
- 2026-05-03 14:52-14:54：完成管理员账号兜底补齐，新增可重复执行的最小脚本并在 ECS 复验通过。
  - 脚本结果：新增 `scripts/ensure-admin-user.js`，仅处理 `admin@163.com` 的存在性、密码、角色与状态，优先加载 `.env.local`/`.env`，仅在账号缺失或字段不满足要求时创建或更新，避免改动无关用户数据。
  - 环境结果：本地仓库 `.env` 仍为 `file:./dev.db`，不满足当前 Prisma PostgreSQL schema 要求；脚本在本地已按预期快速失败并给出明确提示，避免误连错误环境。
  - 远端执行结果：已将脚本同步到 `/srv/resumer/scripts/ensure-admin-user.js` 并在 ECS `47.95.233.109` 的 `/srv/resumer` 下执行，首次执行返回 `created`，成功创建管理员 `admin@163.com`，状态为 `ACTIVE`、角色为 `ADMIN`。
  - 幂等验证结果：第二次执行同一脚本返回 `unchanged`；随后通过 Prisma + `bcryptjs.compare` 只读复核，确认 `admin@163.com / yy1741..` 密码匹配为 `true`，账号已满足登录所需的管理员权限条件。
- 2026-05-03 14:55 前后：完成顶部 admin 入口最小增量调整，将会员管理入口口径统一到 `/admin/users`。
  - 导航结果：`src/components/layout/Header.tsx` 中仅管理员可见的顶部入口已由“用户管理”调整为“会员管理”，目标地址保持 `/admin/users`，普通用户与未登录用户继续不可见。
  - 文案结果：`src/app/dashboard/page.tsx` 中同页辅助按钮同步由“进入用户管理”调整为“进入会员管理”，避免与顶部入口口径不一致。
  - 鉴权结果：保留现有 `src/app/admin/layout.tsx` 页面级重定向与 `src/lib/permissions.ts` 接口级 `requireAdminUser()` 鉴权逻辑，不改动服务端权限链路。
- 2026-05-03 15:18-15:20：完成 admin 会员管理入口需求的 ECS 部署与最终验收。
  - 部署结果：已通过 `tar + scp + 远端解包` 将当前代码同步到 `/srv/resumer`；同步前执行 PostgreSQL 备份，随后在 ECS 完成 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 线上结果：`systemctl is-active resumer` 返回 `active`；内网 `http://127.0.0.1:3000/home` 返回 `200`；未登录访问 `http://127.0.0.1:3000/admin/users` 返回重定向到 `/dashboard`，说明后台页面保护仍生效。
  - 测试账号结果：ECS 上已确认存在管理员账号 `admin@163.com`，角色为 `ADMIN`、状态为 `ACTIVE`，密码 `yy1741..` 已通过幂等脚本与会话验证复核。
  - 验收结果：`.trae/specs/expose-admin-membership-menu/tasks.md` 与 `checklist.md` 已全部勾选完成，当前可直接使用 `admin@163.com / yy1741..` 登录测试顶部“会员管理”入口。
- 2026-05-03 15:55 前后：完成发起面试链路去假数据与确认页收口，仅调整 `setup/profile/interview/api/parse/api/chat` 相关文件。
  - setup 结果：`src/app/setup/page.tsx` 已将简历改为必传，移除前端文件上传时拼接的 mock 文本；上传提示同步收口为真实支持格式（PDF / Markdown / TXT），当简历缺失或解析失败时会直接展示明确错误，不再静默继续。
  - parse 结果：`src/app/api/parse/route.ts` 已移除“解析失败返回默认职位/默认技能/默认项目”的假数据兜底，改为仅基于真实简历内容返回结构化结果；新增 `resumeSummaryMarkdown`、`persona` 与 `missingDataHints` 字段，关键信息不足时明确提示且不造数据。
  - 确认页结果：`src/app/profile/page.tsx` 已改为以“简短 Markdown 简历摘要 + 用户画像”为主的确认视图，继续沿用既有卡片化 UI 基调；若岗位、摘要或画像缺失，则前置告警并禁用“进入面试”按钮。
  - 面试上下文结果：`src/app/interview/page.tsx` 与 `src/app/api/chat/route.ts` 已移除默认岗位文案和 prompt 兜底，缺少已确认岗位时会明确提示返回补充，避免进入房间后继续使用假职位。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/setup/page.tsx src/app/profile/page.tsx src/app/interview/page.tsx src/app/api/parse/route.ts src/app/api/chat/route.ts` 与 `npm run build` 全部通过。
- 2026-05-03 16:40 前后：完成实时面试模式合并、会议式房间重构、统一题量/时长策略、离开确认与 prompt 组装收口，仅调整面试主链路相关文件。
  - 模式与房间结果：`src/app/setup/page.tsx` 已将 `voice/video` 收敛为统一的 `realtime` 模式，名称口径为“实时面试（音频优先）/ 实时面试（音频 + 视频）”；`src/app/interview/page.tsx` 已将实时房间重构为会议式体验，局部使用 `brand-guidelines` 的深色、浅底、橙/蓝/绿点缀，未改动站内其他页面 UI。
  - 配置策略结果：`src/lib/interview/config.ts` 新增统一模式、题量、时长与历史快照契约；`setup`、`profile`、`practice` 三处已统一接入题量/时长配置，支持“不限”；有限上限会自动结束并进入报告，不限时不会自动结束。
  - 结束与留存结果：`src/components/interview/InterviewExitGuardProvider.tsx`、`src/components/layout/Header.tsx` 与 `src/app/interview/page.tsx` 已补齐“点击其他菜单 / 刷新 / 关闭页面”结束确认；面试会话改为进入房间后即真实创建，消息逐条落库，确认离开后会真实结束并记录一次面试，不再等到报告生成时才建会话。
  - prompt 结果：`src/lib/interview/prompt.ts` 与 `src/app/api/chat/route.ts` 已建立统一 prompt 组装机制，确保模式、简历解析结果、用户画像、专项训练重点、题量/时长策略、轮次信息与知识检索结果真实进入 prompt；专项训练模式已强制围绕 `topic/desc/focus` 连续追问，修复“训练重点未真实影响提问方向”的问题。
  - 报告与复盘结果：`src/app/api/reports/generate/route.ts` 改为优先复用现有 `sessionId` 生成报告，避免提前建会话后再重复创建；`src/app/report/page.tsx`、`src/app/review/page.tsx`、`src/app/api/sessions/recent/route.ts` 已对齐 `realtime/targeted/text` 新模式口径，并删除复盘页未登录态 demo/mock 卡片。
  - 验证结果：本地执行 `node node_modules/typescript/lib/tsc.js --noEmit` 通过；聚焦本轮改动文件的 `node node_modules/eslint/bin/eslint.js ... --max-warnings=0` 通过；`npm run build` 通过。补充说明：仓库内 `npx tsc` 与 `npx eslint` 二进制入口仍有既有损坏问题，本轮改用底层入口完成验证；全量 ESLint 仍存在仓库既有 33 个 error、11 个 warning，主要集中在旧脚本、既有 `api/*` 与学习后台文件，不是本轮新增问题。
  - 当前风险：实时模式的浏览器刷新/关闭页面结束依赖 `beforeunload + keepalive`，在极端网络或浏览器策略下仍存在最后一个结束请求丢失的概率；浏览器返回键未单独做自定义拦截，当前重点覆盖的是顶部菜单跳转与页面离开场景。
- 2026-05-03 17:05 前后：完成发起面试链路最后阻断项修复，去掉专项训练默认岗位造数并把离开守卫范围收回到面试页内。
  - 专项训练结果：新增 `src/app/api/interview/resolve-targeted-role/route.ts`，改为由 DeepSeek 仅基于用户原话推断专项训练的目标岗位、主题、训练目标与重点；`src/app/practice/page.tsx` 不再本地硬编码“后端/前端/开发工程师”等兜底岗位，若信息不足会直接提示用户补充更明确的岗位信息。
  - 数据真实性结果：专项训练开始前会先完成真实岗位识别，再将 `role/topic/desc/focus/questionLimit/durationLimitMinutes` 写入 `parsedProfileData`；若岗位识别失败，不再写入任何默认岗位到 prompt 或面试房间上下文。
  - 范围收紧结果：`src/components/providers/AuthProvider.tsx` 已移除全站 `InterviewExitGuardProvider` 包裹；`src/components/layout/Header.tsx` 已恢复普通导航跳转，不再感知面试守卫；`src/app/interview/page.tsx` 改为仅在当前面试页内拦截顶部菜单点击与页面刷新/关闭，并展示“是否结束本次面试”确认框，避免继续影响其他历史页面逻辑。
  - 文档结果：`.trae/specs/iterate-launch-interview-flow/tasks.md` 与 `checklist.md` 已按当前实现全部回填为完成状态。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/practice/page.tsx src/app/interview/page.tsx src/components/layout/Header.tsx src/components/providers/AuthProvider.tsx src/app/api/interview/resolve-targeted-role/route.ts` 与 `npm run build` 全部通过。
- 2026-05-03 16:49-16:50：完成本轮“发起面试链路阻断项修复” ECS 部署与线上复验。
  - 部署结果：已通过 `tar + scp + 远端解包` 将当前代码同步到 `/srv/resumer`；同步前执行 PostgreSQL 备份脚本，生成 `resumer_prod_20260503_164904.dump`；随后在 ECS 执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 服务结果：`systemctl status resumer` 与 `systemctl is-active resumer` 均确认服务处于 `active (running)`；启动日志显示 Next.js 在 `http://127.0.0.1:3000` 正常 ready。
  - 健康检查结果：内网 `http://127.0.0.1:3000/practice`、`http://127.0.0.1:3000/interview`、`http://127.0.0.1:3000/home` 均返回 `200`；公网 `http://47.95.233.109/practice` 返回 `200`，说明本轮专项训练去造数与面试页内离开确认改动已上线可测。
- 2026-05-03 17:25 前后：完成面试限制样式、专项训练不限、房间内摄像头开关与空报告无评分收口，仅调整 `setup/practice/profile/interview/prompt/report` 相关链路。
  - 限制配置结果：`src/lib/interview/config.ts` 新增 `limitType` 与互斥归一化逻辑，发起面试链路改为“数量 / 时长”二选一；数量仅保留少量固定枚举，时长保留少量固定枚举并支持自定义分钟数，`buildInterviewLimitStrategy()` 同步改为只输出单一生效策略。
  - 专项训练结果：`src/app/practice/page.tsx` 已删除题量 / 时长配置区，进入专项训练时固定写入 `limitType: none` 与空上限，页面明确提示“默认不限时、不限题量，直到你主动结束”。
  - 发起面试结果：`src/app/setup/page.tsx` 已移除进入房间前的静态视频开关，并将自动结束方式改成先选类别再选枚举的两级交互；`src/app/profile/page.tsx` 的策略摘要同步改为单一策略展示，实时模式提示改为“进入房间后可随时开启或关闭视频”。
  - 房间与 prompt 结果：`src/app/interview/page.tsx` 已新增房间内摄像头 icon 按钮，可在实时面试过程中随时开启或关闭本地视频预览；`src/lib/interview/prompt.ts` 已强约束专项训练开始后禁止再次确认主题或训练目标，开场语改为直接进入训练问题。
  - 报告结果：`src/app/api/reports/generate/route.ts` 已新增有效问答判断，若没有形成有效面试内容则直接返回 `noEffectiveInterview`，不再请求模型生成评分；`src/app/report/page.tsx` 已新增“本次不生成评分与正式报告”的空态页面，避免空面试出现伪造分数。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/lib/interview/config.ts src/app/setup/page.tsx src/app/practice/page.tsx src/app/profile/page.tsx src/app/interview/page.tsx src/lib/interview/prompt.ts src/app/api/reports/generate/route.ts src/app/report/page.tsx` 与 `npm run build` 全部通过。
- 2026-05-03 17:16-17:18：完成本轮“面试限制样式与空报告收口” ECS 部署与线上复验。
  - 部署结果：已通过 `tar + scp + 远端解包` 将当前代码同步到 `/srv/resumer`；同步前执行 PostgreSQL 备份脚本，生成 `resumer_prod_20260503_171651.dump`；随后在 ECS 执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 服务结果：`systemctl is-active resumer` 返回 `active`；`systemctl status resumer` 显示 Next.js `16.2.4` 已在 `http://127.0.0.1:3000` 正常 ready。
  - 健康检查结果：内网 `http://127.0.0.1:3000/setup`、`/practice`、`/interview`、`/report` 均返回 `200`；公网 `http://47.95.233.109/setup` 与 `http://47.95.233.109/practice` 返回 `200`，说明本轮“专项训练不限、二选一限制、房间内摄像头开关、空报告不打分”已上线可测。
- 2026-05-03 17:22-17:23：按最新反馈删除专项训练页输入框下方的“不限时 / 不限题量”提示卡片，并重新部署 ECS。
  - 页面结果：`src/app/practice/page.tsx` 已删除输入框下方的说明卡片，仅保留标题、输入框、开始按钮与错误提示，避免页面视觉上多出一块无必要说明。
  - 本地验证结果：执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/practice/page.tsx` 与 `npm run build` 全部通过。
  - 部署结果：已通过 `tar + scp + 远端解包` 同步到 `/srv/resumer`；同步前执行 PostgreSQL 备份脚本，生成 `resumer_prod_20260503_172216.dump`；随后在 ECS 执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 健康检查结果：`systemctl is-active resumer` 返回 `active`；内网 `http://127.0.0.1:3000/practice` 与公网 `http://47.95.233.109/practice` 均返回 `200`，说明本轮页面精简已上线可测。
- 2026-05-03 17:26-17:28：按最新反馈增强“未上传简历”场景的上传框警示态，并重新部署 ECS。
  - 页面结果：`src/app/setup/page.tsx` 在“请先上传简历文件，或直接粘贴真实简历内容后再开始解析”场景下，上传框与下方简历文本框会同步进入克制的浅红警示态，边框、底色和辅助说明会更醒目，但未做夸张视觉强化。
  - 本地验证结果：执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/setup/page.tsx` 与 `npm run build` 全部通过。
  - 部署结果：已通过 `tar + scp + 远端解包` 同步到 `/srv/resumer`；同步前执行 PostgreSQL 备份脚本，生成 `resumer_prod_20260503_172658.dump`；随后在 ECS 执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 健康检查结果：`systemctl is-active resumer` 返回 `active`；内网 `http://127.0.0.1:3000/setup` 与公网 `http://47.95.233.109/setup` 均返回 `200`，说明本轮上传框提示增强已上线可测。
- 2026-05-03 18:05 前后：完成 `strengthen-interview-prompt-intelligence` 的任务 1-4，重构 prompt 架构、首题策略、上下文工程与最小质量拦截。
  - Prompt 结果：`src/lib/interview/prompt.ts` 已新增模式化提问策略、持续上下文锚点、最近对话摘录、阶段描述、检索边界说明与题目质量自检规则；专项训练、文字面试、实时面试三种模式不再沿用同一套泛化面试口径。
  - 首题结果：`buildInterviewOpening()` 已移除“先讲原理/边界/经验”的固定弱题模板，改为按岗位、主题、目标、项目和风险信号选择项目切入题、场景决策题、原理落地题、故障排查题或设计取舍题。
  - 追问与质量结果：`src/app/api/chat/route.ts` 已改为先生成面试官回复草稿，再通过本地质量检查拦截过空、过泛、与岗位脱节或未承接上一轮回答的回复；未通过时会基于修复指令重写一次。检索结果继续作为辅助资料，但不再替代面试题设计职责。
  - 文档结果：`.trae/specs/strengthen-interview-prompt-intelligence/tasks.md` 已完成 Task 1-4 勾选回填。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/lib/interview/prompt.ts src/app/api/chat/route.ts` 与 `npm run build` 全部通过。
- 2026-05-03 17:55-18:10：完成 `strengthen-interview-prompt-intelligence` 的 ECS 运行态部署与 Task 5 / checklist 验证。
  - 部署结果：先发现 ECS `/srv/resumer` 仍运行旧版 `prompt.ts`，首题仍会落回“讲原理/自我介绍”的弱题模板；随后按最小风险流程先执行 `/srv/resumer/scripts/ops/backup-postgres.sh`，生成 `resumer_prod_20260503_175523.dump`，再通过 `tar + scp + 远端解包` 同步当前代码到 `/srv/resumer`，并在 ECS 依次执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`。
  - 健康检查结果：`systemctl is-active resumer` 返回 `active`；内网 `http://127.0.0.1:3000/home` 与 `http://127.0.0.1:3000/practice` 返回 `200`；重新读取 ECS 运行态 `buildInterviewOpening()` 后，专项训练首题已切换为围绕 `Java 后端开发 / JVM 内存模型与 GC 调优` 的真实排障题。
  - Task 5.1 / 5.2 结果：使用现有正式用户 `yangyudei163@163.com / yy1741..` 在 ECS 内网完成真实登录，会话接口返回正常；专项训练样本中，首题已要求按“信号、缩小范围、验证根因、修复复盘”回答，随后两轮 `/api/chat` 继续围绕 `promotion failed`、`Concurrent Mode Failure`、GC 日志字段、`traceId` 聚合缓存 Map、对象引用链与动态验证手段追问，未漂移为泛化综合面试。
  - Task 5.3 结果：正式文字面试样本继续围绕订单履约、库存预占、消息表结构、幂等消费、库存释放失败后的补偿与回滚机制追问，未出现“专项训练”“训练目标”一类高聚焦训练口径污染正式面试体验。
  - checklist 补充结果：在 ECS 运行态复核 `buildInterviewSystemPrompt()` 与 `inspectInterviewReplyQuality()`，确认模式分层说明、上下文锚点与“检索资料不能替代出题职责”的边界文案已真实进入系统 prompt；弱题样例“请先用自己的话完整讲清这个主题里最核心的原理、使用边界或实战经验”会被质量检查拦截并返回 4 条问题说明。
  - 文档结果：`.trae/specs/strengthen-interview-prompt-intelligence/tasks.md` 与 `checklist.md` 已全部勾选完成。
- 2026-05-03 18:20 前后：完成用户画像页与面试页的结构收口，修正解析阶段阻断时机、画像页信息结构与面试页文本样式。
  - 解析链路结果：`src/app/api/parse/route.ts` 已把必要信息收缩到“目标岗位、技术基础、项目/经历线索”三类；`JD 正文`、`GPA/排名`、`离职原因/动机` 不再作为阻断项。接口新增 `resumeImprovements`，用于单独展示“简历可优化点”，并在解析阶段直接返回不足提示，不再把阻断放到画像页。
  - setup 结果：`src/app/setup/page.tsx` 已在解析成功后把 `resumeSummaryMarkdown` 直接回填到上传区下方的大文本框；从画像页返回时，如果文本框为空，会自动带回最近一次摘要，方便用户继续修改。
  - profile 结果：`src/app/profile/page.tsx` 已移除“简历摘要 / 用户画像 / 高优可追问项目”的混乱拼盘，主体只保留“岗位匹配提醒和优化建议”“简历可优化点”“本场策略”三块，同时继续保留顶部岗位编辑、底部训练重点输入和“返回修改 / 确认并进入面试”按钮。
  - interview 结果：`src/app/interview/page.tsx` 已将文本模式与实时模式中的“AI”称呼统一收口为“面试官”；文字气泡增加更清晰的角色标签，压缩气泡留白与底部按钮尺寸，并在 AI 最新消息上增加更轻的流式光标感；`src/app/globals.css` 新增 `interview-markdown` 样式，统一段落、列表和标题节奏，避免 AI 输出文本散乱。
- 2026-05-03 18:46 前后：按最新用户反馈继续修复解析误拦截与文字面试流式体验，仅调整 `src/app/api/parse/route.ts` 与 `src/app/interview/page.tsx`。
  - 解析结果：`src/app/api/parse/route.ts` 已移除“缺少明确目标岗位”硬阻断及“返回上一步”错误文案，不再因为 `missingDataHints` 中存在补充提醒就直接返回 `422`；当前仅在缺少技术基础线索或缺少可追问项目经历时阻断进入下一步。
  - 提示策略结果：解析 Prompt 已明确约束 `missingDataHints` 不得再返回年龄、居住地、GPA、排名、离职原因、求职动机等不影响技术判断的信息，避免继续用非必要字段卡住画像生成。
  - 文字面试结果：`src/app/interview/page.tsx` 已新增底部滚动锚点与逐帧追底逻辑，AI 流式回答过程中会持续自动滚动到最新内容；面试官头像改为固定比例圆形图标，避免在对话区被挤压成扁形。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/api/parse/route.ts src/app/interview/page.tsx` 与 `npm run build` 全部通过。
- 2026-05-03 18:57 前后：继续按最新反馈修复画像页岗位自动回填与文字/专项训练真流式输出，仅调整 `src/app/profile/page.tsx`、`src/app/api/chat/route.ts` 与 `src/app/interview/page.tsx`。
  - 画像页结果：`src/app/profile/page.tsx` 在解析结果未直接给出岗位时，会基于真实简历摘要、技术优势和项目经历自动调用现有 AI 岗位识别接口补全岗位，并保留用户手动修改能力；页面同步增加识别状态提示，明确“已自动识别，可继续修改”。
  - 聊天接口结果：`src/app/api/chat/route.ts` 已从“整段生成后一次性返回”改为直接转发 DeepSeek 的增量 token 流，`__STATUS_SEARCHING__` 与 `__STATUS_GENERATING__` 仍保留给前端驱动“检索中 / 思考中”状态，不再阻塞到整段答案生成完才显示正文。
  - 面试页结果：`src/app/interview/page.tsx` 已补充流式分片解析逻辑，即使状态标记和正文出现在同一个分片内，也会正确区分“思考中”和“流式回答”，避免控制指令把首段正文吞掉。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/profile/page.tsx src/app/api/chat/route.ts src/app/interview/page.tsx` 与 `npm run build` 全部通过。
- 2026-05-03 19:20 前后：开始实现 `rebuild-realtime-interview-room`，集中重建实时面试房间与媒体链路，仅调整 `src/app/interview/page.tsx`。
  - 房间结构结果：实时面试主界面已从“主面板 + 文字侧栏”重构为双人会议室布局，包含顶部状态栏、面试官席位、用户席位、底部实时字幕区与会议控制栏；页面主舞台只保留面试官与用户两块核心区域，留白明显增加，不再沿用文字面试的拥挤结构。
  - 设备控制结果：新增麦克风静音/恢复切换，摄像头状态从单纯布尔值改为更明确的 `off / requesting / on / error` 状态；摄像头开启、关闭与本地预览仍复用同一房间流，并新增安全上下文检测，在非 HTTPS 场景下直接给出“浏览器不会开放设备权限”的中文提示。
  - 播放与字幕结果：AI 语音播放已从基于 `Audio` 标签的对象 URL 播放改为通过 `AudioContext` 解码后顺序播放，减少浏览器自动播放策略导致的“有回答但没有声音”；面试官发言同步改为底部轻量滚动字幕，不再把大段回答堆在房间主区域。
  - 状态表达结果：实时房间已收口为“等待进入会议 / 正在连接设备 / 检索中 / 思考中 / 面试官正在播报 / 正在识别你的回答 / 你的麦克风已静音 / 设备连接异常”等明确状态，并和真实链路绑定。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx` 与 `npm run build` 全部通过；待上线后继续做浏览器实机验证。
- 2026-05-03 19:35 前后：继续收口实时房间字幕一致性，并核对本地部署缓存目录。
  - 字幕结果：`src/app/interview/page.tsx` 已统一面试官与用户两侧实时字幕窗的浮层样式，用户侧不再沿用浅色字幕容器；同时修复用户侧空状态字幕默认文案，避免错误显示“面试官发言会显示在这里”。
  - 语音链路核对结果：当前实时面试仍是“浏览器 `getUserMedia` 采集 + 本地 VAD 分段 + `/api/speech/asr|tts` + 火山引擎语音接口 + 浏览器 `AudioContext` 播放”的半实时方案，尚不是全双工流式语音房间。
  - 本地目录结果：已确认 `/Users/yangyu/Desktop/resumer/tmp` 当前没有残留历史部署包，不再额外占用部署缓存空间。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx` 全部通过。
- 2026-05-03 19:45 前后：按最新反馈修正实时字幕口径，并单独新开实时语音专项 spec。
  - 字幕结果：`src/app/interview/page.tsx` 已按最新要求恢复用户侧浅色字幕框，仅保持与面试官侧字幕框的大小、位置和间距对齐，不再把用户侧改成深色浮层。
  - 规格结果：新增 `.trae/specs/upgrade-realtime-voice-experience/spec.md`、`tasks.md`、`checklist.md`，单独定义“继续使用火山引擎、真流式、可打断、低 AI 味”的实时语音专项方案，避免和既有“会议室房间样式重建”规格混在一起。
  - 前置条件结果：当前已在 spec 中明确域名与 HTTPS 仍是正式实机验收前置条件；在域名申请完成前，可以继续推进代码方案设计、状态机、字幕策略和链路改造，但真实设备最终验收项暂不勾选。
- 2026-05-03 19:55 前后：按最新反馈继续强化实时语音专项规格，将“豆包一比一还原”与 `DeepSeek v4` 大脑约束写入正式目标。
  - 目标结果：`.trae/specs/upgrade-realtime-voice-experience/spec.md` 已将“豆包语音通话体验一比一还原”提升为实时语音交互层的唯一对齐目标，不再只是“参考豆包风格”。
  - 架构结果：spec 已明确采用“`DeepSeek v4` 负责面试官大脑 + 火山引擎负责语音层 + 实时状态机负责豆包式交互编排”的技术方案，避免后续实现中误把底层问答模型切到豆包。
  - 任务结果：`tasks.md` 与 `checklist.md` 已同步加入豆包对齐基准、`DeepSeek v4` 固定约束、豆包式首字节奏/接话感/打断体感/字幕推进等专项任务与验收项，后续实现将按这条新目标推进。
- 2026-05-03 20:10 前后：开始执行实时语音专项前两项设计任务，完成当前链路盘点与技术方案落文档。
  - 盘点结果：已完成 `src/app/interview/page.tsx`、`src/app/api/chat/route.ts`、`src/app/api/speech/asr/route.ts`、`src/app/api/speech/tts/route.ts`、`src/lib/speech/volc.ts` 的实时语音链路盘点，确认当前仍是“浏览器采集 + 本地 VAD 分段 + HTTP ASR/TTS + DeepSeek 文本流 + 前端句子级 TTS 队列”的半实时方案。
  - 方案结果：新增 `.trae/documents/实时语音豆包对齐技术方案.md`，系统定义了豆包对齐基准、实时状态机、客户端/服务端事件协议、`DeepSeek v4 -> 文本 segment -> 火山语音层 -> 客户端播放/字幕` 解耦方案，以及域名就绪前后两阶段实施路径。
  - 任务回填结果：`.trae/specs/upgrade-realtime-voice-experience/tasks.md` 已将 Task 1 和 Task 2 全部标记完成；`checklist.md` 中关于豆包目标、`DeepSeek v4` 约束、火山统一链路、状态机、技术方案与 HTTPS 前置条件的设计项已同步勾选。
- 2026-05-03 20:25 前后：继续按实时语音专项计划推进代码实现，优先完成用户增量字幕和更细粒度 AI 播报的基础能力。
  - 用户侧结果：`src/app/interview/page.tsx` 已新增 `partialUserTranscript` 状态和 `requestPartialUserTranscript()` 临时识别逻辑，在当前发言仍未结束时会基于累计 PCM 定时请求一版临时识别结果，先显示到用户侧实时字幕窗，再在最终识别完成后切回正式消息落库流程。
  - AI 播报结果：同文件新增 `findSpeakableSegmentBoundary()` 与 `extractSpeakableTextSegments()`，将 `DeepSeek v4` 的增量文本从“等完整句才播”收口为更短意群优先播报；首段会更早进入 TTS 队列，继续向“豆包式接话感”靠近。
  - 打断结构结果：新增 `assistantTurnIdRef` 作为当前 AI 发言轮次标识，打断后会让旧轮次文本不再继续入队或落最终结果，为后续会话级 barge-in 改造减少返工。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx` 全部通过；实时语音专项 `tasks.md` 中 SubTask 3.2、3.4、4.1 与 `checklist.md` 中对应基础项已按实际完成情况回填。
- 2026-05-03 20:40 前后：继续加固会话级打断链路，重点防止旧轮次语音和旧轮次文本在中断后继续漏出。
  - 打断结果：`src/app/interview/page.tsx` 已将 TTS 队列升级为带 `turnId` 的结构，并把 `queueTts()`、`playNextAudio()`、`interruptAi()`、`stopRealtimeMedia()` 与 `assistantTurnIdRef` 统一绑定；当前一旦发生打断或进入新轮次，旧轮次的音频解码、播放回调和异步 TTS 返回都不会再继续入队或续播。
  - 流式结果：`handleSendMessage()` 中的 `DeepSeek v4` 流式回复也继续与 `assistantTurnIdRef` 对齐，旧轮次在被新轮次抢占后不会再补落最终 AI 文本，避免出现“声音停了但旧回答最终还是写回消息”的错位。
  - 任务结果：实时语音专项 `tasks.md` 中 SubTask 5.1 与 5.2 已按当前代码落地情况回填为完成；`checklist.md` 中“插话时当前播报会立即停止，后续音频队列不会继续播放”已同步勾选，其他仍依赖上下文承接与实机联调的项继续保留未完成状态。
  - 验证结果：再次执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx`，结果全部通过。
- 2026-05-03 20:55 前后：继续实现打断后的上下文承接，避免下一轮追问丢失上一轮已说出的语义。
  - 数据结构结果：`src/lib/interview/config.ts` 已为画像状态增加 `realtimeInterruptionContext`，用于显式记录实时语音场景下“面试官上一轮说到哪里被打断”。
  - 前端承接结果：`src/app/interview/page.tsx` 已新增 `assistantStreamingPreviewRef`、`storeRealtimeInterruptionContext()` 与 `clearRealtimeInterruptionContext()`；当 AI 被用户打断时，会记录当前已生成的面试官文本片段并写回房间画像；下一轮用户消息发出时，接口请求会优先携带最新缓存画像，避免被 React 状态刷新节奏影响；当新的 AI 轮次完整结束后，再清空这段打断上下文。
  - Prompt 结果：`src/lib/interview/prompt.ts` 已将“实时打断上下文”写入系统 prompt，明确要求模型先承接上一轮被打断的语义，再回应候选人最新发言，避免机械重复或完全忘掉刚才正在推进的问题。
  - 任务结果：实时语音专项 `tasks.md` 中 SubTask 5.3 与 `checklist.md` 中“用户打断 AI 后，系统不会丢失当前会话语境，下一轮追问仍能承接上下文”已按当前代码落地情况回填为完成。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx src/lib/interview/prompt.ts src/lib/interview/config.ts`，结果全部通过。
- 2026-05-03 21:10 前后：继续收口豆包式打断体感，重点处理字幕冻结、停播反馈和播放状态同步。
  - 字幕结果：`src/app/interview/page.tsx` 已新增 `assistantCaptionText` 与 `assistantCaptionCommittedRef`，面试官实时字幕不再单纯跟随完整消息全文，而是优先显示“已经进入播报链路的内容”；被打断时字幕会停在当前已播到的位置，不再继续追着未播完的全文增长。
  - 状态结果：同文件新增 `assistantSpeechStatus`，并把 `queueTts()`、`playNextAudio()`、`interruptAi()`、`handleSendMessage()` 接入统一状态切换；当前能显式区分 `thinking / speaking / interrupted / idle`，房间顶部状态和面试官卡片角标会跟随真实播放链路变化，不再只依赖 `isPlaying` 与 `isThinking` 两个粗状态。
  - 播放结果：TTS 队列开始播放某一段时，会同步推进当前面试官字幕；播放结束后再提交为已播报片段，从而让字幕、播报和打断反馈保持同一节奏。
  - 任务结果：实时语音专项 `tasks.md` 中 SubTask 4.2 已按当前代码落地情况回填为完成；更高要求的“低 AI 味主音色调优”和“豆包体感最终收口”仍保留未完成状态。
  - 验证结果：本地再次执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx`，结果全部通过。
- 2026-05-03 21:25 前后：继续推进火山语音参数与口语化播报策略，优先收口“低 AI 味”基础配置。
  - 参数结果：`src/lib/speech/volc.ts` 已扩展 TTS 配置项，支持 `voiceType / speedRatio / pitchRatio / volumeRatio`，并引入 `VOLC_TTS_VOICE_TYPE`、`VOLC_TTS_SPEED_RATIO`、`VOLC_TTS_PITCH_RATIO`、`VOLC_TTS_VOLUME_RATIO` 作为统一默认值；`src/app/api/speech/tts/route.ts` 已同步透传这些参数。
  - 播报结果：`src/app/interview/page.tsx` 已新增 `sanitizeTtsTextForConversation()` 与 `buildRealtimeTtsOptions()`，会先把 Markdown/条目化文本整理成更适合口语播报的短句，再按“首段更强调接话感、后续段落更稳”的策略给火山 TTS 传入不同的轻量参数。
  - 节奏结果：实时语音的首段和后续段落现在会带片段序号进入 `queueTts()`，从而让起句速度、音高和音量策略与片段位置绑定，不再是所有段落都用同一组固定参数。
  - 任务结果：实时语音专项 `tasks.md` 中 SubTask 4.3 已按当前代码落地情况回填为完成；需要真实听感验证的“低 AI 味已明显达成”和“豆包式接话体感最终收口”仍保留未完成状态。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx src/app/api/speech/tts/route.ts src/lib/speech/volc.ts`，结果全部通过。
- 2026-05-03 21:35 前后：继续推进“更像豆包接话”的首句起手方式，收口模型口语化约束与前端首段切分。
  - Prompt 结果：`src/lib/interview/prompt.ts` 已进一步强化实时面试模式约束，明确要求模型在实时语音场景下优先用一句很短的承接或回应起手，再迅速进入核心追问，避免第一句就成为大段书面播报稿。
  - 切分结果：`src/app/interview/page.tsx` 已在 `extractSpeakableTextSegments()` 内新增 `findLeadInBoundary()`，让首段优先在更短的软停顿处切开；当前首句会更早进入播报链路，不再默认等到相对完整的长句。
  - 目标结果：这一步主要是继续压缩首句等待感，让实时语音更接近真人“先接话、再展开”的豆包式体感，但由于最终听感仍需真实设备与耳机联调验证，因此 `tasks.md` 中 SubTask 4.4 继续保留未完成状态。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx src/lib/interview/prompt.ts`，结果全部通过。
- 2026-05-03 21:45 前后：继续收口后续意群衔接，进一步压低“宣读稿件”感。
  - 切分结果：`src/app/interview/page.tsx` 的 `findSpeakableSegmentBoundary()` 已补充对“然后 / 另外 / 但是 / 不过 / 所以 / 如果 / 其实 / 接着”等转折锚点的识别，让后续段落更容易在自然转折处切开，而不是拖成长句一起播报。
  - 节奏结果：同文件的 `sanitizeTtsTextForConversation()` 已补充括号与冒号口语化处理，`buildRealtimeTtsOptions()` 也把第二段和更后续段落的语速继续放稳，减少所有意群都像抢着往外吐的感觉。
  - Prompt 结果：`src/lib/interview/prompt.ts` 已继续强调实时面试的后续展开应按意群自然推进，一次只说一个判断或一个追问点，减少长串并列、编号罗列和书面总结句，避免听感像在宣读答案。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx src/lib/interview/prompt.ts`，结果全部通过。
- 2026-05-03 21:55 前后：继续推进更自然的段间停顿与口语化播报清洗，降低“连珠炮”和“书面起手”带来的 AI 味。
  - 停顿结果：`src/app/interview/page.tsx` 已为 TTS 队列片段增加 `pauseAfterMs`，并新增 `buildRealtimeTtsPauseAfterMs()`；当前每段播报结束后，会根据片段位置和句末标点决定一个更自然的短暂停顿，再进入下一段，而不是上一段一结束下一段立刻顶上去。
  - 清洗结果：同文件的 `sanitizeTtsTextForConversation()` 已继续压掉“总体来说 / 总的来说 / 从这个角度来看 / 我想先说一下 / 我先补一句”等更像书面答辩或稿件开场的起手表达，优先保留更像真人接话的开头。
  - 目标结果：这一步继续服务于 `Task 4.4` 的最终目标，即让首句之后的第二、第三段衔接更自然、停顿更像真人而不是播报器；由于最终效果仍需真实设备听感验证，`Task 4.4` 继续保持未完成。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx`，结果全部通过。
- 2026-05-03 22:05 前后：继续收口实时语音里的书面追问腔，只对 prompt 与 TTS 清洗做最小增量修正。
  - 播报结果：`src/app/interview/page.tsx` 的 `sanitizeTtsTextForConversation()` 已新增对“下面我想进一步追问一下 / 接下来我想追问一下 / 请你系统地阐述 / 请从多个维度分析 / 请详细阐述”等固定书面模板的替换或压缩，优先改写成“那我接着问一个更具体的 / 你具体说说 / 你按你的思路说说”这类更接近真人接话的口气。
  - 节奏结果：同文件的 `buildRealtimeTtsOptions()` 已把首段、第二段和后续段的语速进一步放稳，并为“好 / 行 / 那 / 你刚才 / 如果按你”这类短承接首段单独使用更克制的参数；`buildRealtimeTtsPauseAfterMs()` 也为短 lead-in 片段增加了更像真人接话后的轻停顿。
  - Prompt 结果：`src/lib/interview/prompt.ts` 已进一步明确实时面试应优先使用“那你刚才提到…… / 如果按你这个思路…… / 具体到这一步……”等短追问句式，并把“下面我想进一步追问一下 / 请你系统地阐述 / 请从多个维度分析”等书面模板纳入实时模式的质量拦截。
  - 验证结果：本地执行 `node ./node_modules/typescript/bin/tsc --noEmit`、`node ./node_modules/eslint/bin/eslint.js src/app/interview/page.tsx src/lib/interview/prompt.ts` 与 `npm run build`，结果全部通过。
- 2026-05-03 22:20 前后：完成本地火山语音链路实测，确认当前已具备部分真实连通性，但 ASR 仍未达到“域名就绪后可直接上线”的标准。
  - 环境结果：本地 `.env.local` 已存在 `VOLC_APP_ID`、`VOLC_ACCESS_TOKEN`、`VOLC_SECRET_KEY`、`VOLC_ASR_CLUSTER`、`VOLC_TTS_CLUSTER`，说明当前仓库并非完全未接入火山。
  - TTS 结果：复用本地 `next dev` 后，直接请求 `/api/speech/tts` 返回 `200`，`Content-Type: audio/mpeg`，生成测试音频约 `98880` 字节，说明当前火山 TTS HTTP 链路已真实打通。
  - ASR 结果：使用 macOS `say` 生成中文测试语音、转为 `16k/16bit/mono` PCM 后调用 `/api/speech/asr`，接口返回 `500`，当前统一错误为“语音服务暂时不可用，请稍后再试”；结合官方文档复核，现有 `src/lib/speech/volc.ts` 中使用的 `https://openspeech.bytedance.com/api/v2/asr` HTTP 方案并不是后续“真流式、可打断”目标态的最终正确接法，后续应继续升级到火山官方 WebSocket 流式 ASR 链路。
  - 风险结论：当前可以确认“火山可访问、TTS 可用”，但不能宣称“整条实时语音链路已可上线”；在域名与 HTTPS 就绪前，仍需完成 ASR 正式链路切换、火山资源口径统一和实机联调。
- 2026-05-03 22:45 前后：已将语音识别底层从旧 HTTP 方案切到火山官方 WebSocket 协议，并完成本地真实联调。
  - 代码结果：`src/lib/speech/volc.ts` 已重构 `recognizeSpeech()`，优先走火山官方 WebSocket 二进制协议；当前支持两种模式：`legacy`（`wss://openspeech.bytedance.com/api/v2/asr`）与 `bigmodel`（`wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_async`）。现阶段为保证最小风险，若缺少 `VOLC_ASR_RESOURCE_ID` 或 `VOLC_API_KEY`，会自动回退到 `legacy` 模式继续可用。
  - 配置结果：已为后续正式切到大模型流式 ASR 预留环境变量：`VOLC_API_KEY`、`VOLC_ASR_RESOURCE_ID`、`VOLC_ASR_WS_MODE`、`VOLC_ASR_MODEL_NAME`、`VOLC_ASR_ENABLE_NONSTREAM`、`VOLC_ASR_END_WINDOW_SIZE`、`VOLC_ASR_FORCE_TO_SPEECH_TIME`。
  - 接口结果：`src/app/api/speech/asr/route.ts` 已同步修正异常处理，统一返回明确错误信息，避免 `any` 类型继续扩散。
  - 本地实测：再次使用 macOS `say` 生成中文测试语音、转为 `16k/16bit/mono` PCM 后调用 `/api/speech/asr`，接口返回 `200`，文本成功识别为“你好，我正在测试火山 web socket 语音识别，请把这句话识别出来。”，说明当前本地 ASR 已真实打通。
  - 当前判断：现在可以严谨地说，域名就绪后并不是“从零开始接火山”，而是直接进入 HTTPS + 浏览器设备权限 + 房间级真流式体验联调阶段；与此同时，后续仍建议在你拿到新版控制台 `API Key + Resource ID` 后，把正式生产配置切到 `bigmodel` 模式，进一步逼近豆包同款实时体验。
- 2026-05-03 23:00 前后：继续收口“新版控制台申请后即可直接切生产配置”的最后准备，补齐自检脚本与新版申请文档。
  - 脚本结果：新增 `scripts/volc-speech-smoke-test.mjs`，可直接串行验证本地 `/api/speech/tts` 与 `/api/speech/asr`；同时在 `package.json` 增加 `npm run test:volc-speech` 命令，后续拿到新版火山参数后可直接复验。
  - 文档结果：新增 `.trae/documents/火山新版控制台申请与配置流程.md`，已按新版控制台口径整理完整步骤，覆盖注册登录、实名认证、一键开通服务、创建 API Key、确认 `Resource ID`、保留旧版兼容参数、确认音色、确认计费与并发、环境变量映射与本地验证方式。
  - 本地验证：执行 `npm run test:volc-speech`，结果 `TTS` 与 `ASR` 均通过；执行 `node ./node_modules/typescript/bin/tsc --noEmit` 与 `npm run build`，结果全部通过。
  - 当前判断：现在你只要按新版控制台把 `API Key + Resource ID + 音色 + 旧版兼容参数` 申请齐，我这边就可以无缝切到 `bigmodel` 正式模式，不需要再回头补火山底层接入。
- 2026-05-03 23:20 前后：按方案 B 继续推进，已把 `TTS` 也收敛到新版火山口径，不再依赖旧版 `APP ID / Token` 才能合成。
  - 代码结果：`src/lib/speech/volc.ts` 的 `synthesizeSpeech()` 已从旧版 `https://openspeech.bytedance.com/api/v1/tts` 切到新版 `https://openspeech.bytedance.com/api/v3/tts/unidirectional`，统一使用 `X-Api-Key + X-Api-Resource-Id` 鉴权；同时默认 `VOLC_TTS_RESOURCE_ID` 为 `seed-tts-2.0`，默认音色改为 `zh_male_m191_uranus_bigtts`，彻底进入 2.0 音色体系。
  - 协议结果：已补齐新版 Chunked 流式回包解析，正确兼容音频片段 `code=0` 与会话结束 `code=20000000/message=OK` 的正常场景，不再把新版返回误判为失败。
  - 本地实测：直接在 Node 中注入新版环境变量 `VOLC_API_KEY + VOLC_TTS_RESOURCE_ID=seed-tts-2.0 + VOLC_TTS_VOICE_TYPE=zh_male_m191_uranus_bigtts` 后调用 `synthesizeSpeech()`，成功返回音频字节数 `64557`，说明新版 `TTS` 已真实打通。
  - 当前结论：现在实时语音主链路已经可以严谨地描述为“`DeepSeek v4` 负责面试官大脑，火山新版负责 `ASR + TTS` 语音层”，后续你只需要继续在新版控制台补齐 `API Key / ASR Resource ID / TTS Resource ID / 2.0 音色`，无需再回头找旧版认证参数。
- 2026-05-03 23:40 前后：按最新决策完成“只替换 TTS 为 Qwen3-TTS”的代码与文档收口，保留火山 `ASR` 与 `DeepSeek v4` 不动。
  - 代码结果：`src/lib/speech/qwen.ts` 已作为唯一 `TTS` 底层实现继续保留，并补充了音色兜底、旧火山音色值兼容保护与输出格式收口；`src/app/api/speech/tts/route.ts` 已统一按当前验证路径返回 `audio/wav`；`scripts/speech-smoke-test.mjs` 已同步改为生成 `.wav` 自检音频。
  - 兼容结果：当前前端 `voiceType` 透传入口保留不动；若前端仍传入旧火山音色值，服务端会自动回退到 `QWEN_TTS_VOICE`，避免因为旧值导致路由直接失败。
  - 文档结果：`.trae/documents/火山新版控制台申请与配置流程.md` 已降级为“仅供火山 ASR 保留时参考”的历史文档；新增 `.trae/documents/Qwen3-TTS申请与配置流程.md` 作为当前主申请文档。
  - 环境结果：当前主方案新增 `DASHSCOPE_API_KEY`、`QWEN_TTS_BASE_URL`、`QWEN_TTS_MODEL`、`QWEN_TTS_VOICE`、`QWEN_TTS_LANGUAGE_TYPE`、`QWEN_TTS_ENABLE_STREAM`、`QWEN_TTS_USE_INSTRUCT`、`QWEN_TTS_INSTRUCTIONS`；火山侧仅保留 `ASR` 相关变量。
  - 当前阻断：运行态完整验收仍取决于有效的北京地域 `DASHSCOPE_API_KEY` 是否注入到实际运行中的开发环境；若缺失会报 `缺失环境变量：DASHSCOPE_API_KEY`，若无效会报 `InvalidApiKey`。
  - 验收方式：后续统一使用 `npm run test:speech` 验证 `TTS(Qwen) + ASR(Volc)` 混合链路，再进入房间做一次人工打断与播报回归。
- 2026-05-03 23:54-23:57：完成 Qwen TTS 的 ECS 部署与“语音识别 -> DeepSeek -> 语音合成”全链路真测，未使用 mock。
  - 部署结果：先在 ECS `47.95.233.109` 上执行 `/srv/resumer/scripts/ops/backup-postgres.sh`，生成 `resumer_prod_20260503_235417.dump`；随后通过 `tar + scp + 远端解包` 将当前代码同步到 `/srv/resumer`，并在远端 `.env.local` 中写入 `DASHSCOPE_API_KEY` 与 `QWEN_TTS_*` 默认配置。
  - 构建结果：在 ECS 依次执行 `npx prisma generate`、`npx prisma migrate deploy`、`npm run build` 与 `systemctl restart resumer`，`systemctl is-active resumer` 返回 `active`。
  - 健康检查结果：内网 `http://127.0.0.1:3000/home` 与 `http://127.0.0.1:3000/api/auth/providers` 均返回 `200`，说明服务重启后站点和认证入口正常。
  - 语音双接口结果：本地带有效 `DASHSCOPE_API_KEY` 启动运行态后，`npm run test:speech` 已真实通过，输出 `TTS(Qwen)` 与 `ASR(Volc)` 双通过。
  - 全链路结果：对 ECS 公网地址执行真实串联测试，步骤为“本地生成中文测试语音 -> `/api/speech/asr` -> 登录态调用 `/api/chat` -> `/api/speech/tts`”；最终 `ASR` 返回中文识别文本，`chat` 返回面试官追问文本，`TTS` 返回 `audio/wav` 且音频字节数 `806444`，说明“火山识别 -> DeepSeek -> Qwen 合成”链路已跑通。
- 2026-05-08 01:40-02:15：按最新要求直接在 ECS 清理学习中心脏数据并重新部署，已完成远端迁移、重种与上线恢复。
  - 数据清理结果：已在 ECS `47.95.233.109:/srv/resumer` 上执行 `npx prisma migrate deploy`，补齐 `20260507_learning_center_v2_demo_architecture`；随后通过远端 `seedLearningCenterV2({ resetExisting: true })` 清空旧学习中心脏数据并重建 V2 标杆数据，当前结果为 `4` 个题库、`8` 个章节、`12` 篇文档、`12` 个版本、`12` 条审核任务、`12` 条 AI 任务。
  - 构建阻断结果：ECS 本机 `next build --webpack` 在 `Creating an optimized production build ...` 阶段持续被 OOM Killer 杀死；即使停掉 `resumer` 并限制 `NODE_OPTIONS=--max-old-space-size=1024`，远端 `node` 进程仍会在约 `1.5GB RSS` 时被系统直接回收。
  - 部署恢复结果：最终改为本地通过 SSH 隧道复用 ECS PostgreSQL 完成 `next build --webpack`，虽然尾部 `Collecting build traces` 仍会触发本机 `ETIMEDOUT`，但 `.next/BUILD_ID`、`prerender-manifest.json`、`routes-manifest.json` 与 `server/` 产物均已完整生成；随后使用精简运行包直传 ECS，远端只执行 `npx prisma generate`，不再进行 OOM 风险构建。
  - 线上验证结果：当前 `systemctl is-active resumer` 返回 `active`；内网复核 `GET http://127.0.0.1:3000/learning` 返回 `200`，`GET /api/learning/banks` 返回 `200` 且已读到新的 4 个学习中心 V2 题库，抽查题目页 `GET /learning/cmovs80h4005hpaebg9d4m7my/category/cmovs80h7005jpaebvr3yk2ea/question/cmovs80h9005lpaebyokbp321` 返回 `200`，题目详情接口同样返回 `200` 并命中新文档内容。
- 2026-05-08 08:50-09:05：根据最新验收反馈继续收口学习页文档壳，修复“文档滚动后顶部模式栏仍然消失”的漏改问题。
  - 根因结果：上一轮只把左侧导航与右侧目录做成了 `sticky`，但中间阅读区真实负责“5分钟速读 / 15分钟深读 / 面试模式”的 `.learning-reader__header` 仍是普通流式布局，因此滚动后自然会离开视口。
  - 代码结果：已在 `src/app/globals.css` 中把 `.learning-reader__header` 补成真正的 `position: sticky`，同时增加 `top: 82px`、更高 `z-index`、半透明白底、`backdrop-filter` 与轻阴影，避免滚动时被正文内容盖住。
  - 部署结果：已沿用“本地通过 SSH 隧道复用 ECS PostgreSQL 构建，再用精简运行包直传 ECS”的稳定链路重新部署；远端继续仅执行 `npx prisma generate` 与 `systemctl restart resumer`，避免再次触发 ECS OOM 构建。
  - 方向结论：最新用户验收结论已确认“页面样式可保留、题库颗粒度基本通过、适合小规模试生产但不适合无门禁全量放大”；下一轮主优先级应从继续扩文档切换到“自测反馈闭环 + 面试训练先答后评 + 深读最低标准门禁”，尤其是 JVM、Java 并发这类抽象专题要补图解和更强教学化表达。
- 2026-05-08 09:05-10:00：按“统一学习评估系统”要求完成最终上线收口，已在 ECS 部署统一评分引擎、自测反馈、面试四状态训练流、深读门禁增强版并完成公网抽查。
  - 架构结果：本轮不再把“自测反馈 / 面试评分 / 掌握度判断 / 薄弱点推荐”拆成多套逻辑，而是统一收口到 `Assessment Engine / 学习评估引擎`。当前 `src/lib/learning/assessmentEngine.ts` 提供统一输入输出协议，`src/lib/learning/assessmentService.ts` 负责把 `self_test / interview / follow_up` 三种模式接到同一套评估、进度同步与会话持久化逻辑；`/api/learning/assessment` 已成为统一学习评估 API。
  - 训练闭环结果：题目页 `QuestionDetailClient.tsx` 已正式接入新的 `SelfTestAssessmentCard` 与 `InterviewTrainingPanel`。当前自测提交后会返回 `score / level / hitPoints / missingPoints / wrongPoints / expressionFeedback / recommendedAnswer / improvedAnswer / whyThisAnswer / nextAction`；面试模式已按“未作答 -> 已评分 -> 改进版答案 -> 继续追问”四状态运行，并在文末保留“去面试”入口跳专项训练页。
  - 内容门禁结果：`content-contract.ts` 已新增 `templateType`、`resolveLearningContentTemplateType()` 与 `validateDeepReadReadiness()`；`v2SeedData.ts` 已为首批标杆文档补齐真实场景、图解、工程落地、常见误区、代码示例与至少 2 道自测，`v2Seeder.ts` 导入时会带着 `deep_read_gate` 一起校验，避免薄内容继续冒充“15 分钟深读”。
  - 部署修复结果：本次远端部署过程中发现 ECS `47.95.233.109:/srv/resumer/.env.local` 的 `DATABASE_URL` 被错误写成了本地 SSH 隧道地址 `postgresql://yangyu@127.0.0.1:54329/resumer_local?schema=public`，会导致远端 `prisma migrate deploy` 与重种命中本地验证库配置。已手工修正为生产库 `postgresql://resumer:resumer123ABC@127.0.0.1:5432/resumer_prod?schema=public` 后继续执行迁移、生成 Prisma Client、重种与服务重启。
  - 远端重种结果：已在 ECS 上再次执行 `prisma migrate deploy`、`prisma generate` 与 `seedLearningCenterV2({ resetExisting: true })`；当前结果稳定为 `4` 个题库、`8` 个章节、`12` 篇文档、`12` 个版本、`12` 条质量报告、`12` 条审核任务、`12` 条 AI 任务。
  - 构建结果：本地已重新执行 `DATABASE_URL='postgresql://resumer:resumer123ABC@127.0.0.1:54329/resumer_prod?schema=public' NODE_DISABLE_COMPILE_CACHE=1 node ./node_modules/next/dist/bin/next build --webpack`，结果 `Compiled successfully`，并确认新增路由 `/api/learning/assessment`、题目页路由和学习中心 API 都已进入正式产物；本机 `tsc` 仍存在长期无输出的环境级不稳定，因此本轮仍以真实生产构建通过作为最终可部署标准。
  - 公网验证结果：公网 `GET http://47.95.233.109/learning` 返回 `200`，`GET /api/learning/banks` 返回 `200` 且当前已是 `MySQL / Redis / Java 并发 / JVM` 四套 V2 标杆题库；题目页 `GET /learning/cmow9h3bw0002padyv3a7a4ol/category/cmow9h3c00004padykb0j3zbx/question/cmow9h3c50006pady8wrhtuzy` 返回 `200`；统一评估接口 `POST /api/learning/assessment` 已真实返回结构化反馈，其中自测样例成功返回 `70 / 100`、`partially_mastered`、命中点、遗漏点、表达建议、推荐答案、改进版答案、为什么这样答与下一步建议，说明“先学 -> 作答 -> 系统指出问题 -> 再训练”的主闭环已在线可测。
- 2026-05-08 21:45-22:00：根据最新验收意见，开始把抽象机制题从“通用模板深读”升级为“专题专属模板深读”，首个落点是 AQS 标杆文档。
  - 问题结论：用户对 `AQS 为什么能成为 Java 并发包的同步器基础？` 的验收意见成立。此前这篇的主要问题不是页面样式，而是 `v2SeedData.ts` 里对“真实场景 / 工程落地 / 应用题 / 代码示例 / 图解”仍走大量通用增强块，导致 AQS 这种抽象专题出现明显模板味，无法作为 Java 并发批量生产样板。
  - 模板升级结果：已在 `src/lib/learning/v2SeedData.ts` 中为 AQS 增加专属增强逻辑，而不是继续沿用通用场景模板。当前 AQS 会单独生成：
    - 专属真实场景：明确说明 AQS 的真实项目价值是“理解 ReentrantLock / Semaphore / CountDownLatch / 线程 dump 排查”，而不是“业务里直接采用 AQS 方案”；
    - 专属流程图：改成“`acquire -> tryAcquire -> 入队 -> park -> unpark -> retry`”完整获取资源流程，而不再只是模糊的 `state + 队列` 示意；
    - 专属代码示例：补 `acquire()` 模板骨架与 `compareAndSetState()` 伪代码，明确 CAS 与模板方法关系，修复之前 AQS 代码示例混入解释文本的问题；
    - 专属工程落地：把重点改成“理解 JUC 工具行为与排查线程阻塞”，不再误写成“上线时采用 AQS 方案”；
    - 专属误区：明确补“ AQS 不是锁 / state 不是固定语义 / 被唤醒不等于拿到锁”。
  - AQS 内容结果：已将 AQS 文档正文从 3 个泛化章节扩展为 8 个专题化章节，新增并强化 `state 作为同步语义载体`、`CAS 修改 state`、`Node + 队列 + park/unpark`、`独占模式 vs 共享模式`、`Condition 与同步队列关系`、`AQS 常见误区`。同时新增 `AQS state 在不同同步器里的含义` 对比表，把 `ReentrantLock / Semaphore / CountDownLatch` 的 state 映射与获取条件放到同一张表里，解决“知道 AQS 但不会解释复用原因”的问题。
  - 自测与面试结果：AQS 当前已不再只有一道泛化自测；现在线上自测包含“为什么 AQS 适合做框架而不是单一一把锁”以及“Semaphore 为什么能限制最多 N 个线程同时访问资源”两道题；面试内容已补到 30 秒 / 2 分钟 / 进阶答案，并把必答点、加分点、进阶点、追问都更新为 AQS 专属表达。
  - 样式收口结果：`src/app/globals.css` 已继续把中间阅读区收成更接近语雀的轻容器风格，主要调整为更轻的边框与阴影、更克制的 sticky 标题栏和更稳定的正文留白，减少卡片感与“AI 页面壳”味道。
  - 部署结果：本地重新执行 `next build --webpack` 通过后，已再次将最新源码与 `.next` 产物直传 ECS，并通过 `node --experimental-strip-types --loader ./scripts/alias-loader.mjs` 在远端重新执行 `seedLearningCenterV2({ resetExisting: true })`。远端重种结果仍为 `4` 个题库、`8` 个章节、`12` 篇文档、`12` 个版本、`12` 条质量报告、`12` 条审核任务、`12` 条 AI 任务。
  - 公网验证结果：公网 `GET /api/learning/banks` 返回 `200`，新一轮 Java 并发题库默认首题路径已变为最新数据；新的 AQS 文档公网路径为 `http://47.95.233.109/learning/cmowzbv2x003ppag3bsj1fwr1/category/cmowzbv31003rpag3wvyqvpfn/question/cmowzbv34003tpag31kjz86wc`，接口抽查已确认正文里出现 `CAS 修改 state`、`独占/共享模式`、`Condition`、`AQS state 映射表`、`Semaphore 应用题` 等新内容，说明本轮 AQS 标杆修正已在线生效。
- 2026-05-08 22:00-22:20：继续根据第二轮验收意见收口“最后一层”，重点补模板残留清理、评分反馈显式化和并发题门禁。
  - 残留清理结果：`src/lib/learning/v2SeedData.ts` 已修掉 AQS 对比表下方的错误高亮文案，不再显示“为什么不是另一个方案”这类错位模板话术；同时在 `buildLearningContent()` 中增加显式判断，若正文里已经手写了“误区/常见坑”章节，则不再额外拼接通用 `buildMistakeSection()`，避免再次出现“前面讲了一次，后面又跟一个空壳常见误区”的重复问题。
  - AQS 细节修正结果：AQS 工程落地表述已从 `WAITING/BLOCKED` 改成更严谨的 `WAITING / TIMED_WAITING / parking`；流程图里的最后一跳也改成更短的 `再次竞争 state`，降低渲染时出现截断成 `stat` 的风险。
  - 并发题门禁结果：`src/lib/learning/content-contract.ts` 已新增 Java 并发抽象题专属检查。凡是标题命中 `AQS / 线程池 / volatile / 并发 / JMM / Condition / Semaphore / CountDownLatch` 的文档，除了已有 15 分钟深读门禁外，还必须额外包含：并发核心机制（如 `state / CAS / 队列 / park/unpark / 可见性`）、具体 JUC 组件映射、线程状态或排查视角，否则 `deep_read_gate` 不通过。
  - 训练闭环显式化结果：`src/app/learning/_components/LearningAssessmentPanels.tsx` 已把反馈结果往前顶。自测现在会先提示“先提交自测，系统会给你得分、命中点、遗漏点和推荐答案”，且在拿到评分前不再鼓励直接看标准答案；面试模式提交主回答后，会先出现 `必答点命中 / 加分点命中 / 进阶点命中` 三个评分摘要，再进入完整评分报告。追问模式提交前也会显示“提交后会返回这一轮追问的得分、遗漏点、表达建议和下一轮追问建议”。
  - 部署与验证结果：本地再次执行 `next build --webpack` 通过后，已重传产物到 ECS 并再次执行远端 `seedLearningCenterV2({ resetExisting: true })`。公网抽查确认 Java 并发题库最新默认首题路径已更新为 `http://47.95.233.109/learning/cmowzn2j0003ppalug17o5saz/category/cmowzn2j3003rpalu534tjawp/question/cmowzn2j5003tpaluvjj8n2be`；题目详情接口里已出现新的 AQS 对比表高亮、`WAITING / TIMED_WAITING / parking` 表述、`再次竞争 state` 流程图节点和新的自测题配置，说明这轮“最后一层”修正已在线生效。
- 2026-05-08 22:20-22:30：根据最新样式反馈，把学习文档阅读页从“三个独立卡片”进一步收成“一个大文档面板里的三栏分区”，并补打印态单列规则。
  - 布局调整结果：`src/app/globals.css` 中的 `learning-reader__shell` 现在承担整页统一容器职责，已增加统一边框、圆角和轻阴影；`learning-reader__layout` 改成无缝三栏，左栏从 `280px` 扩宽到 `340px`，带目录时右栏为 `250px`，中间缝隙改为 `0`，通过左右分割线而不是三个独立卡片来区分章节列表、正文和本文目录。
  - 视觉收口结果：左侧章节列表卡片边框和阴影已移除，右侧目录也不再单独像一块漂浮区域；中间正文主容器的独立边框和阴影已取消，改为和左右栏共用同一外层壳。章节标题允许两行展示，减少“左边明明有空白但标题被截断”的问题；同时把 `learning-rich-panel / summary / sources` 和面试模式顶部问题区、必答点区、页尾 CTA 的卡片感继续压低，减少“到处都是气泡”的视觉噪音。
  - 打印态结果：新增 `@media print` 规则，打印时会隐藏站点顶栏、左侧章节列表、右侧目录、顶部模式切换和交互按钮，并把学习页切回单列主文档，避免浏览器打印时把整页三栏和全站导航拼成多块断裂区域。文档主标题和正文仍保留，互动区块增加 `page-break-inside: avoid`，尽量减少章节被一刀切断。
  - 部署结果：本地再次执行 `next build --webpack` 通过后，已将最新样式产物直传 ECS 并重启 `resumer` 服务；公网学习页 `GET /learning/cmowzn2j0003ppalug17o5saz/category/cmowzn2j3003rpalu534tjawp/question/cmowzn2j5003tpaluvjj8n2be` 返回 `200`，说明最新统一壳层已在线生效。
- 2026-05-08 22:30-22:40：继续按“彻底修复”要求处理标题区压正文和导航可读性问题。
  - 结构修复结果：`QuestionDetailClient.tsx` 里已把学习页头部从“一个整体 sticky 的 header”拆成“上方工具条 `learning-reader__toolbar` 固定 + 下方标题 `learning-reader__header` 正常流式”。这样正文滚动时不会再从标题区下面穿过去，根因不再是靠调间距糊住，而是直接拆掉了原本会遮住正文的 sticky 标题结构。
  - 分隔带结果：`globals.css` 已在 `learning-reader__toolbar` 下新增固定分隔带（`::after` 渐变分隔线），让工具条和正文之间有稳定的视觉隔离，用户滚动时能明确看出“上面是固定工具层，下面是正文层”，不再出现内容贴着标题区的错位感。
  - 对比度修复结果：右侧目录文字已整体提亮到接近黑色，并提升字重；当前激活目录项进一步加粗、加深。左侧章节列表当前激活题也额外提高黑度和字重，并增加轻微内描边，避免当前题目在列表里不够明显。
  - 部署结果：本地再次执行 `next build --webpack` 通过后，已重新部署 ECS；公网学习页仍返回 `200`，说明这轮“标题分层 + 导航对比度”修复已经在线生效。
