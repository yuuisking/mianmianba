---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/01-foundation-migration-guardrails/01-CONTEXT.md"
  - ".planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md"
  - ".planning/phases/01-foundation-migration-guardrails/01-PATTERNS.md"
  - "prisma/schema.prisma"
  - "src/lib/interview-v2/domain.ts"
  - "src/lib/interview-v2/planService.ts"
  - "src/lib/interview/config.ts"
  - "src/app/setup/page.tsx"
  - "src/app/profile/page.tsx"
autonomous: true
requirements:
  - "MODEL-01"
  - "MODEL-02"
  - "MODEL-03"
requirements_addressed:
  - "MODEL-01"
  - "MODEL-02"
  - "MODEL-03"
---

<objective>
先完成 Phase 1 的领域盘点与真源收敛设计，把“长期基础档案 / 目标画像 / 运行时快照”的边界、字段归属、旧字段映射和迁移分级明确下来，作为后续 schema 与主链改造的唯一依据。
</objective>

<must_haves>
<truths>
- D-04: 候选画像采用双层模型：长期基础档案 + 目标画像。
- D-05: 长期基础档案保持最轻，目标岗位、公司、JD、简历与项目都进入目标画像。
- D-06: 一份简历版本可以派生多个目标画像。
- D-07: 发起面试时必须复制目标画像快照，历史面试不能被后续修改污染。
- MODEL-02: 目标画像不仅要描述输入资料，还要冻结“岗位能力权重、目标轮次、公司流程假设”这类派生结果。
- D-17: 合理旧链路保留并纳入迁移；不合理旧链路直接改掉。
</truths>
</must_haves>

<threat_model>
- T-01-HIGH: 如果继续让 `InterviewProfileState` 同时承担画像真源、运行态和身份层，后续任何主链切换都会继续产生双真源污染。
- T-02-HIGH: 如果 `InterviewPlan.latestProfileInput` 与 `planningSummary` 继续重复保存画像主字段，schema 迁移后会出现读写口径分裂。
- T-03-MEDIUM: 如果盘点阶段不显式标记“合理旧链路 / 补丁链”，执行期会再次落回边修边猜。
</threat_model>

<tasks>
  <task id="1-01-01" type="execute">
    <title>盘点画像相关真源、快照与旧字段分布</title>
    <read_first>
      <file>src/lib/interview/config.ts</file>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>src/app/setup/page.tsx</file>
      <file>src/app/profile/page.tsx</file>
      <file>prisma/schema.prisma</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-CONTEXT.md</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md</file>
    </read_first>
    <action>在 `.planning/phases/01-foundation-migration-guardrails/` 新增或更新字段映射文档，明确 `长期基础档案`、`目标画像`、`运行时快照` 三层对象的字段边界；逐项标出当前字段来自 `InterviewProfileState`、`InterviewPlan.latestProfileInput`、`InterviewPlan.planningSummary`、`resumeText/jdText/focusAreas` 中的哪一处，并标记每个字段是保留、迁移、合并还是淘汰。</action>
    <acceptance_criteria>
      <item>文档中明确列出三层对象及字段归属，且包含 `companyName`、`targetRoleName`、`resumeSummaryMarkdown`、`projects`、`launchId`、`currentStageStatus` 等现有关键字段。</item>
      <item>文档中出现“保留 / 迁移 / 合并 / 淘汰”四类分级结果，不只描述现状。</item>
      <item>文档明确指出 `InterviewProfileState` 不再承担业务真态。</item>
    </acceptance_criteria>
  </task>

  <task id="1-01-02" type="execute">
    <title>定义目标画像真源 contract 与旧链路分级清单</title>
    <read_first>
      <file>src/lib/interview-v2/domain.ts</file>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>src/lib/interview/config.ts</file>
      <file>src/app/interview/page.tsx</file>
      <file>src/app/report/page.tsx</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-PATTERNS.md</file>
      <file>说明文档.md</file>
    </read_first>
    <action>补充一份目标画像真源 contract，写明：`一份简历版本可派生多个目标画像`、`发起面试时生成冻结快照`、`roomKey 仅作为身份层不属于画像本体`；同时列出 `setup -> profile -> interview`、`setup -> interview`、`interview -> report` 相关旧链路中的合理资产与补丁逻辑清单，为后续计划提供唯一迁移依据。</action>
    <acceptance_criteria>
      <item>contract 中明确写出“目标画像真源”“运行时快照”“身份层”三个词及其职责。</item>
      <item>清单中至少覆盖 `setup/page.tsx`、`profile/page.tsx`、`interview/page.tsx`、`report/page.tsx`、`config.ts` 五处入口或热点。</item>
      <item>清单明确标记至少 3 类应淘汰补丁模式，例如“前端缓存承担业务真态”“报告生成接口顺带状态收口”“浏览器缓存承担长期主链真相”。</item>
    </acceptance_criteria>
  </task>

  <task id="1-01-02A" type="execute">
    <title>补齐 MODEL-02 的派生结果 contract</title>
    <read_first>
      <file>.planning/REQUIREMENTS.md</file>
      <file>.planning/ROADMAP.md</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-CONTEXT.md</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md</file>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>src/app/api/parse/route.ts</file>
    </read_first>
    <action>新增一份 `MODEL-02` 派生结果 contract，明确目标画像必须冻结并可回放的 3 类派生结果：`岗位能力权重`、`目标轮次`、`公司流程假设`；为每一类写清来源、生成时机、冻结时机、存储位置、回放位置，以及与 `长期基础档案 / 目标画像 / 运行时快照` 的归属关系。</action>
    <acceptance_criteria>
      <item>文档中明确出现 `岗位能力权重`、`目标轮次`、`公司流程假设` 三项，并逐项说明来源与冻结时机。</item>
      <item>每一项都标出是写入目标画像真源、运行时快照还是其他派生层，不允许“后续再看”。</item>
      <item>文档中明确这些派生结果如何支持 `MODEL-02` 的回放与后续全流程计划生成。</item>
    </acceptance_criteria>
  </task>

  <task id="1-01-03" type="execute">
    <title>把画像与入口迁移决策同步回 Phase 1 产物</title>
    <read_first>
      <file>.planning/phases/01-foundation-migration-guardrails/01-CONTEXT.md</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md</file>
      <file>.planning/STATE.md</file>
      <file>说明文档.md</file>
    </read_first>
    <action>将字段分层、真源 contract、旧链路分级结果同步到 `01-RESEARCH.md`、必要时补充到 `01-CONTEXT.md` 的 `Claude's Discretion` 或 `Specific Ideas`，并在 `说明文档.md` 记录“画像真源与迁移清单已完成”的阶段性结果。</action>
    <acceptance_criteria>
      <item>`01-RESEARCH.md` 明确反映三层画像与旧链路分级结论。</item>
      <item>`说明文档.md` 新增一条与 Phase 1 画像/迁移盘点及 `MODEL-02` 派生结果相关的进度记录。</item>
      <item>未引入与 Phase 1 无关的新需求或新模块。</item>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- 运行 `grep -n "目标画像\|运行时快照\|身份层" .planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md`
- 运行 `grep -n "岗位能力权重\|目标轮次\|公司流程假设" .planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md`
- 运行 `grep -n "InterviewProfileState" .planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md`
- 人工检查 `说明文档.md` 是否新增本计划完成记录
</verification>

<success_criteria>
- 画像真源三层边界被明确记录，后续 schema 设计不再依赖口头约定。
- `MODEL-02` 所需的派生结果 contract 被明确记录，后续 schema 与计划生成可直接引用。
- 旧链路合理/不合理分级完成，后续执行可直接按清单保留或清理。
- 至少为 `MODEL-01`、`MODEL-02`、`MODEL-03` 提供了可执行的落地依据。
</success_criteria>
