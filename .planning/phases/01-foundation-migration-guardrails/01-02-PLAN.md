---
phase: "01"
plan: "02"
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - "prisma/schema.prisma"
  - "src/lib/interview-v2/domain.ts"
  - "src/lib/interview-v2/planService.ts"
  - "src/lib/interview/config.ts"
  - "src/app/api/v2/interview-plans/route.ts"
  - "src/app/setup/page.tsx"
  - "src/app/profile/page.tsx"
autonomous: true
requirements:
  - "MODEL-01"
  - "MODEL-02"
  - "MODEL-03"
requirements_addressed:
  - "MODEL-01"
  - "MODEL-02"
  - "MODEL-03"
---

<objective>
把“目标画像真源 + 运行时快照”从文档 contract 落到领域类型和 schema，为后续主链切换提供服务端权威结构，同时保留 brownfield 过渡所需的兼容读写。
</objective>

<must_haves>
<truths>
- D-04: 候选画像采用双层模型。
- D-06: 一份简历版本可派生多个目标画像。
- D-07: 发起面试时运行时必须复制目标画像快照。
- D-15: 旧链路判定以是否破坏画像真源、状态机权威、房间身份一致性和报告可信度为准。
</truths>
</must_haves>

<threat_model>
- T-04-HIGH: schema 设计如果继续把画像主字段塞进 `planningSummary`，后续迁移会永久保留双真源。
- T-05-HIGH: 如果没有快照层，用户修改简历后会污染历史 plan/stage/round 与 report 口径。
- T-06-MEDIUM: 如果没有 brownfield 双写兼容，现网入口会在切换期间丢失画像或房间身份。
</threat_model>

<tasks>
  <task id="1-02-01" type="execute">
    <title>在 schema 与 domain 中引入目标画像和运行时快照结构</title>
    <read_first>
      <file>prisma/schema.prisma</file>
      <file>src/lib/interview-v2/domain.ts</file>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-PATTERNS.md</file>
    </read_first>
    <action>在 `prisma/schema.prisma` 与 `src/lib/interview-v2/domain.ts` 增加服务端权威的目标画像/运行时快照结构，至少包含：简历版本标识、目标公司、目标岗位、目标级别、JD 文本、画像摘要、项目列表、快照版本、来源标识，以及 `岗位能力权重`、`目标轮次`、`公司流程假设` 三类 `MODEL-02` 派生结果；避免继续把这些字段主存于 `planningSummary` 或前端缓存。</action>
    <acceptance_criteria>
      <item>`prisma/schema.prisma` 中出现可承载目标画像真源或快照的结构化字段/实体定义。</item>
      <item>`src/lib/interview-v2/domain.ts` 中出现与 schema 对齐的目标画像/快照类型。</item>
      <item>定义中明确包含 `岗位能力权重`、`目标轮次`、`公司流程假设` 的结构化字段或子对象。</item>
      <item>定义中明确区分“画像真源”和“运行时快照”，不是同一个对象名换皮。</item>
    </acceptance_criteria>
  </task>

  <task id="1-02-02" type="execute">
    <title>让计划创建与恢复链开始读写目标画像快照</title>
    <read_first>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>src/lib/interview/config.ts</file>
      <file>src/app/api/v2/interview-plans/route.ts</file>
      <file>src/app/setup/page.tsx</file>
      <file>src/app/profile/page.tsx</file>
      <file>prisma/schema.prisma</file>
    </read_first>
    <action>修改 `planService` 与计划创建入口，让创建阶段面试/全流程/专项训练时把当前选中的目标画像复制为服务端快照；快照中同时冻结 `岗位能力权重`、`目标轮次`、`公司流程假设`；保留旧 `latestProfileInput` 过渡兼容读取，但新写入必须明确落到新快照结构；恢复运行态时优先从新快照装配，旧字段只作 fallback。</action>
    <acceptance_criteria>
      <item>`planService` 中存在“优先读新快照、旧字段 fallback”的明确分支。</item>
      <item>`setup/page.tsx`、`profile/page.tsx` 或专项训练入口到计划创建链不再只依赖浏览器缓存作为画像唯一来源。</item>
      <item>创建快照时明确冻结 `岗位能力权重`、`目标轮次`、`公司流程假设`，而不是执行期临时推断。</item>
      <item>代码中出现针对历史数据兼容的 fallback 注释或显式分支，而不是直接删除旧字段读取。</item>
    </acceptance_criteria>
  </task>

  <task id="1-02-03" type="execute">
    <title>[BLOCKING] 在 schema 修改后执行数据库 push 并校正类型</title>
    <read_first>
      <file>prisma/schema.prisma</file>
      <file>package.json</file>
      <file>tsconfig.json</file>
    </read_first>
    <action>在所有 schema 与领域类型修改完成后执行 `npx prisma db push`；如果 push 需要非交互参数，则使用当前仓库可接受的非 TTY 方式；随后重新生成并校正受影响的 TypeScript 类型引用，确保计划创建与恢复链编译通过。</action>
    <acceptance_criteria>
      <item>`npx prisma db push` 退出码为 0。</item>
      <item>变更后的 `prisma/schema.prisma` 与 TypeScript 引用可通过 `npx next build --webpack` 的相关阶段。</item>
      <item>未留下“schema 已改但数据库未推送”的半完成状态。</item>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- 运行 `npx prisma db push`
- 运行 `npx eslint src/lib/interview-v2 src/app/setup src/app/profile src/app/api/v2/interview-plans --ext .ts,.tsx`
- 运行 `grep -n "snapshot\|profile" prisma/schema.prisma src/lib/interview-v2/domain.ts`
</verification>

<success_criteria>
- 服务端具备明确的目标画像真源与运行时快照结构。
- 计划创建与恢复链开始以快照为主读写，而不是继续依赖前端运行时画像大包。
- 为 `MODEL-01`、`MODEL-02`、`MODEL-03` 提供可迁移的 schema 与代码基础。
</success_criteria>
