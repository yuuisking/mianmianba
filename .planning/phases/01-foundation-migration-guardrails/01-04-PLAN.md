---
phase: "01"
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - "src/app/setup/page.tsx"
  - "src/app/profile/page.tsx"
  - "src/app/interview/page.tsx"
  - "src/lib/interview/config.ts"
  - "src/lib/interview-v2/planService.ts"
  - "src/app/api/chat/route.ts"
  - "tests/interview-runtime.spec.ts"
autonomous: true
requirements:
  - "MODEL-03"
  - "ORCH-01"
  - "ORCH-03"
requirements_addressed:
  - "MODEL-03"
  - "ORCH-01"
  - "ORCH-03"
---

<objective>
完成 `setup -> interview` 主链切换，让阶段面试、全流程和专项训练都基于唯一目标画像快照和唯一房间身份进入运行时，并把前端业务真态下沉为 UI 态。
</objective>

<must_haves>
<truths>
- D-09: 前端只保留界面态，不再持有业务真态。
- D-13: brownfield 迁移采用先盘点后分级。
- D-14: 第一优先主链是 `setup -> interview`。
- D-16: 优先清理绕开真源和状态机的补丁链。
</truths>
</must_haves>

<threat_model>
- T-10-HIGH: 如果入口链仍有 `setup -> profile -> interview` 与 `setup -> interview` 两套口径，任何后续状态或报告切换都会继续双写错乱。
- T-11-HIGH: 如果前端继续缓存 `currentStageStatus/currentRoundStatus` 作为真态，服务端迁移结果会被 UI 覆盖。
- T-12-MEDIUM: 如果保留旧 `launchId` 作为主要身份来源，恢复链仍可能出现房间漂移。
- T-12A-MEDIUM: 如果专项训练继续游离在共享画像和状态机之外，Phase 1 的 `MODEL-03 / ORCH-01` 仍是不完整的。
</threat_model>

<tasks>
  <task id="1-04-01" type="execute">
    <title>统一 setup、profile 与专项训练入口的画像快照与房间身份</title>
    <read_first>
      <file>src/app/setup/page.tsx</file>
      <file>src/app/profile/page.tsx</file>
      <file>src/app/practice/page.tsx</file>
      <file>src/lib/interview/config.ts</file>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>src/app/interview/page.tsx</file>
      <file>.planning/phases/01-foundation-migration-guardrails/01-RESEARCH.md</file>
    </read_first>
    <action>调整 `setup`、`profile` 与专项训练入口，使阶段面试、全流程、专项训练创建链都显式传递目标画像快照标识和 `plan/stage/round/roomKey` 身份；将 `launchId` 降为过渡关联键，不再作为运行时主要身份来源；保留现网必要 fallback，但新主链必须优先使用服务端快照与 `roomKey`。</action>
    <acceptance_criteria>
      <item>阶段面试、全流程、专项训练的创建链都能在代码中找到统一的 `roomKey + plan/stage/round` 或等效身份传递路径。</item>
      <item>`launchId` 不再作为主运行时身份唯一来源。</item>
      <item>至少一个旧入口分叉被合并或显式标记为过渡 fallback，且专项训练被标明是共享主链还是过渡模式。</item>
    </acceptance_criteria>
  </task>

  <task id="1-04-02" type="execute">
    <title>将前端业务真态下沉为 UI 态并改造恢复逻辑</title>
    <read_first>
      <file>src/lib/interview/config.ts</file>
      <file>src/app/interview/page.tsx</file>
      <file>src/app/api/chat/route.ts</file>
      <file>src/lib/interview-v2/planService.ts</file>
      <file>tests/interview-runtime.spec.ts</file>
    </read_first>
    <action>收缩 `InterviewProfileState` 或等效前端运行时对象，使其只保留输入草稿、滚动位置、等待态、sessionId/roomKey 等 UI 辅助信息；把 `currentStageStatus/currentRoundStatus` 等业务真态改为从服务端恢复；更新相关恢复逻辑与测试，确保刷新后仍能回到正确房间与阶段。</action>
    <acceptance_criteria>
      <item>`config.ts` 中前端运行时对象不再把 `currentStageStatus/currentRoundStatus` 当作主业务真态来源。</item>
      <item>`interview/page.tsx` 恢复链优先从服务端读取当前阶段/轮次状态。</item>
      <item>相关运行时测试通过，至少覆盖刷新后恢复与继续面试场景。</item>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- 运行 `npx eslint src/app/setup src/app/profile src/app/interview src/lib/interview --ext .ts,.tsx`
- 运行 `npx eslint src/app/setup src/app/profile src/app/practice src/app/interview src/lib/interview --ext .ts,.tsx`
- 运行 `npm run test:interview-runtime`
- 人工验证从 setup 发起阶段面试、全流程、专项训练各一次，刷新后仍进入正确房间或正确过渡入口
</verification>

<success_criteria>
- `setup -> interview` 主链由唯一画像快照与唯一房间身份驱动，且专项训练被纳入同一主链口径或明确过渡策略。
- 前端不再持有业务真态，刷新与恢复主要依赖服务端。
- 为 `MODEL-03`、`ORCH-01`、`ORCH-03` 打通第一条迁移主链。
</success_criteria>
