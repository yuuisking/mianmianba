- [x] 用户进入 `mode=video` 时，浏览器能够正确弹出包含**摄像头和麦克风**的权限请求
- [x] 用户同意授权后，本地界面能够正常渲染出带画面的 `<video>`（画中画或并排结构），且无回音（muted）
- [x] 现有的语音打断、ASR 和 TTS 逻辑在视频模式下依然正常工作
- [x] 当用户点击挂断按钮时，摄像头的绿色指示灯能够立刻熄灭
- [x] 用户拒绝摄像头权限时，页面能够给出友好的错误提示，而不是直接白屏崩溃
