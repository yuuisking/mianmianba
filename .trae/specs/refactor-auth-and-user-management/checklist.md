- [x] `/home` 被定义为默认公开入口，未登录用户访问首页不会被强制重定向到 `/login`。
- [x] 匿名用户可以访问站点首页与功能页框架，但在触发受保护动作时才被要求登录或注册。
- [x] 认证方案同时覆盖登录弹层模式与独立 `/login` 页面，并定义登录成功后的回流与原操作续接。
- [x] 用户数据模型包含角色、账号状态、VIP 类型、VIP 到期时间、昵称、头像、最近登录时间等基础字段。
- [x] 管理员后台用户管理系统的页面、入口与接口权限规则已定义清楚，且仅 `admin` 可见与可用。
- [x] 用户详情页的信息结构、入口方式、查看范围与可编辑范围已定义清楚。
- [x] 登录/注册弹层、用户详情页、后台用户页的视觉规范已按 `brand-guidelines` 约束。
- [x] PostgreSQL 自动备份、恢复验证、数据库密码轮换与认证密钥轮换方案已纳入本轮设计。
- [x] 关键场景与异常场景验证范围完整，且明确要求不破坏现有用户数据与当前登录能力。
