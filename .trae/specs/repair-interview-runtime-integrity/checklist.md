- [x] 新的 `planId + stageId + roundId + mode` 面试链接进入房间时，不会恢复其他面试的历史消息或旧缓存
- [x] 刷新当前面试房间时，只会恢复当前房间对应的快照，不会串轮次、串候选人、串模式
- [x] 数据库正式消息与本地快照并存时，系统会按当前房间隔离键正确恢复，不会被“最近一次本地快照”覆盖
- [x] 首题和后续追问只会引用当前候选人的真实项目、经历和能力点，不会出现其他人的项目名
- [x] 当当前证据不足时，系统会先澄清或要求补充信息，而不是围绕错误项目继续深挖
- [x] 面试开场语自然通顺，用户侧不再看到 `**`、原始 Markdown 或格式错乱
- [x] 文字面试中的强调、高亮和换行展示符合统一渲染规则
- [x] 候选人连续消极回答时，面试官可以主动结束当前面试，并记录结束原因
- [x] 主动结束的结果会写入当前轮次状态、报告上下文和后续可消费结构
- [x] 候选人表现积极且满足阈值时，系统会弹出算法题入口而不是静默跳过
- [x] 候选人确认进入算法题后，会进入限时算法题流程，并将结果回写到当前评估链路
- [x] 算法题页面达到真实可答、可跑、可提、可判的产品标准，而不只是存在 `CodingSession` 数据模型
- [x] Agent 蓝图、运行时真实执行链和对外能力描述保持一致，不会把占位角色包装成已完整上线能力
- [x] Planner、Composer、Guard 与主面试官 prompt 已通过自然度、错误项目拦截和内部词泄漏校验
- [x] 会话隔离、首题锚定、文案渲染、主动结束、算法题触发已有可重复执行的自动化回归
- [x] 本轮修复不会破坏既有文字面试、实时面试、全流程轮次恢复和报告生成主链
