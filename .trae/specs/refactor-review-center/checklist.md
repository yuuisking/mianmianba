# 验收清单 (Checklist)

- [x] `/dashboard` (控制台) 页面的训练记录最多只展示最近的 3 条，且不再有底部分页。
- [x] `/dashboard` 底部有一个明显的链接/按钮指向 `/review`。
- [x] `/review` (复盘中心) 页面成功接入真实的后端 `/api/sessions/recent` 数据。
- [x] `/review` 页面的“历史面试记录”列表拥有完整的分页、搜索和删除能力。
- [x] `/review` 页面的“错题本 (高频薄弱维度)”不再是死数据，而是根据用户真实的面试报告（维度评分 < 7，或提取的 risks）动态生成。
- [x] 错题本为空时，有良好的空状态展示。
- [x] 点击错题本中任何一项的“专项突破”按钮，能正确跳转到 `/interview?mode=targeted` 房间，并携带了该薄弱项的 `topic` 和 `desc`。