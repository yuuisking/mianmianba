# 验收清单 (Checklist)

- [x] 顶部导航栏新增了“专项训练”入口。
- [x] 独立页面 `/practice` 包含了一个高级感、居中、无杂乱元素的表单（基于 `brand-guidelines`）。
- [x] 表单强制用户输入至少“面试方向”和“训练主题”，否则按钮不可点击。
- [x] 点击“开始专项训练”按钮后，正确跳转到 `/interview` 房间，并且顶部显示“专项训练 (输入的主题)”。
- [x] 房间内的 AI 面试官成功化身为“高级 [输入的方向] 辅导官”，并围绕输入的 `topic` 和 `desc` 开始定向压测，不需要做自我介绍。