# 验收清单 (Checklist)

- [x] `/api/parse/route.ts` 解析出的 JSON 中包含准确的 `role`（如：后端开发、Java 工程师、测试开发等）。
- [x] `/api/chat/route.ts` 已经完全删除了硬编码的“前端开发面试官”字样，能根据传入的 `profile.role` 动态调整大模型的身份。
- [x] `/profile`（候选人画像确认页面）包含一个清晰的可编辑文本框，允许用户修改 AI 识别出的岗位方向（Role）。
- [x] 修改后的岗位能够被正确保存并在进入面试房间后传递给大模型（AI 开始提问对应岗位方向的问题，而不是前端问题）。
- [x] `/profile` 页面的整体视觉效果得到了大幅简化与美化，遵循了 `brand-guidelines`（无杂乱的色块、宽裕的间距、高级的版式设计）。