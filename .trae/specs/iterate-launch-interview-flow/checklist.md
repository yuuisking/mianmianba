- [x] 本次改动范围只覆盖“发起面试”菜单及其直接关联链路，没有影响其他现有功能。
- [x] 发起面试时简历变为必传，未上传简历无法继续解析或进入下一步。
- [x] 解析确认页不再只显示简历文件名，而是展示简短 Markdown 简历摘要，并保持用户画像为主展示。
- [x] 简历、JD、技术栈、项目、差距预警、专项训练重点等关键数据一旦拿不到，系统会明确提示，且不再展示任何 mock 内容。
- [x] 原语音/视频面试已合并为一个统一的实时面试模式，用户进入后可直接音频交流，并可选择是否开启本地视频。
- [x] 实时面试房间体验已按会议式交互重构，但整体视觉风格仍与当前系统和 `brand-guidelines` 保持一致。
- [x] 所有面试模式都支持统一的题量或时长配置，并在达到有限上限后自动结束。
- [x] 所有面试模式都提供 `不限` 选项，选择后系统不会因题量或时长自动结束。
- [x] 用户在面试中点击其他菜单或离开当前页时，会看到“是否结束本次面试”的确认提示。
- [x] 用户确认结束面试后，系统会真实结束当前面试；对于已经形成有效会话的场景，会写入或更新面试记录。
- [x] 系统已建立统一的 prompt 组装机制，面试模式、解析结果、用户画像、专项训练重点和题量/时长策略都会真实进入上下文。
- [x] 专项训练重点会真实进入 prompt，并显著影响 AI 面试官的提问方向。
